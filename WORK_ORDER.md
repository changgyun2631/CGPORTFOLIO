# 작업명령서 — CGPORTFOLIO

최종 갱신 2026-09-17 · 기준은 `main`과 `HANDOFF.md` 12절 · **다음 담당자는 Claude**

이 문서는 **아무 맥락 없이 처음 이 프로젝트를 받은 사람**이 읽고 바로 일할 수 있도록
쓰였다. 순서대로 읽을 것. 0절만 읽어도 30분치 삽질은 줄어든다.

같이 읽을 문서: **`HANDOFF.md`** (지금까지 만든 것·검증된 것·이미 잡은 버그).
특히 `HANDOFF.md` J절(최신 재검토), 5절(검증 상태), 8절(이미 잡은 버그)은 읽지 않고 손대면
같은 실수를 반복하게 된다.

---

## 0. 먼저 알아야 할 것 (필독)

### 0-1. 이 프로젝트가 뭔가

개인 자산 포트폴리오 대시보드. Next.js 16 + React 19 + TypeScript + Tailwind v4.
**실제 사용자의 실제 미국주식 계좌 데이터가 들어 있다** (2026-09-15부터).
숫자가 틀리면 아무 가치가 없는 프로젝트다 — 계산 정확성이 전부다.

### 0-2. 경로 — 헷갈리면 여기부터 확인

| 무엇 | 경로 |
|---|---|
| **작업 폴더 (여기서 일한다)** | `C:\Users\ACC-002\Desktop\cgportfolio` (브랜치 `main`) |
| 낡은 워크트리 (쓰지 말 것) | `C:\Users\ACC-002\Desktop\cgportfolio\.claude\worktrees\continue-previous-work-f7f06a` |
| 백업 | `C:\Users\ACC-002\cgportfolio-backups\` (매일 새벽 3시 자동) |
| 시세 갱신 로그 | `C:\Users\ACC-002\cgportfolio-logs\refresh.log` |
| API 키 | `C:\Users\ACC-002\Desktop\cgportfolio\.env.local` (gitignore됨) |

**⚠ 워크트리 주의**: 이전 작업은 `.claude/worktrees/...` 안에서 진행됐고 2026-09-15에
메인으로 병합됐다. 워크트리는 이제 낡은 사본이다. 거기서 고치면 사용자 화면에 반영되지
않는다. 반드시 `C:\Users\ACC-002\Desktop\cgportfolio`에서 작업할 것.

### 0-3. 지금 이 순간 돌아가고 있는 것

**서버가 이미 떠 있다. 새로 띄우기 전에 반드시 확인할 것.**

```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/
```

| 포트 | 정체 | 누가 띄웠나 |
|---|---|---|
| **3000** | 프로덕션 서버 (`npm start`) | Windows 작업 스케줄러 "CGPORTFOLIO 서버" |
| **3200** | dev 서버 (`next dev --port 3200`) | **정체 불명.** 이전 담당자가 띄운 게 아니다. 같은 프로젝트를 dev 모드로 서빙 중 |

포트 3200은 건드리지 말 것 (사용자나 다른 세션의 작업일 수 있다). 다만 **두 서버가 같은
`data/`를 공유**하므로, 3200에서 본 화면과 3000에서 본 화면이 다르면 그것 때문이다.

**등록된 Windows 작업 스케줄러 작업 4개** (`Get-ScheduledTask -TaskName "CGPORTFOLIO*"`):

| 작업 이름 | 주기 | 하는 일 |
|---|---|---|
| `CGPORTFOLIO 서버` | 매일 04:00 + **로그온 시(30초 지연)** | `scripts/start-server.cmd` → `npm start` (포트 3000) |
| `CGPORTFOLIO 시세 갱신` | 6시간마다 (02/08/14/20시) | `scripts/refresh-quotes.mjs` → `/api/cron/refresh` 접수 후 상태 조회 |
| `CGPORTFOLIO 데이터 백업` | 매일 03:00 | `scripts/backup-data.mjs` → `~/cgportfolio-backups/` |
| `CGPORTFOLIO 주간 리포트` | 매주 월 09:00 | `scripts/generate-weekly-report.mjs` → `/api/cron/weekly-report` 호출 |
| `CGPORTFOLIO 받은CSV 가져오기` | 10분마다 | `scripts/import-inbox.mjs` → `~/cgportfolio-inbox`의 증권사 CSV 자동 반영 |

로그온 트리거는 "재부팅하면 다음 날 새벽 4시까지 서버가 안 뜬다"를 막으려고 추가했다.
다만 이 작업은 "로그인한 사용자로 실행"이라 **부팅만 하고 로그인을 안 하면 안 뜬다** — 수용 위험.

프로덕션 빌드라 **소스를 고쳐도 화면에 반영되지 않는다.** 반영하려면:

```bash
npm run build
```

그 다음 포트 3000 프로세스를 죽이고 작업을 다시 시작해야 한다 (아래 0-6 참고).

### 0-4. 이 환경의 함정 (전부 실제로 겪은 것들)

1. **Avast 안티바이러스가 TLS를 가로챈다.** `curl`이 인증서 폐기 검사에서 실패한다.
   외부 HTTPS를 부를 때는 `curl --ssl-no-revoke`를 쓸 것. Node의 `fetch`는 영향 없다.
2. **Git Bash에서 `node`에 `/tmp/파일` 경로를 넘기면 `C:\tmp\파일`로 해석된다.**
   Node 스크립트에 경로를 넘길 때는 Windows 절대경로를 쓸 것.
3. **Git Bash에서 `taskkill /F /PID`는 `//F //PID`로 써야 한다** (경로 변환 때문).
4. **Windows 작업 스케줄러에서 `ONLOGON` 트리거는 등록이 거부된다** (`Access is denied`).
   시간 기반(`DAILY`, `ONCE`+반복)은 정상 등록된다. 그래서 서버 작업이 "매일 04:00"이다.
5. **`Register-ScheduledTask`에 `[TimeSpan]::MaxValue`를 넘기면 XML 오류가 난다.**
   반복 기간은 유한값(예: `New-TimeSpan -Days 3650`)을 쓸 것.
6. **Bash 도구에서 `cmd | tail && echo OK` 형태는 앞 명령이 실패해도 OK가 찍힌다**
   (파이프의 종료코드는 마지막 명령 것). 종료코드를 봐야 하면 파이프 없이 실행하고 `$?`를 볼 것.
7. **`npm test`/`eslint`를 메인에서 돌릴 때 중첩 워크트리까지 스캔되던 문제**는
   `eslint.config.mjs`/`vitest.config.mts`에 제외 규칙을 넣어 해결됨. 그 규칙 지우지 말 것.
8. **`.cmd` 배치 파일에 한글 `ECHO` 텍스트를 넣으면 `cmd.exe`가 잘못 해석해 스크립트가
   죽을 수 있다.** 실제로 `scripts/start-server.cmd`에 한글 로그 메시지(em dash 포함)를
   넣었다가 `ERROR: Input redirection is not supported, exiting the process
   immediately.`로 즉시 죽는 걸 겪었다. `REM` 주석의 짧은 한글은 괜찮지만, 파일에
   `ECHO`로 쓰는 텍스트는 영문 ASCII만 쓸 것. `.cmd` 파일을 고치면 Task Scheduler를
   거치지 않고 `cmd.exe /c "경로\파일.cmd"`로 먼저 직접 실행해 몇 초 안에 죽지
   않는지 확인한 뒤 배포할 것.
9. **`taskkill`로 자식 프로세스만 죽이면 Task Scheduler가 그 작업을 계속 "Running"으로
   착각해 새 실행을 거부할 수 있다** (`Start-ScheduledTask` 결과가 "이미 실행 중" 코드로
   계속 나옴). `Stop-ScheduledTask -TaskName "..."`로 먼저 정리한 뒤 다시 시작할 것.
10. **`Stop-ScheduledTask`가 작업을 "Ready"로 표시해도 실제 `node.exe`는 안 죽을 수
    있다** — 오래 걸리는 요청(시세 갱신처럼 몇 분짜리)을 처리 중이면 종료 신호를
    곧바로 못 받는다는 게 최초 가설이었지만, **2026-09-16 09:24에 진행 중인 요청이
    전혀 없는 상태에서도 같은 일이 또 일어났다** — `Stop-ScheduledTask` 직후
    `netstat`로 보니 이전 PID가 여전히 `LISTENING` 상태였다. 즉 "느린 요청 때문"은
    충분조건이 아니라 그럴듯한 사례 하나였을 뿐이고, **이 환경에서
    `Stop-ScheduledTask`는 일반적으로 신뢰할 수 없다**고 보는 게 안전하다(정확한
    근본 원인은 못 찾음 — Task Scheduler가 자식 프로세스를 추적하는 방식과 관련
    있을 가능성). 안 죽은 상태에서 `Start-ScheduledTask`로 새 인스턴스를 띄우면
    포트가 이미 물려 있어 `EADDRINUSE`로 즉시 죽고, 재시작 루프가 10초마다 그걸
    반복해 "짧은 반복 재시작" 구간을 만든다(2026-09-16 09:00경 최초 재현,
    `WORK_ORDER.md` B-0 참고). **`Stop-ScheduledTask` 다음엔 절대 바로
    `Start-ScheduledTask`를 부르지 말고, 매번 `netstat -ano | grep
    ":3000.*LISTENING"`으로 포트가 실제로 비었는지 먼저 확인할 것.** 안 비어 있으면
    남은 PID를 `taskkill //F //PID <PID>`로 정리한 뒤 시작한다(2026-09-16 09:24
    사례처럼 매번 필요할 수 있다고 가정할 것 — 예외가 아니라 기본 절차로 다룰
    것). 시세 갱신 요청이 진행 중일 때는 아예 재시작을 미룰 것.
11. **`c54ee4f`(부팅 시 자동 기동)로 바뀐 뒤, 일반 사용자 세션의 `taskkill`/
    `Stop-Process`로 서버 프로세스를 못 죽일 수 있다.** 부팅 트리거로 뜬
    `node.exe`는 세션 0(비대화형 서비스 세션)에서 돈다 — `tasklist /FI "PID eq
    <PID>" /V`로 `Session Name`이 `Services`인지 확인하면 알 수 있다. 이 경우
    일반 PowerShell/Git Bash에서 `taskkill //F //PID <PID>`를 실행하면 프로세스가
    실제로 존재하는데도 **"액세스가 거부되었습니다"**로 막힌다(2026-09-21 실제
    재현 — `Stop-ScheduledTask`도 마찬가지로 소용없었다). **관리자 권한으로 연
    PowerShell/cmd**에서 `taskkill /F /PID <PID>` → `Start-ScheduledTask
    -TaskName "CGPORTFOLIO 서버"` 순으로 실행해야 한다. Claude 세션이 일반 권한
    도구만 쓴다면 이 단계는 사용자에게 직접 요청할 것 — 억지로 우회하려 하지 말 것.

### 0-5. 시세 API 한도 — 제일 자주 밟는 지뢰

**Twelve Data 무료 플랜은 분당 8크레딧이고, 심볼 1개 = 1크레딧이다.**
여러 심볼을 한 URL에 묶어 보내도 크레딧은 심볼 수만큼 그대로 든다.
보유 종목이 28개라 한 번에 다 요청하면 **무조건 429**가 난다.

`lib/providers/twelve-data.ts`가 **8개씩 나눠 61초 간격으로** 보내도록 되어 있다.
그래서 `/api/cron/refresh` 한 번이 **약 3분 10초** 걸린다. 느린 게 아니라 정상이다.
타임아웃을 짧게 잡고 "실패했다"고 오판하지 말 것.

- 하루 한도 800크레딧. 현재 하루 4회 × 63심볼 = 252크레딧 사용 (여유 있음)
- 테스트한다고 반복 호출하면 분당 한도에 걸려 한동안 아무것도 안 된다

### 0-5a. 시세 갱신 프로토콜 — 왜 접수/조회로 나뉘어 있나

보유 종목이 늘어 한 번 갱신에 **약 7분**이 걸린다. 예전에는 호출 스크립트가 그 시간 내내
응답 하나를 붙잡고 기다렸는데, **Undici의 `headersTimeout`이 5분**이고 요청 옵션으로 못
늘린다. 그래서 정상 성공한 갱신도 `UND_ERR_HEADERS_TIMEOUT`으로 끊겨 작업 스케줄러에는
`LastTaskResult=1`(실패)로 남았다(2026-09-16 23:00 UTC 실행 기록이 그 예다: 스크립트는
23:05에 끊겼고 서버는 23:07:19에 정상 완료). `AbortSignal`을 늘리는 건 해결이 아니다.

**지금 구조**

| 단계 | 주소 | 응답 |
|---|---|---|
| 접수 | `GET /api/cron/refresh` | 즉시 `202 {jobId}` — 작업은 서버 안에서 계속 돈다 |
| 조회 | `GET /api/cron/refresh/status?jobId=…` | 즉시 `{job}` |

- 요청 타임아웃 30초, 조회 간격 15초, 작업 한도 20분(넘기면 매달림으로 보고 실패).
- **중복 실행 방지**: 이미 도는 작업이 있으면 접수가 `409 code=duplicate`. `withDataLock`은
  쓰기 단계만 막으므로, 그 앞의 외부 API 호출까지 두 벌 도는 것을 이 단일 실행이 막는다.
- **성공 판정**: 작업이 `done`이고 **`quotes.json`·`fx-quote.json`·`snapshots.json` 세 파일의
  mtime이 작업 시작 이후**여야 exit 0. "완료 보고"만으로는 성공으로 치지 않는다.
- **종료 코드**(`scripts/lib/refresh-diagnostics.mjs`의 `REFRESH_EXIT`): 0 성공 / 1 서버 문제 /
  2 시세 공급자 실패 / 3 시간 초과 / 4 중복 / 5 완료 보고했으나 데이터 그대로.
- 작업 상태 파일은 `~/cgportfolio-logs/refresh-job.json`. 단계·건수·소요시간·원인 코드만
  쓰고 평가금액 같은 값은 쓰지 않는다. 서버가 중간에 죽으면 조회가 `failed`로 답한다.

### 0-5b. 그 밖에 확정된 운영 규칙

- **리포트 라우트는 동적이다.** `/reports`와 `/reports/[slug]` 모두 `force-dynamic` —
  예약 실행이 만든 주간 리포트가 **재빌드 없이** 바로 보여야 하기 때문이다.
- **주간 리포트 흐름**: 월요일 09:00 → `scripts/generate-weekly-report.mjs` →
  `/api/cron/weekly-report` → `lib/data/views.ts`의 `loadWeeklyReportInput()`(화면과 같은
  계산) → `data/weekly-reports.json`(**gitignored**, 본문에 실제 비중이 들어간다).
  같은 주에 다시 돌리면 그 주 글을 덮어쓴다. 쓰기 실패 시 5초 뒤 1회 재시도.
- **QQQ 실효 노출**: 배수표는 `lib/domain/exposure.ts`(QLD 2, TQQQ 3, QQQ 1, 나머지 0).
  표에 없는 종목은 조용히 0이 되므로, 새 레버리지 상품을 사면 여기에 먼저 적어야 한다.
  과거 시점은 거래를 다시 돌려 그날 종가·환율로 평가하고, 비중의 분모는 그날 스냅샷의
  실제 예탁자산을 쓴다.
- **낙폭(MDD)은 두 가지다.** 화면 카드의 `평가액 낙폭`은 평가금액 고점 대비라 입출금이
  섞여 있다. 주간 리포트의 낙폭은 `cashflowAdjustedDrawdown` — 스냅샷 사이 `principalKrw`
  변화를 외부 현금흐름으로 보고 제거한 수익지수 기준이다. 실데이터에서 두 값은 눈에 띄게
  다르다(납입을 계속하는 계좌라 그렇다). **둘을 섞어 쓰지 말 것.** 실제 수치가 필요하면
  로컬에서 직접 계산할 것 — 이 문서는 커밋되므로 성과 수치를 적지 않는다.
- **임시 파일**: `writeJsonAtomic`은 rename이 실패해도 임시 파일을 지운다. 이중 안전장치로
  `.gitignore`에 `/data/.*.tmp-*`도 넣어 뒀다(내용이 대상 파일과 같아 계좌 데이터가 들어간다).
- **LAN 공개·무인증은 수용 위험이다.** `0.0.0.0:3000`으로 열려 있고 인증이 없다 —
  2026-09-16에 사용자가 "그냥 다 오픈된 페이지여도 상관없다"고 명시적으로 선택했다.

### 0-6. 자주 쓰는 명령

```bash
cd C:\Users\ACC-002\Desktop\cgportfolio
```

검증 4종 세트 (작업 후 매번):

```bash
npx tsc --noEmit && npx eslint . && npm test && npm run build
```

서버 재시작 (소스 수정 후 반영하려면):

```bash
netstat -ano | grep ":3000" | grep LISTENING
```

PID를 확인한 뒤 (Git Bash 기준 슬래시 2개):

```bash
taskkill //F //PID <PID>
```

그 다음 PowerShell에서:

```powershell
Start-ScheduledTask -TaskName "CGPORTFOLIO 서버"
```

시세 수동 갱신 (약 3분 소요):

```bash
node scripts/refresh-quotes.mjs
```

데이터 수동 백업:

```bash
npm run backup
```

---

## A. 작업 규칙

### 코드

1. **주석은 한국어로, "무엇"이 아니라 "왜"를 적는다.** 기존 코드의 주석 밀도와 어조를
   맞출 것. 코드를 읽으면 아는 내용은 주석으로 달지 않는다.
2. **차트 라이브러리를 도입하지 않는다.** 모든 차트는 SVG 직접 구현이다. 이건 취향이
   아니라 결정 사항이다 — 번들 크기와 서버 렌더 일관성 때문.
3. **의존성 추가는 최소화한다.** 현재 런타임 의존성은 `next`, `react`, `react-dom`,
   `server-only` 4개뿐이다. 추가하려면 왜 직접 못 쓰는지 먼저 설명할 것.
   (개발 의존성은 Vitest가 추가돼 있다.)
4. **과거 이력과 현재 잔고의 기준을 섞지 않는다.** 매매·입출금·배당 이력은 원장에서
   파생하고, 현재 보유수량·평단·매입원가는 증권사 잔고 스냅샷을 우선한다.
   (`HANDOFF.md` 원칙 1)
5. **페이지에서 외부 API를 직접 부르지 않는다.** 시세는 cron만 가져온다. (원칙 2)
6. **모듈 수준 캐시를 쓰지 않는다.** 요청 경계에서 비워지는 `react cache`를 쓸 것.
7. 반응형 그리드에는 항상 `grid-cols-1`을 기본으로 둔다.
8. **계산 로직을 고치면 `lib/domain/__tests__/`에 경계 사례 테스트를 같이 추가한다.**

### 데이터

9. **개인 금융 데이터를 커밋하지 않는다.** `.gitignore`의 해당 블록을 지우거나
   `git add -f`로 강제 추가하지 말 것. 저장소는 **Public**이다.
   커밋 전 `git status`로 `data/*.json`이 안 잡히는지 반드시 확인.
9-1. **`.md` 문서·커밋 메시지에도 실제 금액·수량·평단을 적지 않는다.** gitignore는
   `data/`만 막아줄 뿐, 문서에 옮겨 적은 숫자는 그대로 커밋된다.
   실제로 2026-09-15에 인계 문서를 쓰면서 총 평가금액과 특정 종목의 보유수량·평단을
   적었다가 푸시 직전에 발견해 지웠다. 예시가 필요하면 `<보유수량>` 같은 자리표시자를
   쓸 것. 푸시 전 확인:

```bash
git diff origin/main..main | grep -nE "[0-9]{3},[0-9]{3}|\"shares\":|\"price\":"
```
10. 스키마를 바꾸면 `data/sample/`도 같이 갱신한다. 새로 클론한 사람이 못 돌리면 안 된다.
11. **`data/`를 통째로 덮어쓰기 전에 `npm run backup`을 먼저 돌린다.**

### 진행

12. **푸시·배포·외부 서비스 연동은 사용자 승인을 받고 한다.** 커밋까지는 진행해도 된다.
    2026-09-16 인계 커밋은 로컬에만 만들었다. 정확한 차이는
    `git rev-list --left-right --count origin/main...HEAD`로 확인하고, 사용자가
    명시적으로 요청할 때만 푸시할 것.
13. **검증하지 않은 것을 "완료"라고 보고하지 않는다.** 돌려보지 않았으면 그렇게 적을 것.
    이 프로젝트는 앞선 담당자들이 미검증 항목을 명시해 둔 덕에 인계가 이어졌다.
14. 작업이 끝나면 `HANDOFF.md`의 5절(검증 상태)을 갱신한다.

---

## B. 남은 작업

**우선순위 순.** 지금 사용자의 최우선 관심사는 **B-0(서버 안정성 관찰)**이다.

2026-09-16 추가 보완 감사 결과와 각 완료 조건은 `HANDOFF.md`의
**“1-1-H. 추가 보완 작업 — Claude 우선순위 제안”**에 정리했다. 새 기능보다 먼저
P0(가져오기 검증, 원자적 JSON 쓰기, 백업 복원 훈련, 데이터 신선도 표시)을 검토할 것.

2026-09-17 Claude 마무리 뒤 GPT가 코드·테스트·운영 로그와 실제 예약 작업을 다시
대조했다. **현재 최우선 판정은 `HANDOFF.md` 12절**이다. I·J·11절은 당시 작업 기록이며,
12절에서 남긴 항목을 이전 완료 표기보다 우선한다. Claude는 아래 B-0B부터 처리한다.

후속 계좌 원본 감사에서 메인 운영 데이터는 Claude 작업 전 백업과 동일했지만, Claude의
별도 seed worktree가 공용 백업 폴더에 운영본이 아닌 최신 백업을 만든 사실을 확인했다.
`2026-09-16T01-14-58-657Z`는 운영 복원에 사용하지 말고, 현재 운영본과 전체 해시가 같은
`2026-09-16T01-08-34-400Z`를 기준 후보로 삼는다. 자세한 근거와 보완조건은 HANDOFF I절.

완료된 항목(P0-1, P0-2, P1-1, P2-1)은 이 문서 맨 아래 "부록: 완료된 작업"에 요약만
남겼다. 상세 검증 내역은 `HANDOFF.md` 5절에 있다.

---

### B-0B. Claude 후속 보강 작업 — ✅ 2026-09-17 완료 (`a1966ee`)

상세 원인·독립 검증 근거·완료 조건은 `HANDOFF.md` 12절을 따른다. 아래 세 항목만 먼저
처리하고, 실제 계좌 수치나 운영 데이터를 테스트·문서·커밋에 넣지 않는다.

1. **P1 — MDD의 원금 누락/존재 혼합 구간 수정**
   - `cashflowAdjustedDrawdown()`에서 이전 스냅샷에 `principalKrw`가 없고 현재에만 있는
     경우 현재 원금 전체를 순입금으로 오인한다.
   - 원금 두 값이 모두 유효할 때만 차이를 현금흐름으로 사용하도록 정책을 명시하고,
     이전만 없음·현재만 없음·둘 다 없음·둘 다 있음·비정상 값 테스트를 추가한다.
   - 현재 631개 운영 스냅샷에는 원금이 모두 있어 현행 결과에는 영향이 없지만, 과거 형식
     자료가 다시 들어왔을 때 조용히 틀어지는 것을 막는 작업이다.
2. **P1 — 원자적 저장의 임시파일 삭제 실패 진단**
   - TS/mjs 양쪽에서 rename 실패 후 unlink까지 실패하면 현재 아무 진단도 남지 않는다.
   - 원래 rename 예외를 유지하면서 삭제 실패 코드와 basename만 안전하게 보고한다.
   - rename+unlink 연속 실패 주입 테스트를 추가하고 `.gitignore` 규칙은 유지한다.
3. **P2 — 갱신 중 서버 재시작 상태 복구 테스트**
   - persisted `running` 상태 뒤 모듈 메모리를 초기화한 테스트에서 조회가 `failed`가 되는지
     확인한다.
   - 손상된 상태 JSON을 단순 미존재와 구분해 진단하고 상태 저장의 원자적 교체를 검토한다.
   - 상태 라우트 인증 및 재시작 응답을 라우트 수준에서 테스트한다. 운영 서버 강제 종료는
     사용자 승인 없이 하지 않는다.

**처리 결과**: 세 항목 모두 구현·회귀 테스트 완료. 전체 테스트는 42개 파일 367개가
통과했고 lint·TypeScript·데이터·백업 검증도 통과했다. 새 빌드는 09:40부터 프로덕션에서
정상 응답한다. 실제 운영 서버 강제 종료는 수행하지 않았고, 새 빌드 이후 첫 14:00 예약
갱신은 아직 실행 전이라 운영 관찰만 미확인이다. 상세 근거는 `HANDOFF.md` 12-6절.

검증은 `npm test`, `npm run lint`, `npx tsc --noEmit`, `npm run build`,
`node scripts/validate-data.mjs`, `npm run backup:verify`, `git diff --check`,
`git status --short` 전부다. 소스 반영으로 서버를 재배포했다면 `/status`와 리포트
목록·상세·404를 다시 확인하고, 다음 예약 갱신을 실제로 관찰하지 않았다면 그렇게 적는다.

---

### B-0A. Claude 즉시 보강 작업 — 2026-09-16 GPT 2차 재검토

이 묶음이 현재 최우선 작업이다. **새 기능·배포·장기 종가·ETF 자동화보다 먼저**
처리한다. 한 번에 전부 크게 갈아엎지 말고 아래 순서대로 작은 커밋으로 나눈다.
단, 사용자가 별도로 요청하지 않으면 커밋까지만 하고 푸시하지 않는다.

#### B-0A-0. 시작 전 확인

1. 반드시 메인 작업 폴더에서 시작한다.

```powershell
Set-Location 'C:\Users\ACC-002\Desktop\cgportfolio'
git status --short --branch
git log -5 --oneline --decorate
git rev-list --left-right --count origin/main...HEAD
```

2. 작업 트리가 예상과 다르면 기존 사용자 변경을 덮지 말고 먼저 구분한다.
3. Next.js 코드를 고치기 전에 이 설치본의 `node_modules/next/dist/docs/`에서 관련
   Server Actions/Route Handler/설정 문서를 다시 확인한다. 훈련 데이터의 Next.js
   지식을 그대로 적용하지 않는다.
4. `data/*.json`, `.env.local`, 운영 로그의 실제 금액·수량·평단·API 키를 Git에
   추가하거나 문서/커밋 메시지에 복사하지 않는다.
5. 포트 3000 운영 서버와 포트 3200의 정체 불명 dev 서버가 같은 `data/`를 공유한다.
   테스트 서버가 필요하면 별도 포트와 테스트 데이터/워크트리를 쓰고, 운영 `data/`를
   seed로 덮지 않는다.

#### B-0A-1. P0 — 자동 갱신 실패를 진단 가능하고 유한하게 만들기

**현재 증거**: 최신 코드가 기동된 뒤 2026-09-16 14:00(KST) 예약 갱신이 14:05에
`fetch failed`로 끝났다. 그 시각 `server.log`에는 같은 시각의 명시적 종료가 없고,
재검토 시 `/`, `/accounts/import`, `/status`는 모두 200이었다. 따라서 현재 메시지
`서버에 연결하지 못했습니다`는 원인을 과도하게 단정한다.

**수정 범위**

- `lib/providers/twelve-data.ts`
- `lib/providers/naver-kr.ts`
- `scripts/refresh-quotes.mjs`
- 필요하면 `app/api/cron/refresh/route.ts`와 해당 테스트

**할 일**

1. Twelve Data 종목, Twelve Data 환율, 네이버 요청 각각에 명시적 timeout을 둔다.
   `AbortSignal.timeout()` 또는 이 Next/Node 버전에서 공식 지원되는 동등한 방식을 쓴다.
   61초 rate-limit 대기는 요청 timeout과 별개다. 대기 자체를 짧게 줄여 429를 다시
   만들지 않는다.
2. cron 전체에 단계 로그를 남긴다: 종목 공급자 시작/완료, 환율 시작/완료, 파일 세대
   쓰기 시작/완료. 로그에는 건수·소요시간·오류 종류만 쓰고 평가금액은 쓰지 않는다.
3. `refresh-quotes.mjs`의 fetch 오류를 모두 `서버가 죽음`으로 표현하지 않는다.
   오류 이름·원인(`error.cause` 중 비밀이 아닌 코드)·경과시간을 기록해 연결 거부,
   연결 중단, timeout을 구분한다.
4. 복구 판단에서 Task Scheduler의 `Running`만 믿지 않는다. 짧은 timeout의 실제
   HTTP 헬스체크를 추가한다. `Running + HTTP 실패`는 정상으로 기록하면 안 된다.
5. 자동으로 `Stop-ScheduledTask`/`taskkill`까지 하는 것은 프로세스 오인 종료 위험이
   있다. 정확한 PID·포트 검증과 사용자 승인 없이 확대하지 않는다. 이번 단계의 최소
   목표는 잘못된 정상 판정을 없애고 정확히 진단·보고하는 것이다.
6. 실제 시세 API를 부르지 않는 mock 테스트로 다음을 고정한다.
   - 공급자 응답이 끝없이 지연되면 timeout으로 종료
   - 한 묶음 timeout 뒤에도 나머지 묶음 처리 원칙이 유지
   - cron 호출 연결이 중간 종료되면 원인을 `서버 미기동`으로 단정하지 않음
   - Scheduler는 `Running`이지만 HTTP 헬스체크 실패인 경우 경고/실패 처리

**완료 조건**

- 자동 갱신이 외부 응답 하나 때문에 무한정 또는 호스트 한도까지 매달리지 않는다.
- 실패 로그만으로 어느 단계에서 어떤 종류로 실패했는지 구분된다.
- 다음 02/08/14/20시 **예약 실행**이 성공하는지 관찰한다. 사용자 승인 없이 수동
  시세 갱신을 호출하지 않는다. 예약 실행을 기다리지 못했다면 `코드/테스트 완료,
  운영 예약 실행 미검증`으로 보고한다.

#### B-0A-2. P0 — import 미리보기 스냅샷과 적용 동시성 완성

**문제 1**: 세 미리보기는 관련 파일을 읽어 결과를 계산한 뒤 baseline 해시를 찍는다.
다른 프로세스가 읽기와 해시 사이에 파일을 바꾸면 오래된 결과에 새 baseline이 붙을 수
있다. 적용 단계 잠금만으로는 이 창을 막지 못한다.

**문제 2**: `position-basis.json`은 position-basis 적용의 실제 target인데 baseline
목록에 없다. 같은 기준에서 만든 미리보기 두 개가 모두 적용될 수 있다.

**수정 범위**

- `lib/import/account-history-actions.ts`
- `lib/import/position-basis-actions.ts`
- `lib/import/normalize-ledger-actions.ts`
- `lib/import/data-version.ts`, `lib/data/atomic-write.ts`는 공통화가 실제로 단순해질 때만
- 세 action 테스트

**할 일**

1. 업로드 파일 자체의 decode/parse는 잠금 밖에서 해도 된다. 하지만 `data/` 입력을
   읽는 순간부터 결과 계산·검증·baseline 생성까지는 하나의 일관된 스냅샷이어야 한다.
2. 방법은 둘 중 더 단순하고 테스트 가능한 쪽을 택한다.
   - 같은 `withDataLock` 안에서 입력 읽기→계산→baseline 생성
   - 읽기 전/후 해시를 비교하고 다르면 미리보기를 폐기해 재시도 요구
3. 긴 CSV parsing이나 외부 네트워크 호출을 잠금 안에 넣지 않는다.
4. position-basis의 baseline에 `position-basis.json`을 포함한다. 파일이 없던 상태도
   `__missing__`으로 비교되어야 한다.
5. 다음 테스트를 추가한다.
   - 입력을 읽은 직후 외부 writer가 파일을 변경하면 오래된 preview가 적용되지 않음
   - account-history에서 cron이 추가한 snapshot이 보존됨
   - `preview A → preview B → apply A 성공 → apply B 충돌 거부`
   - 충돌 시 백업과 target 쓰기가 아예 실행되지 않음

**완료 조건**: 미리보기에서 본 결과와 실제 적용 대상이 같은 파일 세대라는 보장이
테스트로 고정되고, 어느 충돌 경로에서도 최신 운영 파일을 덮어쓰지 않는다.

#### B-0A-3. P0 — cron의 읽기·계산·세대 쓰기를 같은 스냅샷으로 묶기

현재 `app/api/cron/refresh/route.ts`는 외부 시세 조회 후 `loadPortfolio()`와
`getFxQuote()`를 잠금 전에 읽고, 잠금 안에서 다시 `getSnapshots()`를 호출한다.
`loadPortfolio()`가 같은 요청의 React cache를 이미 채웠기 때문에 잠금 안 호출도
잠금 전 값을 재사용할 수 있다.

**할 일**

1. 느린 외부 종목/환율 조회는 잠금 밖에서 완료한다. 네트워크를 기다리는 동안
   `data/.write.lock`을 잡고 있지 않는다.
2. 쓰기에 필요한 운영 데이터 읽기, 총액/원금 계산, snapshots 병합, originals 캡처,
   세 파일 쓰기는 같은 잠금 안에서 fresh snapshot으로 수행한다.
3. 요청 단위 React cache가 잠금 전 값을 재사용하지 않도록 명시적인 uncached snapshot
   reader를 만들거나, 이 경로에서만 직접 읽는 작은 서버 전용 helper를 둔다. 같은
   데이터 계산 규칙을 복제하지 말고 `buildPortfolio` 등 기존 domain 함수를 재사용한다.
4. 외부 시세 조회 동안 symbols 또는 관련 데이터가 바뀌었다면 무조건 조용히 섞지 말고
   변경을 감지해 해당 실행을 실패시키거나 새 snapshot으로 안전하게 재조립한다.
5. 잠금 직전 외부 writer 변경을 주입한 회귀 테스트에서 새 snapshot이 보존되고 계산도
   그 세대 기준인지 확인한다.

**완료 조건**: cron이 남긴 `quotes.json`, `fx-quote.json`, `snapshots.json`과 snapshot
총액이 모두 같은 데이터 세대에서 계산됐다는 테스트가 있다.

#### B-0A-4. P1 — generation rollback의 보장과 오류 문구를 일치시키기

**수정 범위**

- `lib/data/generation-write.ts`
- `lib/data/__tests__/generation-write.test.ts`
- `lib/import/normalize-ledger-actions.ts`와 retry 결과 처리

**할 일**

1. `readOriginals()`는 `ENOENT`만 `null`로 바꾸고 권한·I/O·인코딩 등 다른 읽기 오류는
   그대로 던진다. 원본을 못 읽었는데 `원래 없었다`고 기록하면 롤백 때 기존 파일을
   삭제할 수 있다.
2. 롤백 오류를 삼키지 않는다. 원래 쓰기 오류와 롤백 오류를 모두 보존하는 typed error
   또는 `AggregateError`를 사용한다.
3. **롤백이 실제로 성공한 경우에만** `데이터는 바뀌지 않았습니다`와 retryToken을
   반환한다. 롤백 실패/상태 불명은 자동 재시도를 막고 최신 자동 백업에서 확인·복원해야
   한다는 오류를 보여준다.
4. 프로세스 강제 종료나 전원 종료까지 원자성을 보장하지 못한다면 주석·인계서에서
   `트랜잭션 보장`이라고 단정하지 않는다. crash-safe 보장이 반드시 필요하다고 판단되면
   write-ahead journal 또는 generation+manifest 포인터를 별도 설계하고, 복구 경로
   테스트 없이 도입하지 않는다.
5. 두 번째 쓰기 실패 + 첫 번째 롤백 쓰기 실패를 함께 주입하는 테스트를 추가한다.

**완료 조건**: 사용자에게 보이는 `데이터 보존`/`재시도 가능` 문구가 테스트로 확인된
사실과 정확히 일치한다. 상태를 모르면 성공·보존으로 단정하지 않는다.

#### B-0A-5. P2 — import UI 상태와 업로드 전체 한도 정리

1. `components/import/*-import.tsx` 세 파일에서 apply 시작 시 이전 성공/오류 상태를
   정리하고, retryToken 재시도 성공 뒤에는 오류가 남지 않게 한다.
2. 가능하면 컴포넌트 테스트를 추가한다. 현재 UI 테스트 기반을 새로 크게 들여와야 한다면
   action 결과→상태 전이 helper를 순수 함수로 분리해 테스트하는 쪽을 우선한다.
3. `lib/import/limits.ts`에 파일 개수와 합산 크기 상한을 추가한다. 애플리케이션 상한은
   `experimental.serverActions.bodySizeLimit: "8mb"`보다 multipart 여유를 두고 작아야
   한다. 예: 파일당 2MB를 유지한다면 3개/합계 6MB처럼 명확한 정책을 택한다.
4. 단일 파일, 파일 수 경계/초과, 합산 크기 경계/초과, multipart 이전에 앱 검증이
   가능한 범위를 테스트한다. 화면 설명에도 제한을 표시한다.

**완료 조건**: 실패→재시도 성공 화면에 성공만 남고, 허용되지 않는 다중 업로드는
프레임워크의 일반 오류가 아니라 앱의 한국어 오류로 거부된다.

#### B-0A-6. 보안 선택 — ✅ 2026-09-17 사용자 결정 변경

현재 `next start`는 실제로 `0.0.0.0:3000`에서 수신하고 `/accounts/import`의 변경
Server Action에는 인증이 없다.

- 이 PC에서만 쓸 것이 확실하면 `next start --hostname 127.0.0.1`로 제한하는 안을
  사용자에게 제시한다.
- 같은 네트워크의 다른 기기에서도 봐야 한다면 바인딩을 유지하고 인증을 먼저 설계한다.
- **사용자의 접근 범위 선택을 받기 전에는 바인딩·방화벽·인증을 임의 변경하지 않는다.**

**이전 결정 (2026-09-16)**: 휴대폰 등 같은 네트워크의 다른 기기에서도 쓰고
싶다고 했고, Claude가 "그러려면 인증을 먼저 붙이는 게 안전하다"고 설명한 뒤
Basic Auth/비밀번호+쿠키 중 선택지를 제시했다. 사용자는 **"그냥 다 오픈된
페이지여도 상관없다 — 나만 아는거라서"**라며 인증 없이 현재 상태
(`0.0.0.0:3000` 바인딩 유지)를 그대로 두기로 명시적으로 선택했다. **코드는
바꾸지 않았다** — 바인딩도 그대로, 인증도 추가 안 함. 이후 세션이 이 항목을
다시 꺼내 임의로 바인딩을 좁히거나 인증을 강제로 붙이지 말 것 — 사용자가
스스로 요청할 때만 다시 다룰 것.

**최신 결정 (2026-09-17, 이전 결정을 대체함)**: 실제 서버 등록을 준비하면서 사용자가
로그인 인증을 사용하고 로그인 상태를 약 1주일 유지하도록 명시적으로 요청했다. 이에
단일 사용자 비밀번호+scrypt 해시, 7일 고정 HttpOnly 쿠키, 로그아웃, 로그인 실패 제한,
Proxy 보호와 Server Action 직접 검증을 구현했다. 예약 작업은 별도 `CRON_SECRET`으로
보호한다. 계정 값 생성과 운영 빌드 반영은 `npm run auth:setup` 실행 후 진행한다.

#### B-0A-7. 전체 검증과 보고

작업 후 아래를 각각 독립적으로 실행해 종료코드를 확인한다. 빌드와 `tsc`를 병렬 실행하면
`.next/types`가 교체되는 순간과 겹쳐 가짜 타입 오류가 날 수 있으므로 순차 실행한다.

```powershell
npx tsc --noEmit
npx eslint .
npm test
npm run build
```

추가 확인:

```powershell
git status --short
git diff --check
git diff -- data
```

- `/`, `/accounts/import`, `/status`의 HTTP 200을 확인한다.
- 실제 금융 데이터 파일이 Git 변경에 잡히지 않았는지 확인한다.
- `HANDOFF.md` J절과 5절에 실제 검증 결과를 갱신한다.
- 예약 실행을 관찰하지 못했으면 자동 갱신을 `완료`로 보고하지 않는다.
- 운영 서버 재시작이 필요하면 WORK_ORDER 0-4의 포트/PID 검증 절차를 지키고, 진행 중인
  시세 갱신이 없는지 먼저 확인한다.

---

### B-0. 서버 안정성 관찰 — 🔴 예약 갱신 실패 지속, 원인 구분 필요

**사용자 지시 (2026-09-15)**: *"P2-2는 나중에, 다음 며칠 서버 안정성부터 지켜보자."*
→ **사용자가 먼저 꺼내기 전에 배포(B-2) 얘기를 먼저 제안하지 말 것.**

**2026-09-16 2차 재검토 최신 상태**: 최신 코드 기동 뒤 14:00(KST) 예약 갱신이
14:05에 `fetch failed`로 끝났다. 같은 시각 `server.log`에는 명시적인 서버 종료가
없었고 재검토 시 세 주요 페이지는 200이었다. 따라서 아래 과거 설명처럼
`서버에 연결하지 못했습니다 = 서버 사망`으로 단정하면 안 된다. B-0A-1의 timeout,
단계별 로그, 실제 HTTP 헬스체크를 먼저 구현해 원인을 구분한다.

**경과**: 약 36시간 안에 서버가 **최소 2번** 원인 불명으로 죽었다.

1. 2026-09-15 저녁, `0xC000013A`(Ctrl+C 종료와 같은 코드)로 1차 사망
2. 2026-09-16 새벽 02:03(성공한 시세 갱신 직후)~03:46(다음 세션이 다운 발견) 사이 2차 사망

둘 다 Task Scheduler 이벤트 로그가 이 환경에서 비활성이라(`wevtutil qe
"Microsoft-Windows-TaskScheduler/Operational"` → 결과 없음) 진짜 원인은 여전히 못
찾았다. "며칠 지켜보자"고 했지만 하루 반 만에 2번이라 패턴이 뚜렷해서, 2026-09-16에
가장 가벼운 대응(재시작 루프)을 사용자 승인받고 적용했다.

**적용한 것**: `scripts/start-server.cmd`에 무한 재시작 루프 추가
(`:loop` → `call npm start` → 10초 대기 → `goto loop`). 죽어도 10초 뒤 자동으로
다시 뜬다. 재시작 시각은 `C:\Users\ACC-002\cgportfolio-logs\server.log`에 남는다.
**근본 원인은 여전히 모른다 — 증상만 사라진다.**

**⚠ 적용 중 실제로 겪은 함정 — 이 cmd 파일을 다시 고칠 때 반드시 알아둘 것**:
처음에 재시작 로그 메시지를 한글로 썼더니(`echo ... 서버 종료됨 — ...` 같은 식으로,
특히 em dash "—" 포함) `cmd.exe`가 파일을 잘못 해석해 `ERROR: Input redirection is
not supported, exiting the process immediately.`로 **스크립트 자체가 죽어버렸다.**
`REM` 주석의 짧은 한글은 문제없이 넘어가지만(기존 2줄짜리 버전이 하루 넘게 잘
돌았던 이유), `ECHO`로 파일에 쓰는 한글 텍스트, 특히 대시 같은 특수 문장부호는
`cmd.exe`의 기본 코드페이지와 부딪혀 치명적 오류를 낼 수 있다. **`.cmd` 파일의
`ECHO`/로그 출력은 영문 ASCII만 쓸 것.** 고치고 나면 Task Scheduler를 거치지 않고
`cmd.exe /c "경로\start-server.cmd"`로 먼저 직접 실행해 몇 초 안에 죽지 않는지
확인한 뒤에 배포할 것 — 이번에도 그렇게 해서 잡았다.

**참고 — Task Scheduler가 "이미 실행 중"이라며 거부할 때**: 강제 종료
(`taskkill`)로 자식 프로세스만 죽이면 Task Scheduler 쪽 상태가 "Running"으로
유령처럼 남아 새 실행을 계속 거부하는 걸 겪었다. `Stop-ScheduledTask -TaskName
"..."`로 먼저 정리한 뒤 `Start-ScheduledTask`를 부를 것.

**할 일 — 계속 관찰**

1. 로그를 본다 (`server.log`는 시스템 로컬 시각(KST)을, `refresh.log`는 진짜 UTC를
   쓴다 — 서로 섞어 읽지 말 것. `/status` 페이지는 이 둘을 자동으로 KST로 맞춰
   보여준다):

```bash
cat "C:\Users\ACC-002\cgportfolio-logs\refresh.log"
cat "C:\Users\ACC-002\cgportfolio-logs\server.log"
```

2. 판정 기준:
   - `refresh.log`에 `성공: 갱신 30건, 누락 0건, 오류 0건` → 정상
   - `실패: 서버에 연결하지 못했습니다` → 호출 연결이 실패/중단된 것. 서버 사망,
     긴 요청 중 연결 종료, 호스트 timeout 등을 추가 로그와 헬스체크로 구분해야 함
   - `server.log`에 "server exited, restarting" 줄이 잦으면 재시작 루프가 자주
     도는 것 — 여전히 불안정하다는 뜻이니 다음 단계(아래) 대응을 고려할 것
   - `누락`이 0이 아님 → 시세 API 쪽 문제 (서버 문제 아님)
3. 6시간마다 한 줄씩 쌓여야 한다. 하루면 4줄. 줄 자체가 없으면 스케줄러 작업이 안 돈 것:

```powershell
Get-ScheduledTaskInfo -TaskName "CGPORTFOLIO 시세 갱신" | Select-Object LastRunTime, LastTaskResult, NextRunTime
```

4. 스냅샷이 실제로 쌓이는지 확인 (이게 이 기능의 존재 이유다):

```bash
node -e "const fs=require('fs'); const s=JSON.parse(fs.readFileSync('C:\\Users\\ACC-002\\Desktop\\cgportfolio\\data\\snapshots.json','utf8')); console.log(s.length, s[s.length-1])"
```

**재시작 루프로도 자주 죽으면 다음 단계 (난이도 순)**

- (중간) "CGPORTFOLIO 시세 갱신" 작업에 **서버 생존 확인 + 없으면 기동** 로직을 넣는다.
  `refresh-quotes.mjs`가 연결 실패를 감지하면 `Start-ScheduledTask`를 호출하게 하는 식.
- (제대로) `pm2`나 `nssm`으로 Windows 서비스로 등록한다. 의존성이 늘어나므로
  **사용자 승인을 받고** 할 것 (규칙 A-3).

**완료 조건**: 며칠치 `server.log`에 재시작이 드물고(하루 0~1회 이하), 예약 갱신이
연속 성공하며, 실패 시 어느 단계에서 어떤 원인으로 끝났는지 로그로 구분된다. 잦으면
위 대응 중 하나를 적용한다.

**2026-09-16 오전 업데이트 — 원인의 절반을 특정함**: `server.log`가 04:12부터
08:17까지(시스템 로컬 시각, 즉 KST) 약 9회 재시작을 기록했다. 이 중 하나는 이제
원인을 알 수 있다.

**2026-09-16 후속 정정**: 이전 인계서들이 "이 환경의 로컬 시각 = UTC"라고 적었던
건 틀렸다. `Get-TimeZone`으로 직접 확인한 결과 이 PC의 로컬 타임존은 실제로
Asia/Seoul(KST, UTC+9, 서머타임 없음)이다. `/status` 페이지가 `server.log`의
로컬 시각을 UTC로 잘못 라벨링해서 화면에 9시간이 이중으로 더해지던 버그를
고쳤다(`lib/domain/log-status.ts`) — 위 절의 로그 시각들도 실제로는 KST다.

- **Windows Update 자동 재부팅이 최소 1건의 "원인 불명" 사망을 설명한다.**
  `wevtutil`/`Get-WinEvent`로 Task Scheduler·Application 로그를 봐도 여전히
  아무것도 안 남지만, **System 로그의 Kernel-Power/EventLog 이벤트(6006→109→6005)를
  보면 2026-09-16 02:45:44~02:47:00 사이에 `TrustedInstaller.exe`가 SYSTEM 계정으로
  "운영 체제: 업그레이드(계획됨)"(Reason Code `0x80020003`) 재부팅을 두 차례
  연달아 실행했다.** `Get-CimInstance Win32_OperatingSystem`의 `LastBootUpTime`도
  `02:46:50`으로 일치한다. 지난 인계서의 "2026-09-16 새벽 02:03~03:46 사이 2차
  사망" 창과 겹친다 — Task Scheduler 자체 로그가 아니라 System 로그를 봐야 잡힌다.
  같은 패턴(TrustedInstaller "OS: Upgrade" 재부팅)이 08-24, 08-25(2회), 08-28,
  09-09(2회)에도 있었다 — **일회성이 아니라 이 PC에서 주기적으로 반복되는
  현상**이다. 이건 이 대시보드만의 문제가 아니라 이 PC를 상시 서버로 쓰는 한
  계속 겪을 수밖에 없는 제약이다. Windows Update의 자동 재부팅 설정(활성 시간대
  등)을 바꾸는 건 시스템 설정 변경이라 **사용자가 직접 하거나 명시적으로 요청할
  때만** 다룰 것.
- **04:12~05:46 구간의 짧은 반복 재시작(특히 05:45~05:46에 8회, 매번 1초
  이내 생존)은 여전히 원인 불명이다.** 그 시간대 System·Application 로그 모두
  아무 이벤트가 없다 (재부팅도 아니고 앱 크래시 로그도 없음). 재부팅 직후 첫
  기동 구간에 몰려 있는 걸 보면 부팅 직후 무언가(포트 해제 지연 등)와 관련이
  있을 가능성이 있으나 확인하지 못했다.

**2026-09-16 오전 두 번째 업데이트 — 같은 종류의 사고를 실제로 재현하고 원인을
확정함(위 "포트 해제 지연" 추측을 뒷받침)**: P0-2(원자적 쓰기) 검증을 위해
`Stop-ScheduledTask "CGPORTFOLIO 서버"` → `Start-ScheduledTask`로 서버를
재시작했는데, 마침 그 직전에 오래 걸리는 `/api/cron/refresh` 요청(레이트리밋
때문에 ~3분 소요)을 그 서버에 보내 둔 상태였다. **`Stop-ScheduledTask`가 그
요청을 처리 중이던 `node.exe`(pid 256)를 실제로는 죽이지 못했다** — Task
Scheduler는 작업을 "Ready"로 표시했지만 프로세스는 포트 3000을 계속 물고
있었다. 그 뒤 `Start-ScheduledTask`가 새로 띄운 `next start`는 매번
`EADDRINUSE: address already in use :::3000`로 즉시 죽었고, 재시작 루프가
10초마다 그걸 반복해 08:57~09:01 사이 다시 짧은 반복 재시작 구간이 생겼다.
`taskkill /F /PID 256`으로 좀비 프로세스를 강제 종료하자 바로 정상 기동됐다.

**결론**: "부팅 직후 포트 해제 지연"이라는 추측이 실제 메커니즘으로
확인됐다 — 다만 원인은 "부팅"이 아니라 **"이전 서버 프로세스가 느린 요청을
처리 중이어서 종료 신호에 곧바로 반응하지 못하는 것"**이다. Windows Update
재부팅 직후 첫 기동(04:12)도 마찬가지로 무언가(초기화 지연이든, 이전
프로세스의 뒤늦은 정리든) 때문에 포트가 바로 안 풀렸을 가능성이 높다.
**교훈**: `Stop-ScheduledTask` 직후 바로 `Start-ScheduledTask`를 부르지 말고,
`netstat -ano | grep ":3000.*LISTENING"`으로 포트가 실제로 비었는지 확인한
뒤 시작할 것. 안 비어 있으면 남은 PID를 `taskkill //F //PID <PID>`로
정리하고 나서 시작한다. 특히 **시세 갱신 요청이 진행 중일 때는 서버를
재시작하지 말 것** — 최대 3분 넘게 걸리는 요청이 끝나기 전에 종료 신호를
보내면 이 사고가 재현된다.
- **적용한 조치**: `scripts/refresh-quotes.mjs`에 연결 실패 시 "CGPORTFOLIO 서버"
  작업 스케줄러 상태를 확인해 `Running`이 아니면 `Start-ScheduledTask`로 직접
  기동을 시도하는 로직(`tryRecoverServerTask`)을 추가했다. OS 재부팅으로
  재시작 루프의 `cmd.exe`까지 같이 죽는 경우, 기존에는 다음날 새벽 4시 DAILY
  트리거까지 방치됐는데 이제 최대 6시간(다음 시세 갱신 주기) 안에는 복구된다.
  이미 `Running`이면(루프가 정상 동작 중) 건드리지 않아 스케줄러의 "이미 실행
  중" 충돌을 피한다. `node --check`로 문법 확인, 가짜 URL로 연결 실패를
  일으켜 실제 서버 작업 상태를 정확히 감지하는 것까지 확인했다 (`Running` →
  건드리지 않음). `Stopped`/`Ready` 상태에서 실제로 기동시키는 경로는 운영 중인
  서버를 일부러 죽여야 재현되므로 이번엔 검증하지 못했다.

---

### B-1. 실거래 이력·현재 잔고·평가손익 연결 — ✅ 2026-09-16 완료

과거 거래원장과 현재 잔고의 역할을 분리했다. 매매·입출금·배당 이력은 거래원장을
사용하지만, 제한된 조회 기간만으로는 현재 수량과 원가가 완전히 재현되지 않으므로
증권사 잔고 CSV에서 만든 `position-basis.json`을 현재 기준값으로 적용한다.

**한 것**
- `transactions.json`의 일반 체결·주식 이체·액면분할을 `action`으로 구분했다
- 이체와 분할은 예수금 및 실현손익에 영향을 주지 않게 했다
- `cashflows.json`의 외부 입출금·환전·수익·보정을 `kind`로 구분하고 외부 입출금만
  원금에 포함했다
- `scripts/import-position-basis-csv.mjs`로 현재 보유수량·평균단가·매입원가·예상
  매도수수료율을 가져온다
- 현재 평가손익은 `최신 평가금액 - 증권사 매입원가 - 예상 매도수수료`로 계산한다
- `scripts/import-account-history-csv.mjs`로 계좌수익률 CSV의 일별 평가금액과 명시적인
  `입금-출금` 누적 현금원금을 차트에 병합했다. `예탁자산-누적손익` 역산은 비현금
  자산이동을 원금으로 오인하므로 사용하지 않는다
- 차트 기본 기간을 1년으로 늘렸다. 사용자 최신 요청에 따라 `매매` 토글과 초록 매수·
  빨강 매도 점선을 다시 추가했다. 이체·액면분할은 제외하고 같은 스냅샷의 체결은 묶는다

**검증**
- 잔고 CSV의 평가손익 합계와 수수료 포함 재계산 결과가 1원 단위까지 일치
- 현재 기준값의 계좌·종목 참조 누락 및 중복 없음
- 원장 정규화 전후 예수금 차이는 통화 최소 표시 단위 미만
- 일별 계좌 데이터가 2025년부터 현재까지 이어지고 각 과거 점에 당시 원금이 있음
- 타입 검사, ESLint, 57개 단위 테스트, 프로덕션 빌드 통과

**주의**
- 과거 거래원장이 계좌 개설 시점부터의 전체 이력은 아니므로 누적 실현손익은 참고값이다
- 주 지표는 현재 평가손익이며, 최신 시세가 바뀌면 잔고 CSV 시점의 평가손익과 달라질 수 있다
- `position-basis.json`과 `snapshots.json`은 민감 데이터라 반드시 gitignore 상태를 유지한다

---

### B-2. 배포 — 🔄 사용자가 재개 요청, 서버 선택·등록 대기

2026-09-15에는 보류했으나, 2026-09-17 사용자가 실제 서버 등록을 다시 요청했다.
B-3 인증 코드는 준비됐고 계정 설정·빌드 반영 뒤 서버 제공자와 도메인을 정한다.

**핵심 제약**: 지금 구조는 `/api/cron/refresh`가 `data/*.json`에 **파일을 쓴다.**
Vercel 같은 읽기전용 파일시스템에서는 동작하지 않는다.

| 안 | 내용 | 비용 | 작업량 |
|---|---|---|---|
| A | VPS에 그대로 올린다 | 월 $5 내외 | 적음 (지금 코드 그대로) |
| B | 저장소를 DB(Supabase 등)로 바꾸고 Vercel | 무료 가능 | 중간 |
| C | 로컬에서만 쓴다 (현재 상태) | 0원 | 없음 |

B로 간다면 고칠 곳은 **두 군데뿐**이다. 그러라고 저장소를 한 겹 분리해 뒀다:
- `lib/data/store.ts` — 읽기 (`readJsonCached`를 쿼리로 교체)
- `app/api/cron/refresh/route.ts` — 쓰기 (`writeFile`을 upsert로 교체)

**어느 안이든 배포 전에 B-3(인증)을 먼저 붙일 것.**

---

### B-3. 인증 — ✅ 코드 완료, 계정 설정·운영 반영 대기 (2026-09-17)

**외부에서 접근 가능한 곳에 올린다면 배포 전에 반드시 붙인다.** 개인 자산 정보가
그대로 보이는 화면이다. 혼자 로컬에서만 쓴다면 불필요하다.

**참고 — 2026-09-15에 있었던 일**: 사용자가 "홈페이지 공유해줘"라고 해서 `localtunnel`로
임시 외부 링크(`*.loca.lt`)를 만든 적이 있다. 인증이 없어서 링크를 아는 사람은 누구나
실제 자산을 볼 수 있는 상태였고, 사용자 요청으로 곧 끊었다. **다시 이런 요청이 오면
인증을 먼저 붙이자고 제안할 것.**

**구현 결과**:
- `proxy.ts`가 로그인·인증 API·크론 API·정적 파일을 제외한 화면/API를 보호한다.
- 비밀번호 원문 대신 scrypt 해시를 사용하고, 서명된 HttpOnly/SameSite=Lax 쿠키를
  발급 시점부터 7일간 유지한다. 외부 호스트 쿠키는 Secure로만 발급한다.
- 세 가지 가져오기 Server Action도 세션을 직접 확인해 Proxy 설정 실수 시 우회를 막는다.
- 예약 시세/주간 리포트는 사용자 쿠키와 분리된 `CRON_SECRET`을 사용하며 production에서
  비밀키가 빠지면 닫힌다. 예약 스크립트는 `.env.local`을 직접 읽는다.
- `npm run auth:setup`이 아이디·비밀번호를 숨김 입력받아 `.env.local`에 해시와 무작위
  비밀키를 기록한다. 비밀번호를 채팅·문서·git에 쓰지 않는다.

**남은 활성화 순서**: 로컬 터미널에서 `npm run auth:setup` → `npm run build` → 서버
작업 재시작 → 브라우저 로그인/로그아웃·7일 쿠키·예약 작업 1회 확인. HTTPS가 준비되기
전에는 인터넷에 포트를 공개하지 않는다.

---

### B-4. 과거 종가 확보 (백테스트 구간 확장) — ✅ 2026-09-16 보유 US 종목 완료

레버리지 상품의 복리 잠식이나 깊은 하락장의 영향을 보려면 최소 10년, 가능하면
2000년대 초반 닷컴 붕괴와 2008년을 포함하는 구간이 필요하다.
QLD 상장일(2006-06)부터 받으면 2008년 금융위기 구간이 들어와서 의미가 커진다.

**한 것**: `scripts/fetch-price-history.mjs`(신규, `scripts/lib/price-history.mjs`의
순수 함수 사용)를 만들어 Twelve Data `time_series`(무료 플랜 `outputsize` 최대
5000일 — 실제로 확인함, QLD 기준 2006-10-27까지 커버해 2008년 금융위기 포함)로
보유 **US 종목 62개 전부**의 실제 일별 종가를 받아 `data/prices.json`을 대체했다.
분당 8심볼 제한(`lib/providers/twelve-data.ts`와 같은 속도)을 지켜 62/62종목
성공(실패 0건), 실행 시간 약 9분. 적용 전 자동 백업 + `prices.json.bak` 둘 다
남음. 상장일이 늦은 종목은 그만큼 짧게(예: 2024~2025년 상장 종목은 수백 일)
자연스럽게 받아진다 — 억지로 더 채우지 않는다.

**검증**: `scripts/lib/__tests__/price-history.test.mjs` 8건(순서 뒤집기,
오류 응답·숫자 아님·날짜 형식 이상 거부, 실패 종목은 기존 값 보존 등). 실행 후
`data/out-prices.json`에서 62개 전부 날짜 오름차순·숫자 유효성 확인, 각 종목
최신 종가가 그 시점 실시간 시세(`quotes.json`)와 5% 이내로 일치하는지 대조
(전부 통과). 브라우저로 실제 대시보드 확인: 보유 종목 29개 중 28개(현금
예수금 `CASH.USD` 제외 전부)에 스파크라인이 뜸(이전엔 QLD·SCHD 2개뿐이었다).
종목상세 페이지(`/symbols/NVDA`)도 "가격 추이 5000거래일"로 실제 데이터 표시
확인. `npm test`(코드 변경 없음, 새 스크립트 테스트만 추가)·`tsc`·`eslint` 통과.

**범위 밖으로 남긴 것**: 보유 국내 종목 `491620` 1개는 Twelve Data가 국내
상장 ETF를 안 다루고 네이버 쪽엔 과거 일별 종가 API가 없어서 그대로 뒀다 —
옛 `scripts/seed.mjs`의 가상 데이터가 남아 있고, 화면은 가격 이력 없는 종목과
동일하게(안내 문구) 처리한다. 필요해지면 증권사 Open API 같은 별도 경로가
필요하다.

`prices.json` 구조는 `{ "심볼": [{ "d": "YYYY-MM-DD", "c": 종가 }, ...] }`이며
변경 없다. 재실행하려면 `node scripts/fetch-price-history.mjs [--replace]
[--symbols=A,B,C]` (기본은 `data/symbols.json`의 US 심볼 전부, 검토용
`out-prices.json`에 먼저 쓴다).

---

### B-4a. 과거 환율(fx.json) 실데이터 교체 — ✅ 2026-09-16 완료

사용자가 대시보드 차트의 환율선을 보고 "usd 환율이 최근에 1550원까지도
간적이 있는데"라고 지적. 처음엔 참조 사이트(서울외국환중개
`www.smbs.biz/ExRate/StdExRate.jsp`)의 기본 1개월 화면만 보고 "1550원 간
기록 없다"고 잘못 답함 — 사용자가 "실제 차트에서 보여주는 기간과
일치시켜야지"라고 지적해서 같은 1년 구간(2025-09-16~2026-09-16)으로
다시 확인하니 실제로 2026-07-02에 1,554.40원까지 간 기록이 있었다.
`data/fx.json`을 뜯어보니 B-4의 `prices.json`과 같은 문제 —
`scripts/seed.mjs`가 만든 가짜 랜덤워크(2024-01-02~2026-09-15, 매끈하게
1317.93~1352.60 사이)였고, CSV로 만든 과거 스냅샷들의 `fxRate`도 여기서
날짜별로 가져와서 같이 틀려 있었다.

**한 것**: `scripts/fetch-fx-history.mjs`(신규, B-4의
`scripts/lib/price-history.mjs` 파서 재사용)로 Twelve Data `time_series`에서
`USD/KRW` 실제 일별 종가 5000일(2007-11-16~2026-09-16)을 받아
`data/fx.json`을 대체(구조는 그대로 `{ "d": "YYYY-MM-DD", "rate": 환율 }[]`).
이어서 `scripts/fix-snapshot-fx.mjs`(신규, `scripts/lib/fix-snapshot-fx.mjs`의
`fixSnapshotFxRates()`가 기존 `fxRateAt()` 조회 재사용)로 `data/snapshots.json`
591개 전부의 `fxRate`만 새 `fx.json` 기준으로 다시 계산해 덮어썼다.
`totalKrw`/`principalKrw`는 계좌수익률 CSV의 예탁자산을 그대로 쓴 값이라
원래도 정확했고 이번에도 손대지 않았다.

**검증**: `scripts/lib/__tests__/fix-snapshot-fx.test.mjs` 3건(날짜별 조회,
없는 날짜는 직전 값 사용, 정렬 안 된 입력도 처리). 2026-07-02 스냅샷을
전후 대조 — `fxRate` 1350.04 → 1539.79(Twelve Data 그날 종가, 공식
1,554.40원 고점과 근접), `totalKrw`·`principalKrw`는 변화 없음 확인. 로컬
빈 포트(3204)에서 실제 데이터로 대시보드를 열어 6월 말~7월 초 구간을
호버 — 환율이 1509~1545원대로 실제 변동성을 보여줌(이전엔 1350원대
평탄선). `tsc`·`eslint`·`npm test`(전체 279건)·`next build` 전부 통과.
`git push origin HEAD:main`(6300760)로 반영, 운영 서버 재시작 후
`localhost:3000`에서도 동일하게 확인.

**범위 밖으로 남긴 것**: 없음 — `USD/KRW`는 Twelve Data 무료 플랜이 지원하는
통화쌍이라 B-4의 `491620`(국내 상장 ETF) 같은 예외가 없다.

재실행하려면 `node scripts/fetch-fx-history.mjs [--replace]`로 fx.json을
먼저 교체한 뒤, `node scripts/fix-snapshot-fx.mjs [--replace]`로 스냅샷을
다시 계산한다(둘 다 기본은 검토용 `out-*.json`에 먼저 쓴다).

---

### B-5. ETF 구성종목 자동 수집

`data/lookthrough.json`을 수동 갱신하고 있다. 발행사가 공시하는 보유내역(CSV/API)을
받아 갱신하는 스크립트를 만들면 자산맵이 항상 최신이 된다.

**지금 상태**: `nasdaq100`, `dividend100`, `growth-active`, `ai-value-chain` 4개 표가
있고, 실제 보유 종목 중에서는 QLD(→`nasdaq100`)와 SCHD(→`dividend100`)만 연결돼 있다.
나머지 26개 개별 종목은 ETF가 아니라 자기 자신으로 표시된다 (정상 동작).

---

### B-6. UI 개선 (qld.kr 대비 미반영 항목) — ✅ 2026-09-16 "중간" 항목 완료

사용자가 qld.kr과 비교해 미반영/차이가 있다고 짚은 항목들. 우선순위별 진행 상황:

**완료** (`value-chart.tsx`, `accounts/page.tsx`, `dividends/page.tsx`, `portfolio.ts`)
- 총 평가금액 차트에 실제 현금원금 대비 수익률 선, 고점/MDD 마커, 전체화면 버튼 추가
- 사용자 최신 요청에 따라 `매매` 토글과 매수(초록)·매도(빨강) 점선 타점을 기본 ON으로
  표시. 이체·액면분할 제외, 같은 스냅샷 체결 그룹화
- 자료가 듬성듬성할 때 `1일/7일/1개월` 등이 같은 두 점을 보이던 문제를 고쳐, 실제
  관측값만 기간별 2/7/30개 대체 사용(가짜 보간 없음)
- 상단 종목 스트립을 무한 전광판으로 변경하고 미국식 상승 초록/하락 빨강 적용
- 월간 배당 막대에 연도 선택(`?year=` 쿼리)과 종목별 색상 구분 누적 막대 추가
- 계좌별 평가금액에 비중 차트(계좌 2개 이상일 때만 표시)와 입출금 전/후 잔액 컬럼 추가
  (`buildCashFlowLedger()`, 단위 테스트 4건 포함)

이 작업 과정에서 다른 에이전트(GPT)가 같은 요청에 미커밋 상태로 작업해 둔 게 있었는데
qld.kr 색상 체계(상승=초록/하락=빨강)로 전체를 다시 칠해 뒀다. 지난 세션에 같은
"픽셀 단위 복제" 요청을 두 번 거절한 바 있어(§C "qld.kr 관련" 참고), 사용자 확인을
받고 기능만 골라내 CGPORTFOLIO 원래 색상 토큰으로 다시 입혔다. **다음에도 qld.kr
대비 개선 요청이 오면 같은 기준으로 판단할 것 — 구조/기능은 반영, 색상·정확한 hex/rgba
값 복제는 거절.**

**미반영 — "높음" 우선순위, 별도 항목으로 이미 추적 중**
- 매매·입출금·배당을 화면에서 직접 입력/CSV로 가져오는 기능 → **B-1**과 같은 일이다.
  지금 CSV 가져오기는 스크립트(`scripts/import-holdings-csv.mjs`)뿐이고 화면 입력
  폼은 없다. 새로 만들려면 `app/accounts/`나 새 라우트에 입력 폼 + 서버 액션이 필요하다
- 자동 시세 갱신·스냅샷 축적 "미반영"이라 한 부분은 사실 **이미 동작 중**이다(**B-0**
  참고) — 2026-09-16 새벽에도 무인 갱신 성공. 스냅샷이 4개뿐인 건 시작한 지 하루가
  안 돼서일 뿐, 기능이 빠진 게 아니다. 며칠 지나면 자연히 쌓인다
- 서버 배포 시 영구 저장소 → **B-2**(배포)와 묶인 문제. 사용자가 "나중에"로 보류시켰다

**완료 조건**: "중간" 세 항목 전부 화면에서 동작 확인 (대시보드·계좌는 브라우저로 직접
확인, 배당은 `data/dividends.json`이 비어 있어 타입체크·테스트로만 검증 — 실데이터가
들어오면 한 번 더 눈으로 볼 것).

---

## C. 손대지 말 것

- **`C:\Users\ACC-002\Desktop\web-stock`** — 별개 프로젝트다. 이 작업과 무관하다
- **포트 3200에서 돌고 있는 dev 서버** — 이전 담당자가 띄운 게 아니다. 정체 불명이므로
  죽이거나 건드리지 말 것
- **브랜드명 CGPORTFOLIO** — `lib/config.ts`의 `site`에서만 정의된다. 바꿀 일 없다
- **스택 재논의** — Laravel 대 Next.js는 비용까지 따져 이미 결론 났다
- **`.gitignore`의 개인 데이터 블록**
- **`eslint.config.mjs`/`vitest.config.mts`의 `**/.claude/worktrees/**` 제외 규칙**
- **🚨 qld.kr 디자인 복제** — 아래 참고

### qld.kr 관련 — 넘지 말아야 할 선

이 프로젝트는 qld.kr(기승전QLD)이라는 **실제로 운영 중인 타인의 서비스**를 참고해
만들어졌다. `HANDOFF.md`에 명시된 대로 **코드·CSS·이미지·글은 전혀 가져오지 않았다.**

2026-09-15에 사용자가 *"100% 동일한 레이아웃을 원함"*, *"혼자 사용하는거라 문제없어"*
라고 두 차례 요청했으나 **거절했다.** 개인 사용 목적이라도 타인의 서비스 디자인을
픽셀 단위로 복제하는 것은 저작권 문제이기 때문이다.

대신 **구조적 유사성**(메뉴 항목 순서, 카드 배치 단수)만 맞췄고, 색상·타이포그래피·
컴포넌트 스타일은 CGPORTFOLIO 고유의 것을 유지했다.

**같은 요청이 다시 오면 같은 선을 지킬 것.** 구조 조정은 가능, 디자인 복제는 불가.

---

## D. 보고할 때

작업을 마치면 다음을 적어 보고할 것.

1. 무엇을 했는가
2. **어떻게 검증했는가** — 돌려본 명령과 실제 출력. "통과했다"가 아니라 출력을 보일 것
3. 검증하지 못한 부분은 무엇이고 왜인가
4. 새로 알게 된 제약이나 발견한 버그 (→ `HANDOFF.md` 8절에 추가)
5. `HANDOFF.md` 5절 갱신 여부

---

## 부록: 완료된 작업 (2026-09-15)

상세 검증 내역은 `HANDOFF.md` 5절에 있다. 여기는 목록만.

| 항목 | 결과 |
|---|---|
| **P0-1 시세 공급자 실호출** | ✅ Twelve Data·네이버 둘 다 실제 호출 성공. 응답 형태 가정이 맞았음. 버그 1건 수정(네이버 `marketState` 하드코딩) |
| **P0-2 `/api/cron/refresh` 실행** | ✅ 갱신·부분실패 시 기존값 유지·`CRON_SECRET` 인증 전부 확인. `recomputeTotal()`이 정식 계산 경로와 같은 총액을 냄 |
| **P1-1 단위 테스트** | ✅ Vitest 도입, `lib/domain/__tests__/` 5파일 57테스트 통과 |
| **P1-2 실데이터** | ✅ 매매·입출금·배당 이력과 현재 잔고 기준값 연결 완료. 평가손익 및 일별 차트 검증 → **B-1** |
| **P2-1 자동 갱신 스케줄링** | ✅ 작업 스케줄러 3개 등록. 20:00 무인 실행 성공 확인, 스냅샷 누적 확인 |

**이 과정에서 잡은 주요 버그** (자세한 건 `HANDOFF.md` 8절):
- 네이버 공급자가 `marketState`를 항상 `"closed"`로 하드코딩
- Twelve Data 분당 크레딧 한도로 28종목 일괄 요청이 전부 429 → 8개씩 분할
- 중첩 워크트리 때문에 lint 683건 오류·테스트 2배 중복 실행

### 0-5c. 받은 CSV 자동 가져오기 (inbox)

매매·잔고는 시세와 달리 자동으로 따라오지 않는다(증권사 API를 쓰지 않으므로). 매번
`/accounts/import` 화면에서 올리는 수고를 줄이려고, **폴더에 넣어두면 알아서 가져오는**
경로를 뒀다.

- **넣는 곳**: `~/cgportfolio-inbox` (저장소 밖이다 — 안에 두면 실제 계좌 데이터가 든
  CSV가 미추적 파일로 남고, 저장소가 Public이라 위험하다)
- **보유종목(잔고) CSV는 계좌별 폴더에 넣는다**: `~/cgportfolio-inbox/<계좌ID 또는
  계좌이름>/`. 잔고 가져오기는 **그 계좌의 기준값을 통째로 교체**하므로 계좌를
  잘못 잡으면 남의 계좌 잔고를 덮어쓴다 — 폴더로 계좌를 밝히지 않았거나 모르는
  계좌 이름이면 **반영하지 않고 `실패/`로 뺀다**(짐작하지 않는다). 계좌가 하나뿐이던
  시절엔 계좌를 코드에 고정해 뒀는데(2026-09-21까지), 계좌가 늘면서 위험해져 고쳤다.
  계좌수익률 CSV는 여러 계좌를 날짜별로 합산하는 것이라 계좌 구분이 필요 없어
  루트에 그냥 둬도 된다.
- **주기**: 10분마다 (`CGPORTFOLIO 받은CSV 가져오기`)
- **처리 후**: 성공은 `처리완료/`, 실패·판별불가는 `실패/`로 날짜를 붙여 옮긴다.
  옮기지 않으면 다음 주기에 같은 파일을 또 가져간다.
- **로그**: `~/cgportfolio-logs/inbox-import.log` (파일명·종류·건수만, 금액은 안 남긴다)

**판별은 파일 이름이 아니라 헤더로 한다**(`scripts/lib/inbox-classify.mjs`). 증권사
내보내기 이름은 화면 번호나 무작위 문자열이라 내용과 무관하다 — 실제로 화면 번호로 받은
파일 세 개가 전부 예상과 다른 내용이었다(HANDOFF 13절 이전 기록 참고).

| 헤더 조건 | 돌리는 가져오기 |
|---|---|
| `일자`+`예탁자산`+`입금`+`출금` | 계좌수익률 → `snapshots.json` |
| `종목명`+`평가손익` | 보유종목(잔고) → `position-basis.json` |
| 그 외 | **건드리지 않고** `실패/`로 옮김 |

안전장치: 적용 직전 자동 백업, 원자적 쓰기·잠금(기존 importer 그대로), 다운로드 중인
파일은 크기가 안정될 때까지 건너뜀, CSV가 아니면 거부.

**자동으로 돌리지 않는 것**: `scripts/import-holdings-csv.mjs`(보유종목 → 거래원장
부트스트랩)는 실제 매매 이력이 아닌 근사 거래를 만들어내므로 자동화 대상에서 뺐다.
필요하면 사람이 직접 판단해서 돌린다.
