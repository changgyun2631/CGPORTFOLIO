# 작업명령서 — CGPORTFOLIO

대상: 다음 작업 담당자 · 기준 커밋 `a607595` · 작성 2026-09-15

**시작 전에 `HANDOFF.md`를 먼저 읽을 것.** 특히 5절(검증되지 않은 것)과
8절(이미 잡은 버그)을 읽지 않고 손대면 같은 실수를 반복하게 된다.

---

## A. 작업 규칙

### 코드

1. **주석은 한국어로, "무엇"이 아니라 "왜"를 적는다.** 기존 코드의 주석 밀도와 어조를
   맞출 것. 코드를 읽으면 아는 내용은 주석으로 달지 않는다.
2. **차트 라이브러리를 도입하지 않는다.** 모든 차트는 SVG 직접 구현이다. 이건 취향이
   아니라 결정 사항이다 — 번들 크기와 서버 렌더 일관성 때문.
3. **의존성 추가는 최소화한다.** 현재 런타임 의존성은 `next`, `react`, `react-dom`,
   `server-only` 4개뿐이다. 추가하려면 왜 직접 못 쓰는지 먼저 설명할 것.
4. **계산 결과를 파일에 저장하지 않는다.** 보유수량·평단·수익률은 항상 원장에서 파생한다.
   (`HANDOFF.md` 원칙 1)
5. **페이지에서 외부 API를 직접 부르지 않는다.** 시세는 cron만 가져온다. (원칙 2)
6. **모듈 수준 캐시를 쓰지 않는다.** 요청 경계에서 비워지는 `react cache`를 쓸 것.
7. 반응형 그리드에는 항상 `grid-cols-1`을 기본으로 둔다.

### 데이터

8. **개인 금융 데이터를 커밋하지 않는다.** `.gitignore`의 해당 블록을 지우거나
   `git add -f`로 강제 추가하지 말 것. 저장소는 Public이다.
9. 스키마를 바꾸면 `data/sample/`도 같이 갱신한다. 새로 클론한 사람이 못 돌리면 안 된다.

### 진행

10. **푸시·배포·외부 서비스 연동은 사용자 승인을 받고 한다.** 커밋까지는 진행해도 된다.
11. **검증하지 않은 것을 "완료"라고 보고하지 않는다.** 돌려보지 않았으면 그렇게 적을 것.
    이 프로젝트는 앞선 담당자가 미검증 항목을 명시해 둔 덕에 인계가 됐다.
12. 작업이 끝나면 `HANDOFF.md`의 5절(검증 상태)을 갱신한다.

### 매번 돌릴 검증

```bash
npx tsc --noEmit
```

```bash
npx eslint .
```

```bash
npm run build
```

```bash
npm test
```

---

## B. 작업 목록

우선순위 순. P0부터 처리할 것.

---

### P0-1. 시세 공급자 실호출 검증 — ✅ 2026-09-15 완료

`TWELVE_DATA_API_KEY`를 `.env.local`에 설정하고 `tsx`로 `lib/providers/`를
실제로 실행했다. 7개 종목·환율 전부 성공. 버그 1건 발견·수정(네이버 공급자가
`marketState`를 항상 `"closed"`로 하드코딩 — 실제 `marketStatus` 필드를 쓰도록 고침).
존재하지 않는 심볼을 섞어 실패 격리 동작도 확인. 자세한 내용은 `HANDOFF.md` §5 참고.

이 환경의 브라우저 도구가 `finance.naver.com`을 차단해 시세 앱과의 육안 대조는
못 했다 — 다음 담당자가 여유가 있으면 한 번 더 대조해볼 것.

**왜 최우선인가**: 이 프로젝트 아키텍처 전체가 "cron이 시세를 받아온다"를 전제로 한다.
그런데 그 코드는 **한 번도 실행된 적이 없다.** 여기가 안 되면 나머지가 다 무의미하다.

**할 일**

1. `.env.local`에 `TWELVE_DATA_API_KEY` 설정 (사용자에게 요청. 기존 `web-stock`
   프로젝트가 같은 키를 쓰고 있다)
2. 미국 종목(QLD, TQQQ, SCHD) 실호출 → 응답이 `lib/providers/twelve-data.ts`의
   가정과 맞는지 확인
3. USD/KRW 환율 실호출 확인
4. 국내 ETF(418660, 0015B0, 490590, 491620) 네이버 경로 실호출 확인
5. 틀린 부분을 고친다

**예상되는 문제**

- Twelve Data: 여러 심볼을 묶어 보낼 때 응답이 심볼 키 객체로 오는지 확인 필요.
  단일/복수 응답 형태가 다르다고 가정하고 짜 두었다
- 네이버: `polling.finance.naver.com` 응답의 `datas[0].closePrice` /
  `compareToPreviousClosePrice` 필드명이 가정이다. 다르면 `ProviderShapeError`로 터진다
- 무료 플랜 한도(하루 800회)를 테스트로 소진하지 않도록 주의

**완료 조건**

- 7개 종목 전부 실제 시세를 받아온다
- 받아온 값이 네이버 금융/증권사 앱의 값과 일치한다 (육안 대조)
- 실패 시 `ProviderShapeError` 메시지가 무엇이 어떻게 다른지 알려준다

---

### P0-2. `/api/cron/refresh` 실행 검증 — ✅ 2026-09-15 완료

실제로 호출해 시세·환율·스냅샷 갱신, 부분 실패 시 기존 값 유지, `CRON_SECRET`
인증을 전부 확인했다. `recomputeTotal()`이 정식 계산 경로(`loadPortfolio()`)와
같은 총액을 내는 것도 확인했다. 자세한 내용은 `HANDOFF.md` §5 참고.

**환경 참고**: Browser 도구로 띄운 dev 서버는 외부 API fetch가 막혀 있었다.
터미널에서 직접 `npm run dev`로 띄우니 정상 동작했다. 다음에 이 환경에서
검증할 때 참고할 것.

**왜**: 공급자가 되더라도 적재 경로가 별개다. 이것도 실행된 적이 없다.

**할 일**

1. `npm run dev` 후 `curl http://localhost:3000/api/cron/refresh` 호출
2. `data/quotes.json`, `data/fx-quote.json`, `data/snapshots.json`이 갱신되는지 확인
3. 화면을 새로고침해 새 값이 반영되는지 확인
4. 일부 종목만 실패했을 때 기존 값이 유지되는지 확인 (빈 값으로 덮어쓰면 안 됨)
5. `CRON_SECRET` 설정 시 인증이 실제로 걸리는지 확인

**특히 볼 곳**: `recomputeTotal()` 함수. 같은 요청 안에서 `loadPortfolio()`의 캐시와
방금 쓴 파일이 섞이는 지점이라 논리가 미묘하다. 총 평가금액이 맞게 나오는지 반드시 검산.

**완료 조건**: 호출 한 번으로 시세·환율·스냅샷이 갱신되고 화면에 반영된다.
응답 JSON의 `updated`/`missing`/`errors`가 실제 상황과 일치한다.

---

### P1-1. 계산 엔진 단위 테스트 도입 — ✅ 2026-09-15 완료

Vitest 도입, `lib/domain/` 5개 파일에 45개 테스트 작성 (`npm test`로 확인).
아래 경계 사례는 전부 테스트로 존재한다. 새 버그를 잡으면 이 파일들에 케이스를 추가할 것.

**왜**: 소스 5,215줄에 **테스트가 0개다.** 테스트 프레임워크조차 설치돼 있지 않다.
그런데 이 프로젝트의 가치는 계산 정확성에 있다. 숫자가 틀리면 아무 쓸모가 없는데
지금은 틀려도 알 방법이 없다.

**할 일**

1. Vitest 설치 (`web-stock` 프로젝트가 Vitest를 쓰고 있어 일관성 있음)
2. `lib/domain/` 전체에 단위 테스트 작성. 우선순위:
   - `portfolio.ts` — 이동평균 평단, 매도 시 실현손익, 예수금 파생, 통화 혼합
   - `backtest.ts` — 인출 시 차익 과세, 소진 판정, 리밸런싱 원가 승계
   - `metrics.ts` — MDD 계산 (하락 후 회복 후 더 큰 하락 같은 경계 사례)
   - `dividends.ts` — 연간 예상 계산
   - `lookthrough.ts` — 비중 정규화, 중복 종목 합산
3. `package.json`에 `test` 스크립트 추가

**반드시 포함할 경계 사례** (`HANDOFF.md` 8절의 버그들이 테스트로 고정돼야 한다)

- 보유수량보다 많이 매도하는 거래
- 전량 매도 후 재매수
- 예수금이 음수가 되는 경우
- 백테스트에서 자금이 실제로 소진되는 경우
- 수수료 때문에 목표액에 미달하는 경우 (소진으로 오판하면 안 됨)
- 초기 일시금 0인 적립식의 CAGR

**완료 조건**: `npm test` 통과. 위 경계 사례가 전부 테스트로 존재한다.

---

### P1-2. 실제 거래 데이터 입력과 백업 절차 — 🟡 2026-09-15 부분 완료

**된 것**
- 미국주식 위탁계좌 CSV(28종목 현재 보유 스냅샷)를 `scripts/import-holdings-csv.mjs`로
  가져와 `data/accounts.json`·`symbols.json`·`transactions.json`·`cashflows.json`을
  실제 데이터로 교체함. 전 페이지 렌더·계산 정상 확인 (`HANDOFF.md` §5 "실거래 데이터
  상태" 참고)
- Twelve Data 무료 플랜 분당 크레딧 한도(8) 때문에 실제 종목 수(28개+)에서 cron이
  전부 실패하던 버그를 발견·수정함 (`lib/providers/twelve-data.ts`, 8개씩 분당 청크)
- `scripts/backup-data.mjs` 작성, 수동 실행 1회 확인 (`C:\Users\ACC-002\cgportfolio-backups\`)

**안 된 것 — 다음 담당자가 이어받을 부분**
1. 실제 매매 날짜별 개별 거래 이력. 지금은 "평단가×수량"짜리 부트스트랩 거래 1건뿐이라
   종목 상세의 매매 이력·실현손익 이력은 실제와 다르다 (현재 보유분 평단가·평가금액은
   맞다). 사용자가 실제 거래내역(날짜별)을 주면 부트스트랩 거래를 지우고 교체할 것
2. 실제 입출금 이력. 지금 원금은 매입금액 합계로 근사한 값이다
3. 배당 실지급 이력 (`data/dividends.json`이 비어 있음)
4. 백업 자동화. 스크립트는 있지만 Windows 작업 스케줄러 등록은 사람이 해야 한다
   (관리자 권한/시스템 설정 변경이라 에이전트가 임의로 하지 않음)

**완료 조건**: 실제 평가금액이 증권사 앱과 일치한다 (부트스트랩 시점 기준으로는 일치
확인함 — 이후 시세가 계속 바뀌므로 재확인은 큰 의미 없음). 백업이 자동으로 돈다 (미완).

**주의**: `data/`에 실제 금융 정보가 들어가 있다. 커밋·스크린샷·로그에 노출되지 않도록
각별히 주의할 것. `scripts/import-holdings-csv.mjs`는 같은 형식의 CSV를 다시 가져올 때
재사용 가능하다 (`--dry-run`으로 먼저 미리보기, `--replace`로 실제 반영).

---

### P2-1. 시세 자동 갱신 스케줄링

**할 일**: 환경을 정한 뒤 `/api/cron/refresh`를 주기 호출하도록 건다.

- 로컬 상시 구동이면 Windows 작업 스케줄러
- VPS면 crontab
- 주기는 무료 한도를 고려해 정한다. 종목 10개 기준 하루 10회면 100회 수준이라 여유 있음

**완료 조건**: 사람이 개입하지 않아도 스냅샷이 쌓인다. 실패 시 알 수 있는 수단이 있다.

---

### P2-2. 배포

**먼저 결정할 것**: 지금 구조는 파일에 쓰기 때문에 Vercel에서 cron이 동작하지 않는다.
(`HANDOFF.md` 6절) 선택지는 셋이다.

| 안 | 내용 | 비용 |
|---|---|---|
| A | VPS에 그대로 올린다 | 월 $5 내외 |
| B | 저장소를 DB(Supabase 등)로 바꾸고 Vercel | 무료 가능, 작업량 있음 |
| C | 로컬에서만 쓴다 | 0원 |

B로 간다면 고칠 곳은 `lib/data/store.ts`(읽기)와 cron 라우트(쓰기) 둘뿐이다.
나머지 코드는 그대로다 — 그러라고 저장소를 한 겹 분리해 둔 것이다.

**사용자에게 물어서 정할 것.** 임의로 정하지 말 것.

---

### P3-1. 인증

외부에서 접근 가능한 곳에 배포한다면 **배포 전에** 붙여야 한다. 개인 자산 정보가
그대로 보이는 화면이다. 혼자 로컬에서만 쓴다면 불필요.

---

### P3-2. 과거 종가 확보 (백테스트 구간 확장)

현재 `prices.json`은 2024-01-02 이후뿐이라 백테스트가 2.7년으로 제한된다.
레버리지 상품의 복리 잠식이나 깊은 하락장의 영향을 보려면 최소 10년, 가능하면
2000년대 초반 닷컴 붕괴와 2008년을 포함하는 구간이 필요하다.

QLD 상장일(2006-06)부터 받으면 2008년 금융위기 구간이 들어와서 의미가 커진다.

---

### P3-3. ETF 구성종목 자동 수집

`data/lookthrough.json`을 수동 갱신하고 있다. 발행사가 공시하는 보유내역(CSV/API)을
받아 갱신하는 스크립트를 만들면 자산맵이 항상 최신이 된다.

---

## C. 손대지 말 것

- **`C:\Users\ACC-002\Desktop\web-stock`** — 별개 프로젝트다. 이번 작업과 무관하다
- **브랜드명 CGPORTFOLIO** — `lib/config.ts`의 `site`에서만 정의된다. 바꿀 일 없다
- **스택 재논의** — Laravel 대 Next.js는 비용까지 따져 이미 결론 났다
- **`.gitignore`의 개인 데이터 블록**

---

## D. 보고할 때

작업을 마치면 다음을 적어 보고할 것.

1. 무엇을 했는가
2. **어떻게 검증했는가** — 돌려본 명령과 실제 결과. "통과했다"가 아니라 출력을 보일 것
3. 검증하지 못한 부분은 무엇이고 왜인가
4. 새로 알게 된 제약이나 발견한 버그
5. `HANDOFF.md` 5절 갱신 여부
