# 인계서 — CGPORTFOLIO

최종 갱신 2026-09-17 · **`main`의 최신 상태와 아래 12절 재검토 결과가 인계 기준**

이 문서는 **지금까지 무엇을 만들었고, 무엇이 검증됐고, 무엇이 아직 검증되지 않았는지**를
있는 그대로 적는다. 다음 담당자는 이 문서의 5절("검증된 것 / 검증되지 않은 것")을
가장 먼저 읽을 것. **다음에 할 일은 `WORK_ORDER.md`에 있다** — 환경 함정과 실행 방법도
그 문서 0절에 정리돼 있으니 처음 받았다면 거기부터 읽는 게 빠르다.

---

## 1. 한눈에

| 항목 | 값 |
|---|---|
| 프로젝트 | CGPORTFOLIO — 개인 포트폴리오 대시보드 |
| 작업 폴더 | `C:\Users\ACC-002\Desktop\cgportfolio` (브랜치 `main`) |
| 저장소 | https://github.com/changgyun2631/CGPORTFOLIO (Public, `main`) |
| 스택 | Next.js 16 (App Router, Turbopack) · React 19 · TypeScript · Tailwind v4 |
| 런타임 의존성 | `next`, `react`, `react-dom`, `server-only` — **이게 전부다** (개발 의존성에 Vitest 추가됨) |
| 데이터 | **실제 사용자 데이터.** 미국주식 위탁계좌 1개 · 투자자산 28종목과 예수금 (2026-09-16 기준) |
| 구동 | **로컬 상시 구동 중.** 포트 3000, Windows 작업 스케줄러가 기동·갱신·백업 자동화 |
| 배포 | **없음.** 외부 공개 안 함 (인증 미구현이라 공개 전 필수) |
| 푸시 | 2026-09-16 재검토 시 `main == origin/main`. 이후에도 사용자가 별도로 요청하기 전에는 푸시하지 말 것 |

정확한 인계 커밋은 `git log -1 --oneline`으로 확인한다. 인계 시점에 작업 트리는 깨끗해야
하며, `origin/main`에는 자동으로 올리지 않는다.

### 1-1. 2026-09-16 인계 커밋에 포함된 핵심 변경

이번 커밋은 단순 UI 수정이 아니다. **현재 잔고·현금원금 계산, 실제 계좌수익률 차트,
원장 정규화, 화면의 실시간 반영 방식까지 함께 변경**했다. 다음 담당자는 아래를 먼저
숙지한 뒤 수정할 것.

#### A. 평가손익과 원금 기준

- 과거 거래원장은 매매 이력과 참고용 실현손익에 쓴다. 조회 기간이 계좌 전체가 아니므로
  현재 수량·평단·매입원가는 `data/position-basis.json`의 증권사 잔고 스냅샷이 우선한다.
- 현재 평가손익은 `투자자산 평가금액 - 증권사 매입원가 - 예상 매도수수료`다. 예수금은
  평가손익에 포함하지 않는다.
- `Transaction.action`은 `trade | transfer | split`이다. 이체·액면분할은 예수금과
  실현손익을 움직이지 않는다.
- `CashFlow.kind`는 `external | exchange | income | adjustment`다. 원금은 외부
  입출금(`external`)만 합산한다.
- `Snapshot.principalKrw`에 그 날짜의 현금원금을 저장한다. 차트 수익률은 현재 원금을
  과거 전체에 덮어쓰지 않고 각 날짜의 원금을 우선한다.
- cron이 새 스냅샷을 만들 때도 현재 `principalKrw`를 함께 기록한다.

#### B. CSV 가져오기와 원장 정규화

세 스크립트 모두 기본 실행은 `data/out-*.json`을 만들어 검토하게 하고, `--replace`를
붙여야 실제 파일을 교체한다. 출력 파일도 개인 금융 데이터이므로 `.gitignore` 대상이다.

```powershell
node scripts/import-position-basis-csv.mjs "<보유종목.csv>" --account-id=acc-us-main
node scripts/import-account-history-csv.mjs "<계좌수익률.csv>"
node scripts/normalize-ledger.mjs
```

- `import-position-basis-csv.mjs`: 보유수량·평균단가·매입원가·예상 매도수수료율
- `import-account-history-csv.mjs`: 날짜별 예탁자산과 명시적인 `입금-출금` 누적 원금
- `normalize-ledger.mjs`: 이체·분할 거래와 외부입출금·환전·수익·보정의 성격 표시
- 계좌수익률 CSV의 `예탁자산-누적손익` 역산값은 비현금 주식이동을 원금으로 오인하므로
  원금에 사용하지 않는다.

#### C. 총 평가금액 차트

- 기본 기간은 1년이다. `1일/7일/1개월/3개월/6개월/연중/1년/5년/최대`를 제공한다.
- 달력 구간 안에 실제 점이 2개 이상이면 정상적인 날짜 필터를 쓴다. 자료 공백 때문에
  2개 미만이면 가짜 점을 만들지 않고 마지막 실제 관측값을 기간별로 2/7/30/90/180/
  260/365/1825개 사용한다. 그래서 짧은 기간 버튼들이 모두 같은 두 점을 보이던 문제를
  해결했다. 화면의 `N개 스냅샷` 표기가 실제 사용한 점 수다.
- `매매` 토글은 기본 ON이다. 일반 체결만 표시하며 이체·분할은 제외한다. 매수는 초록
  점선, 매도는 빨간 점선, 같은 스냅샷에 둘 다 있으면 강조색이다. 여러 체결은 가장 가까운
  실제 스냅샷에 날짜별로 묶고 SVG `<title>`에 건수와 종목을 넣는다.
- 환율 점선, 날짜별 원금 대비 수익률, 최고점/MDD 마커, 전체화면과 마우스 호버가 함께
  동작한다.
- 사용자 최신 요청으로 매매 타점이 다시 추가됐다. 문서의 과거 기록 중 "타점 제거"라는
  표현이 보이면 이 절이 최종 결정이다.

#### D. 화면과 렌더링

- 상승은 초록색, 하락은 빨간색인 미국식 색상 규칙을 전역 토큰에 적용했다.
- 상단 티커는 같은 목록 두 벌을 이어 붙인 무한 전광판이다. 마우스/키보드 포커스 시
  멈추고, `prefers-reduced-motion`에서는 애니메이션 대신 수동 가로 스크롤을 쓴다.
- 대시보드·계좌·자산맵·종목·종목상세·투자철학·백테스트 상세는
  `dynamic = "force-dynamic"`으로 설정했다. cron이 JSON을 갱신하면 재빌드 없이 다음
  요청에서 반영되게 하기 위함이다.
- 대시보드의 주 손익 표기는 불완전한 누적 실현손익 대신 현재 평가손익을 사용한다.

#### E. 이번 커밋의 검증 결과

```text
npm test          → 5개 파일, 57개 테스트 통과
npm run lint      → 오류·경고 없음
npx tsc --noEmit  → 통과
npm run build     → Next.js 16 프로덕션 빌드 성공, 15개 경로 생성
HTTP GET /        → 200
```

실제 `localhost:3000` 브라우저에서도 다음을 확인했다.

- `7일` 선택 → 7개 스냅샷, `1개월` 선택 → 30개 스냅샷
- `매매` 토글 OFF 시 매수·매도 범례와 타점 제거, ON 시 복원
- 기본 1년 상태에서 원금 대비 수익률·환율·매수·매도 범례와 점선 표시
- 서버를 새 프로덕션 빌드로 재시작한 뒤 200 응답 확인

#### F. Claude가 시작할 때 권장 순서

1. `git status --short`가 깨끗한지 확인한다.
2. 이 문서와 `WORK_ORDER.md`를 읽는다.
3. `npm test`, `npm run lint`, `npx tsc --noEmit`을 기준선으로 한 번 실행한다.
4. 실제 금융 데이터는 `data/*.json`에 있으나 gitignored다. 내용을 응답·커밋·스크린샷에
   노출하지 않는다.
5. 서버는 `http://localhost:3000`에서 상시 구동 중이다. 소스 변경 후 프로덕션 반영은
   `npm run build` 뒤 기존 서버 작업을 정확히 식별해 재시작한다.

#### G. 다음 작업 후보

1. `data/prices.json`의 가상 가격 이력을 실제 장기 가격으로 교체(B-4). 현재 백테스트
   숫자는 의사결정에 쓰면 안 된다.
2. CSV 가져오기를 CLI가 아니라 화면에서 검토·적용할 수 있게 만들기. 민감 파일 업로드와
   검증 실패 시 원본 보존을 반드시 설계해야 한다.
3. 외부 배포가 필요해지면 JSON 쓰기 저장소를 DB로 교체하고 인증을 붙이기(B-2).
4. 1년 차트에서 거래가 많은 날의 점선 밀도를 더 다듬을 수 있다. 현재는 날짜별 그룹화까지만
   적용돼 있다.
5. 원인 불명 서버 종료는 재시작 루프로 완화했을 뿐 근본 원인을 찾지 못했다(B-0).

#### H. 추가 보완 작업 — Claude 우선순위 제안

아래는 단순 아이디어 목록이 아니라 **현재 구조를 검토했을 때 실제로 보완 가치가 있는
항목**이다. 사용자가 별도 우선순위를 주지 않으면 P0부터 진행한다. 한 번에 전부 바꾸지
말고, 각 항목을 별도 커밋으로 끝내며 완료 조건을 충족한 뒤 다음 항목으로 넘어갈 것.

##### P0 — 데이터 정확성과 손실 방지(기능 추가보다 먼저)

1. **가져오기 전후 자동 검증기 추가** — ✅ 2026-09-16 완료 (Claude)
   - 현재 CSV 스크립트는 헤더와 일부 숫자만 확인한다. `--replace` 전에 계좌·종목 참조
     누락, 중복 키, 비정상 날짜, `NaN`/무한대, 음수 수량, 스냅샷 정렬·중복 날짜,
     분할배수 오류, 최신 기준일 역행을 모두 검사해야 한다.
   - 가능하면 `scripts/validate-data.mjs` 하나에서 모든 실제 데이터 파일을 읽고 오류는
     0이 아닌 종료코드로 끝낸다. 개인 금액 자체는 로그에 출력하지 않는다.
   - **완료 조건**: 정상 데이터는 통과하고, 고의로 만든 잘못된 fixture 각각이 실패하는
     자동 테스트가 있다. 검증 실패 시 기존 `data/*.json`은 바뀌지 않는다.
   - **한 것**: 검증 규칙을 `scripts/lib/validate.mjs`에 순수 함수로 모았다(참조
     무결성·중복 id·NaN/무한대·음수 수량·스냅샷 정렬 및 중복 날짜·액면분할 배수·
     "최신 기준일 역행"까지 전부). `node scripts/validate-data.mjs`가 현재
     `data/*.json` 전체를 이 규칙으로 검사해 위반 종류와 식별자만 출력하고(금액은
     출력하지 않음) 실패 시 0이 아닌 종료코드를 낸다. 세 가져오기 스크립트
     (`import-position-basis-csv.mjs`, `import-account-history-csv.mjs`,
     `normalize-ledger.mjs`)에도 `--replace` 직전 같은 검증기를 끼워 넣어, 검증
     실패 시 반영을 중단하고(`process.exit(1)`) 기존 실제 파일은 쓰지 않는다.
     미리보기(`out-*.json`)는 검증 실패해도 그대로 써서 사용자가 무엇이 잘못됐는지
     볼 수 있게 했다.
   - **검증**: `scripts/lib/__tests__/validate.test.mjs`에 정상 픽스처 1개(에러 0건)와
     항목별 고의 오류 픽스처 20여 개(각각 최소 1건 이상 에러) 테스트 작성,
     `npm test` 79개 전부 통과. `node --check`로 다섯 파일 문법 확인. 실제
     `position-basis.json`이 없는 워크트리에서 CLI가 store.ts와 같은 방식으로
     "데이터 없음" 안내를 내는 것, 메인 체크아웃의 실제 데이터로 실행했을 때
     통과하는 것까지 확인. `--replace` 경로에서 실제로 검증 실패를 일으켜
     기존 파일이 보존되는 경로는, 운영 중인 실데이터를 일부러 깨야 재현되므로
     이번엔 실행하지 않았다(코드 경로는 세 스크립트 모두 동일한 패턴).

2. **JSON 쓰기를 원자적 교체 방식으로 변경** — ⚠️ 2026-09-16 파일 단위 완료, 묶음 트랜잭션 보완 필요 (Claude/GPT 재검토)
   - cron과 `--replace` 가져오기가 파일에 직접 쓰는 동안 프로세스가 죽거나 두 작업이
     겹치면 JSON이 일부만 기록될 위험이 있다.
   - 같은 디렉터리에 임시 파일 작성 → flush/close → 기존 파일 백업 → 원자적 rename
     순서의 공용 유틸리티를 만들고, cron 및 세 가져오기 스크립트가 같이 사용하게 한다.
     Windows에서 대상 파일이 존재할 때 rename 동작도 실제로 검증한다.
   - **완료 조건**: 쓰기 도중 실패를 주입해도 기존 파일을 정상적으로 다시 읽을 수 있고,
     cron과 import의 동시 실행이 감지되거나 직렬화된다.
   - **한 것**: `writeJsonAtomic`(임시파일 쓰기 → `fsyncSync` → 기존 파일을 `.bak`으로
     복사 → `renameSync`로 교체)과 `withDataLock`(`data/.write.lock` 파일을 배타적으로
     생성해 잠그고, 이미 잠겨 있으면 보유자 pid가 살아있는지·5분 넘게 오래됐는지로
     "동시 실행"과 "죽은 프로세스가 남긴 잠금"을 구분)을 만들었다. Next.js(TS)와 스크립트
     (plain JS)가 서로 다른 실행 방식이라 로직이 같은 두 사본으로 나눴다
     (`lib/data/atomic-write.ts` / `scripts/lib/atomic-write.mjs`, 파일 상단에
     서로를 가리키는 주석). `app/api/cron/refresh/route.ts`는 quotes/fx/snapshots 세
     파일 쓰기 전체를 한 잠금으로 묶었고, 세 가져오기 스크립트는 `--replace`일 때만
     잠근다(미리보기 `out-*.json`은 cron과 겹칠 일이 없어 잠그지 않는다).
   - **검증**: Windows에서 `renameSync`가 존재하는 대상 파일을 실제로 덮어쓰는 것을
     먼저 스크립트로 확인(문서에 있던 우려와 달리 이 Node/Windows 조합에서는 정상
     동작). `scripts/lib/__tests__/atomic-write.test.mjs` 7건: 신규 파일 쓰기, 기존
     파일 `.bak` 보존, **`renameSync`에 실패를 주입해도 대상 파일이 이전 내용 그대로
     읽히는 것**(Vitest `vi.mock("node:fs", ...)`로 rename 단계만 실패시킴 — 이후
     재시도하면 정상 교체), 잠금 획득 후 정상/예외 종료 양쪽에서 해제, **살아있는
     프로세스의 잠금은 즉시 에러로 거부**(자기 자신의 pid로 재현), **죽은 프로세스의
     잠금은 정리하고 진행**(존재하지 않는 pid로 재현). `npm test` 86개 전부 통과,
     `npx tsc --noEmit`·`npx eslint .` 통과.
   - **메인 체크아웃(실데이터) 검증**: `npm run build`가 실제 데이터로 15개 경로
     전부 생성하며 완전히 통과했다. 새 빌드를 반영하려고 서버를 재시작하는 과정에서
     사고가 하나 났다 — 아래 "재시작 중 발견한 사고" 참고. 사고를 정리한 뒤 실제
     `GET /api/cron/refresh`를 호출해 봤고, `ok:true, 갱신 63건, 누락 0건, 오류 0건`
     으로 성공했다. `data/quotes.json`·`fx-quote.json`·`snapshots.json`이 새 값으로
     교체됐고, 각각의 `.bak` 파일에 교체 전 내용이 그대로 남아 있는 것(원자적
     교체+백업이 실제로 동작)과 작업이 끝난 뒤 `.write.lock`이 정상적으로 사라진
     것(잠금 해제)을 직접 확인했다.
   - **재시작 중 발견한 사고(코드 버그 아님)**: 새 빌드 반영을 위해
     `Stop-ScheduledTask` → `Start-ScheduledTask`로 서버를 재시작했는데, 하필 그
     직전에 보낸 오래 걸리는(~3분) `/api/cron/refresh` 요청을 처리 중이던
     `node.exe`가 `Stop-ScheduledTask`로 실제로는 안 죽고 포트 3000을 계속 물고
     있었다. 이후 새로 뜬 프로세스들이 전부 `EADDRINUSE`로 즉시 죽으며 08:57~09:01
     사이 짧은 반복 재시작이 다시 발생했다 — `taskkill //F //PID <좀비pid>`로
     정리하자 바로 정상화됐다. **이번 P0-2 코드와는 무관한 사고**이고(데이터 파일은
     전혀 안 건드려진 채였다), 오히려 B-0의 "여전히 원인 불명"이던 초단기 반복
     재시작 패턴의 실제 메커니즘 하나를 확인한 계기가 됐다 — 자세한 내용과 교훈은
     `WORK_ORDER.md` B-0과 0-4절(함정 10번)에 기록했다.

3. **백업 복원 훈련과 절차 문서화** — ⚠️ 2026-09-16 리허설 구현됨, 작업공간별 백업 분리 필요 (Claude/GPT 재검토)
   - 백업 생성은 검증했지만 실제 복원은 아직 검증하지 않았다. 최신 백업을 임시 폴더에
     풀어 JSON 파싱·필수 파일·참조 무결성을 검사하는 `--verify` 또는 복원 점검 스크립트가
     필요하다.
   - 운영 데이터에 바로 덮어쓰는 자동 복원은 만들지 말고, 복원 전 새 백업 생성과 사용자
     확인 단계를 둔다.
   - **완료 조건**: 원본을 건드리지 않는 복원 리허설 성공 기록과 수동 복원 명령이 이
     문서에 남아 있다.
   - **한 것**: `scripts/verify-backup.mjs`(`npm run backup:verify`)를 만들었다.
     지정한 백업(생략 시 `~/cgportfolio-backups/`의 최신)을 **임시 폴더에 복사한
     뒤에만** 검사한다 — 원본 백업 폴더는 읽기만 하고 절대 쓰지 않는다. 필수 파일
     8종(`accounts/symbols/transactions/cashflows/dividends/snapshots/quotes/
     fx-quote.json`) 존재 확인, JSON 파싱, 그리고 P0-1에서 만든 `validateAll`로
     참조 무결성까지 검사한다. 검사 후 임시 폴더는 지운다(`--keep-temp`로 남길 수
     있음). 자동 복원 기능은 일부러 만들지 않았고, 아래 수동 절차를 따르게 했다.
   - **검증**: `scripts/lib/__tests__/verify-backup.test.mjs` 4건 — 정상 백업
     통과(+검사 전후 원본 백업 폴더 파일 목록이 그대로인 것까지 확인), 필수 파일
     누락·JSON 손상·참조 무결성 위반(존재하지 않는 계좌) 각각 실패. 실제
      당시 `~/cgportfolio-backups/`의 최신 백업으로도 돌려서 통과 확인(6절 참고).
      `npm test` 90개 전부 통과, `npx tsc --noEmit`·`npx eslint .` 통과.
   - **GPT 후속 감사 정정**: 이후 Claude의 별도 seed 작업공간도 같은 전역 백업 폴더를
     사용하면서, 현재 가장 최신인 `2026-09-16T01-14-58-657Z`는 운영 원본이 아니라
     5계좌/테스트 원장 백업이 됐다. 검증기는 데이터가 내부적으로 일관된지만 보므로 이를
     정상 운영 백업으로 오인해 통과시킨다. 이 폴더는 복원에 사용하지 말고, 아래 I절의
     데이터 원본 감사 결과와 작업공간별 백업 분리 작업을 먼저 따를 것.

   **수동 복원 절차** (자동화하지 않음 — 사람이 매 단계 확인하고 진행할 것):
   ```powershell
   # 1. 복원으로 지금 상태를 잃지 않게 먼저 현재 data/ 를 백업한다.
   npm run backup

   # 2. 복원하려는 백업이 실제로 정상인지 리허설로 먼저 확인한다.
   npm run backup:verify -- "<백업 폴더 경로>"
   # (경로 생략 시 가장 최신 백업을 검사한다)

   # 3. 리허설이 통과하면 서버를 멈춘다.
   Stop-ScheduledTask -TaskName "CGPORTFOLIO 서버"

   # 4. data/ 를 백업 내용으로 덮어쓴다(기존 data/ 에만 있고 백업엔 없는 파일은
   #    남는다 — 완전히 똑같이 맞추려면 미리 data/ 를 비우고 복사할 것).
   Copy-Item -Recurse -Force "<백업 폴더 경로>\*" "C:\Users\ACC-002\Desktop\cgportfolio\data\"

   # 5. 서버를 다시 띄우고 화면이 맞게 보이는지 확인한다. 재시작 전 반드시
   #    `netstat -ano | grep ":3000.*LISTENING"`로 포트가 비었는지 확인할 것
   #    (WORK_ORDER.md 0-4절 함정 10번 — 안 그러면 EADDRINUSE 크래시 루프가 난다).
   Start-ScheduledTask -TaskName "CGPORTFOLIO 서버"
   ```

4. **시세와 잔고의 신선도 경고 표시** — ✅ 2026-09-16 완료 (Claude)
   - 현재 총평가액은 최신 quote, 매입원가는 마지막 `position-basis`, 원금은 마지막
     snapshot 기준일을 섞어 쓴다. 날짜가 오래되거나 서로 크게 벌어져도 화면상 경고가 없다.
   - 대시보드에 `시세 기준`, `잔고 기준`, `원금 기준`을 표시하고, 장중/장후 기준을 감안한
     임계시간을 넘으면 “업데이트 필요” 배지를 띄운다. 데이터가 오래된 상태에서 숫자를
     틀렸다고 오해하는 일을 줄이는 항목이다.
   - **완료 조건**: 정상·경계·오래된 시각을 고정한 테스트와 브라우저 표시 확인.
   - **한 것**: `lib/domain/freshness.ts`에 순수 함수로 만들었다.
     `isUsMarketHours(now)`는 뉴욕 시각 기준 평일 09:30~16:00을 정규장으로 보고
     (공휴일은 고려 안 함 — 완벽한 거래소 달력이 아니라 대략적인 임계시간 분기가
     목적), `assessFreshness`는 이를 바탕으로 시세·원금은 장중 8시간/장 마감
     48시간(6시간 주기 cron + 여유, 주말엔 마지막 종가가 그대로 유효), 잔고는
     수동 CSV 갱신이라 30일로 훨씬 느슨하게 잡는다. `earliestAsOf`는 `quotes.json`
     여러 종목 중 "일부만 갱신 실패"해도 가장 오래된 값을 잡아내려고 최솟값을
     쓴다. `components/dashboard/freshness-badges.tsx`가 fresh는 눈에 안 띄게,
     stale/missing만 빨간 "업데이트 필요" 배지로 강조해 대시보드 상단(`app/page.tsx`)
     에 붙였다.
   - **검증**: `lib/domain/__tests__/freshness.test.ts` 16건 — 장중/장마감/개장
     경계(09:30)/폐장 경계(16:00)/주말/서머타임까지 고정된 실제 타임스탬프로
     `isUsMarketHours`를 확인했고, `assessFreshness`는 정상(1시간 전)·경계
     (정확히 임계시간·1분 초과)·오래됨(1년 전)·값 없음(`missing`)·잔고의 느슨한
     기준(같은 10일 전이 시세는 stale, 잔고는 fresh)·장중 임계시간(8시간)까지
     각각 고정 시각으로 검증했다. 브라우저 확인: `node scripts/seed.mjs`로 이
     워크트리에 가상 데이터를 만들고(실데이터 아님) `next dev`로 직접 띄워 확인 —
     정상 상태(잔고 데이터가 아예 없어 "잔고 기준 없음 업데이트 필요"만 빨간
     배지, 시세·원금은 회색), `quotes.json`의 `asOf`를 100시간 전으로 강제
     조작한 뒤 "시세 기준 ... 업데이트 필요"로 바뀌는 것, 모바일(375px)에서
     가로 오버플로 없이 배지가 줄바꿈되는 것, 다크 테마에서 배지 색 대비가
     기존 `text-down`/`bg-down-soft`/`text-faint` 토큰 그대로 나오는 것까지
     전부 확인. `npm test` 106개 전부 통과, `npx tsc --noEmit`·`npx eslint .` 통과.

##### P1 — 현재 기능의 정직한 표현과 회귀 방지

5. **희소 기간 차트의 의미를 화면에 명시** — ✅ 2026-09-16 완료 (Claude)
   - 지금 `7일`에 달력상 7일치가 없으면 “최근 7개 실제 관측값”을 보여준다. 버튼은
     작동하지만 날짜 간격이 길 수 있어 사용자가 달력 7일로 오해할 수 있다.
   - 우선 실제 계좌수익률 CSV로 빠진 날짜를 최대한 채운다. 그래도 대체 규칙이 작동하면
     `7일 · 실제 관측 7개` 또는 `자료 공백으로 최근 관측값 표시`를 차트에 표시한다.
   - 장기적으로는 `filterSnapshots`이 데이터와 함께 `usedFallback`/실제 시작일을 반환하게
     만들어 UI와 계산이 같은 사실을 공유하도록 한다.
   - **완료 조건**: 달력 필터와 관측값 대체 두 경로의 단위 테스트, 실제 시작·종료일 표기.
   - **한 것**: `filterSnapshots`가 `Snapshot[]`만 돌려주던 걸 `{ snapshots, usedFallback }`
     로 바꿨다 — 값 자체가 아니라 "대체 규칙을 썼는지"라는 사실을 계산과 화면이
     공유하게 하려는 게 스펙의 요지였다. 실제 시작·종료일은 별도 필드 없이
     화면이 `snapshots[0].at`/`snapshots.at(-1).at`을 직접 읽게 했다(중복 데이터를
     만들지 않으려고). `ValueChart`(`components/charts/value-chart.tsx`)가 기간
     버튼 아래에 `실제 시작일 ~ 실제 종료일 · N개 스냅샷`을 항상 보여주고,
     `usedFallback`이면 그 옆에 "자료 공백으로 최근 관측값 표시 — ○○ 구간을 다
     못 채웠습니다" 배지를 추가로 붙인다.
   - **검증**: `lib/domain/__tests__/metrics.test.ts`에 `usedFallback` 관련 6건
     추가(빈 배열·all 범위는 false, 달력에 점이 충분하면 false, 부족하면 true,
     기간별로 다른 대체 개수를 쓰면서도 전부 true, 대체된 결과의 실제 시작일이
     달력 구간과 다를 수 있음을 직접 확인). 브라우저로 두 경로 다 확인: 정상
     달력 구간(예: 실제 인트라데이 스냅샷이 있던 최근 1일 → "2026-09-14 ~
     2026-09-15 · 15개 스냅샷", 대체 배지 없음)과, 최근 스냅샷을 일부러 성기게
     만든 뒤(이 워크트리의 가상 데이터만 건드림, 확인 후 원복) "7일" 버튼 →
     실제로는 "2026-08-07 ~ 2026-09-15 · 7개 스냅샷"에 대체 배지가 뜨는 것까지
     확인. 모바일(375px)에서 배지가 줄바꿈되며 오버플로 없음. `npm test` 108개
     전부 통과, `npx tsc --noEmit`·`npx eslint .` 통과.

6. **매매 타점 계산을 순수 함수로 분리하고 테스트** — ✅ 2026-09-16 완료 (Claude, 접근성은 부분 검증)
   - 현재 가장 가까운 스냅샷 선택과 그룹화가 `ValueChart` 렌더 본문에 있어 단위 테스트가
     없다. 일자 경계, 동일 시각, 같은 날 매수·매도 동시 발생, 구간 밖 거래를 회귀 테스트로
     고정한다.
   - 거래가 많은 1년/최대 차트는 점선이 조밀하다. 줌을 새로 만들기 전에 일별 집계,
     최대 표식 수, 심볼 필터 중 하나를 사용자와 선택한다. 임의로 거래를 누락하면 안 된다.
   - **완료 조건**: 타점 매핑 테스트와 키보드로 `매매` 토글 가능한 접근성 검증.
   - **한 것**: `lib/domain/trade-markers.ts`의 `groupTradeMarkers(snapshotTimes, trades)`로
     뽑아냈다. 화면 좌표(x/y) 없이 `at` 문자열 배열만 받게 해서, 픽셀 계산과 완전히
     분리된 순수 함수로 테스트할 수 있다. `ValueChart`는 이 함수의 결과를
     `points[snapshotIndex]`로 매핑만 한다. 점선 밀도 줄이기(일별 집계/표식 수 제한/
     심볼 필터)는 완료 조건에 없고 사용자와 먼저 방향을 정해야 하는 항목이라 이번엔
     손대지 않았다 — 필요하면 별도로 다룰 것.
   - **검증**: `lib/domain/__tests__/trade-markers.test.ts` 10건 — 점/거래 없음,
     구간 밖(이전/이후), 일자 경계(스냅샷과 정확히 같은 시각), 같은 날 매수·매도
     동시(side: "both"), 동일 시각 여러 종목 집계, 정중앙 동점 시 먼저 나온 스냅샷
     유지, 가장 가까운 쪽 배정, 여러 그룹의 인덱스 오름차순 정렬, 잘못된 날짜 무시.
     `npm test` 118개 전부 통과, tsc·eslint 클린.
   - **접근성 — 부분 검증에 그침**: 코드는 순정 `<button type="button">`(커스텀
     `onKeyDown` 없음, `tabIndex: 0`, `disabled: false`, `aria-pressed` 제공)이라
     브라우저 표준대로면 Enter/Space로 반드시 활성화된다. 다만 **이 환경의 Browser
     자동화 도구(`computer` key 액션)로 실제 종단간 검증은 못 했다** — 마우스
     클릭으로는 `aria-pressed`가 정상 토글되는 걸 확인했지만, 포커스가 버튼에 있는
     상태에서 `Return`/`space` 키를 눌러도 값이 안 바뀌었다(자동화 브라우저 패널이
     "hidden" 상태였던 것과 관련 있어 보임 — 실제 OS 레벨 키 입력이 렌더러까지
     안 갔을 가능성). 코드 구조상 표준 동작이 보장된다고 보지만, **실제 사용자
     브라우저(Tab으로 포커스 이동 → Enter/Space)로 한 번 더 눈으로 확인하는 걸
     권한다** — 검증 못 한 걸 완료라고 적지 않는다는 규칙에 따라 여기 명시해 둔다.

7. **계산 결과 교차검증 리포트 추가** — ✅ 2026-09-16 완료 (Claude)
   - 새 잔고 CSV 반영 때 `수량 × 현재가`, 매입금액, 예상 수수료, 종목별 평가손익,
     전체 합계가 증권사 파일과 얼마 차이 나는지 로컬에서만 보여주는 검증 리포트가 있으면
     오류를 화면에서 발견하기 전에 잡을 수 있다.
   - 리포트에는 실제 금액이 들어가므로 `data/out-*` 또는 별도 gitignored 폴더에만 만들고
     터미널에는 종목명과 차이의 개수/허용범위만 출력한다.
   - **완료 조건**: 허용 오차를 넘는 종목이 있으면 `--replace`가 중단된다.
   - **한 것**: "수량 × 현재 시세"와 비교하는 대신, **CSV 자체가 이미 갖고 있는
     "평가손익" 열**과 CSV의 다른 열(평가금액-매입금액-수수료)로 재계산한 값을
     맞춰보는 쪽을 택했다 — 시세 API에 기대면 CSV를 뽑은 시점과 지금 시세가 늘
     다르므로 허용 오차가 시장 변동에 휘둘려 "얼마가 정상"인지 정하기 어렵다.
     CSV 내부 일관성만 보면 시장과 무관하게 결정적이고, 열이 밀렸거나 인코딩이
     깨진 파싱 오류를 정확히 잡아낸다. `scripts/lib/cross-check.mjs`의
     `crossCheckPositionBasis`(허용 오차 기본 ±5원)를
     `import-position-basis-csv.mjs`에 연결했다. 상세 금액(재계산값·CSV값·
     종목별 차이)은 `data/out-position-basis-crosscheck.json`(기존 `/data/out-*.json`
     gitignore 규칙에 이미 포함)에 항상 쓰고, 터미널에는 초과한 종목명과 건수·
     허용범위만 찍는다. `--replace`일 때 허용 오차를 넘는 종목이 하나라도 있으면
     `process.exit(1)`로 중단하고 기존 `position-basis.json`은 그대로 둔다
     (미리보기 모드에선 경고만 찍고 `out-*.json`은 계속 쓴다 — 검토용이라 그대로
     보여줘야 함).
   - **검증**: `scripts/lib/__tests__/cross-check.test.mjs` 6건(정확히 일치·오차
     이내·오차 초과·여러 종목 중 일부만 초과·사용자 지정 허용 오차·빈 배열). 여기
     그치지 않고 **PowerShell로 실제 EUC-KR 인코딩 CSV를 만들어 스크립트 전체를
     세 번 실제로 돌렸다**(이 워크트리의 스크래치 파일, 커밋 전 삭제): (1) 정상
     값 2종목 → 교차검증 통과, 리포트 파일에 `diffKrw: 0` 확인 (2) 평가손익을
     고의로 틀리게 넣은 실제 종목코드(QLD) → `--replace`가 정확히 교차검증
     단계에서 `교차검증 실패 — 반영을 중단합니다`로 멈추고 `position-basis.json`이
     생성되지 않는 것 확인 (3) 같은 종목을 정상 값으로 고쳐 `--replace` → 실제로
     `position-basis.json`이 만들어지고 쓰기 잠금도 정상 해제되는 것까지 확인.
     `npm test` 124개 전부 통과, `npx tsc --noEmit`·`npx eslint .`·`node --check` 통과.

8. **동적 렌더링 경계 재검토** — ✅ 2026-09-16 완료 (Claude) — **실제 버그 2건 발견·수정**
   - 현재 포트폴리오 숫자가 있는 페이지는 `force-dynamic`으로 바꿨지만, 새 페이지를
     추가할 때 빠뜨리기 쉽다. 어떤 페이지가 실데이터에 의존하는지 목록/테스트로 고정하거나
     포트폴리오 레이아웃 경계에서 일관되게 적용할 방법을 검토한다.
   - **완료 조건**: cron 갱신 후 재빌드 없이 각 실데이터 페이지가 새 기준시각을 보이는
     통합 확인 절차가 있다.
   - **찾은 버그**: 전체 `app/` 페이지를 조사한 결과 `app/backtests/page.tsx`와
     `app/dividends/page.tsx`가 `loadBacktests`/`loadDividendSummary`·`loadPortfolio`로
     cron이 갱신하는 시세·잔고 파생값을 쓰면서도 `dynamic = "force-dynamic"`이
     빠져 있었다 — 스펙이 경고한 "새 페이지 추가 때 빠뜨리기 쉽다"가 실제로
     일어난 사례다. 둘 다 추가했다. (`calendar`/`reports`/`reports/[slug]`도
     `dynamic`이 없지만 이쪽은 사용자가 직접 편집하는 콘텐츠 파일이지 cron이
     갱신하는 포트폴리오 숫자가 아니라서 — 스펙 원문의 "포트폴리오 숫자가 있는
     페이지" 기준에 맞춰 — 이번 범위에서는 손대지 않았다.)
   - **재발 방지**: `lib/data/__tests__/dynamic-pages.test.ts`가 `app/**/page.tsx`
     전체를 스캔해서, `lib/data/views.ts`의 포트폴리오·시세 파생 로더
     (`loadPortfolio`/`loadAccountSummary`/`loadAssetMap`/`loadDividendSummary`/
     `loadRecentTrades`/`loadChartTrades`/`loadRecentCashFlows`/`loadBacktests`/
     `loadBacktest`/`loadSymbolDetail`/`loadSparklines`/`loadPriceHistory`) 중
     하나라도 쓰는 페이지는 반드시 `dynamic = "force-dynamic"`을 내보내야
     한다고 소스 텍스트로 고정했다. 완벽한 파서는 아니고(문자열 매칭), 목록도
     새 로더가 추가되면 같이 갱신해야 하지만, 이 프로젝트 규모에서 "새 페이지가
     이 규칙을 깜빡했는지" 자동으로 잡기엔 충분하다.
   - **검증**: 고친 뒤 픽스 전 상태를 일부러 재현해(dividends 페이지의
     `dynamic` 줄을 임시로 지움 — 워크트리에서만, 커밋 전 원복) 테스트가 정확히
     그 한 줄을 잡아 실패하는 것까지 확인했다(회귀 감지가 실제로 작동함을
     증명). "cron 갱신 후 재빌드 없이 반영"의 가장 결정적인 증거는 `npm run
     build`의 Route 표다 — 고치기 전엔 두 라우트가 표에서 다른 정적 페이지들과
     구분이 안 됐을 텐데, 지금은 `ƒ /backtests`, `ƒ /dividends`로 명시적으로
     Dynamic(요청마다 서버 렌더링) 표시가 뜬다. 브라우저로 이 워크트리의 가상
     데이터를 넣고 두 페이지가 여전히 정상 렌더링되는 것도 확인했다. `npm test`
     134개 전부 통과(새 테스트 10건 포함), `npx tsc --noEmit`·`npx eslint .` 통과.

##### P2 — 사용 편의와 운영 품질

9. **안전한 CSV 가져오기 화면** — ⚠️ 2026-09-16 구현됨, 동시 변경 보호 보완 필요 (Claude/GPT 재검토)
   - CLI 세 도구를 한 화면에서 “파일 선택 → 파싱 결과 미리보기 → 검증 → 적용” 순서로
     사용할 수 있게 한다. 적용 직전 자동 백업, 오류 행 표시, 적용 후 전후 차이 요약,
     재실행 중복 방지가 필요하다.
   - 로컬 전용이어도 업로드 파일명·계좌번호·금액을 브라우저 로그나 서버 로그에 남기지
     않는다. 외부 배포 전에는 인증 없이는 절대 노출하지 않는다.
   - **완료 조건**: 적용 취소·검증 실패·중복 업로드·부분 실패 시 기존 데이터가 보존된다.
   - **먼저 한 리팩터링**: 네 CLI 스크립트(`import-position-basis-csv.mjs`,
     `import-account-history-csv.mjs`, `normalize-ledger.mjs`, `backup-data.mjs`)에
     절차적으로 섞여 있던 파싱·변환·백업 로직을 파일 IO 없는 순수 함수로
     `scripts/lib/`(csv.mjs, import-position-basis.mjs, import-account-history.mjs,
     ledger-normalize.mjs, backup.mjs)에 뽑아냈다. CLI 스크립트는 이제 인자
     처리·파일 IO만 하는 얇은 wrapper고, 웹 화면의 서버 액션이 **같은 함수를
     그대로** 호출한다 — 로직을 두 번 구현하지 않는다. 리팩터링 후 기존 네
     스크립트를 실제 EUC-KR CSV로 다시 돌려 동작이 그대로인지 확인했다.
   - **한 것**: `/accounts/import`(계좌 화면에 링크 추가)에 세 섹션. 각각
     "미리보기(파싱·검증·[잔고만] 교차검증)" → 결과 확인 → "적용(자동 백업 후
     반영)" 또는 "취소" 순서다. 미리보기 결과는 `data/`가 아니라 OS 임시
     폴더(`lib/import/staging.ts`)에 30분 TTL로 저장하고 불투명 토큰(UUID)만
     클라이언트에 준다 — 적용하면(성공/실패 무관) 그 자리에서 지워서 같은
     토큰으로 두 번 적용을 막는다(재실행/중복 업로드 방지). 적용 서버 액션은
     받은 토큰의 데이터를 **한 번 더** 검증한 뒤에만 `backupData` →
     `withDataLock`+`writeJsonAtomic`으로 반영한다 — 클라이언트가 검증 실패
     상태에서 억지로 적용을 시도해도(적용 버튼은 꺼져 있지만) 서버가 최종
     방어선이다. 서버 코드 어디에도 파일명·계좌ID·금액을 `console.log`하지
     않는다(화면에는 물론 보여준다 — 로컬 전용 화면이라 그건 미리보기의
     목적이다).
   - **일부러 안 한 것**: "적용 후 전후 차이 요약"은 만들지 않았다. 잔고
     CSV는 이미 P1-7의 교차검증(CSV 자체 평가손익 vs 재계산값)이 오류 잡이
     역할을 겸하고, 계좌수익률·원장 정규화는 미리보기 화면에 이미 건수·검증
     결과가 나오므로 최소 범위(완료 조건에 명시된 것)로 마쳤다. 필요해지면
     `data/*.json`을 적용 전후로 비교하는 diff를 추가할 수 있다.
   - **검증**: 새 순수 함수 5개에 대한 단위 테스트 28건. 서버 액션 3종에
     대한 통합 테스트 14건(`lib/import/__tests__/`) — 임시 격리 디렉토리를
     `dataDir`/`backupRoot`로 주입해(`vi.mock("../paths", ...)`) 실제 파일
     IO까지 검증한다: 정상 반영(파일이 실제로 써지고 백업이 만들어짐),
     **클라이언트가 검증 실패 상태로 적용을 직접 호출해도 서버가 거부하고
     기존 파일이 그대로임**(클라이언트 우회 방어), **같은 토큰으로 두 번
     적용하면 두 번째는 거부됨**(재사용 방지), **여러 계좌수익률 CSV 중
     하나라도 헤더가 깨지면 전체가 실패하고 기존 스냅샷이 보존됨**(부분
     실패), 30분 TTL 만료. `staging.ts` 자체도 7건(경로 조작 토큰 거부 포함).
     브라우저로 이 워크트리의 seed 데이터에 실제 EUC-KR 인코딩 CSV를
     업로드해(JS로 File 객체를 만들어 input에 주입) 정상 플로우와 취소를
     직접 확인했다 — 파싱된 종목 수·검증 통과·교차검증 통과가 화면에 정확히
     나왔고, 적용 후 `data/position-basis.json`이 실제로 생성되고 자동
     백업도 만들어진 것을 파일시스템에서 직접 확인했다. **클라이언트 우회
     방어는 브라우저에서 직접 재현하지 못했다** — 이 세션의 브라우저
     자동화 도구가 비활성화된 버튼에 보낸 synthetic click/이벤트가 React의
     이벤트 처리로 이어지지 않았다(환경 한계로 보임, 실제 사용자 클릭이면
     문제없이 막힐 것). 그래서 그 경로는 위 통합 테스트로 대체 검증했다.
     `npm test` 180개 전부 통과, `npx tsc --noEmit`·`npx eslint .`·`npm run
     build`(`/accounts/import`가 `ƒ` Dynamic으로 등록됨) 통과.

10. **서버/시세 작업 상태 화면과 로그 보존정책** — 🔴 2026-09-16 구현됨, 시각 계산 오류 수정 필요 (Claude/GPT 재검토)
    - B-0의 서버 재시작 횟수, 마지막 성공 갱신, 다음 예정 시각, 누락 심볼 수를 로컬 관리
      화면이나 읽기 전용 상태 파일에서 확인하게 한다.
    - 현재 로그에는 평가금액이 남을 수 있다. 민감 값은 제거하고 날짜·성공/실패·건수만
      남기며 크기 기반 또는 일 단위 회전 정책을 둔다.
    - **완료 조건**: 서버 재시작/시세 실패를 숫자 노출 없이 진단할 수 있다.
    - **한 것**: `/status`(푸터에 "운영 상태" 링크) 화면 하나로 정리했다.
      `lib/domain/log-status.ts`가 `server.log`/`refresh.log` 텍스트를 순수
      함수로 파싱한다(파일 IO는 `lib/status/read-logs.ts`가 따로 맡는다) —
      최근 24시간 재시작 횟수·마지막 기동/최근 이벤트 8개, 10분 안에 3회
      이상 재시작하면 "크래시 루프 의심" 경고, 시세 갱신의 마지막 성공/실패
      시각과 건수(갱신·누락·오류 개수만, 총액은 없음), 최근 8회 중 실패
      비율, 다음 예정 시각(등록된 고정 스케줄 02/08/14/20시를 그대로 반영한
      순수 날짜 계산 — 작업 스케줄러를 실제로 조회하진 않는다)까지 전부
      화면에 보이지만 **어디에도 원화·달러 금액은 없다**.
    - **로그 자체의 민감값도 제거**: `refresh.log`가 매 갱신마다 "총액
      ...원"을 실제로 기록하고 있었다(회고해 보니 이것도 P2-10이 고치라고
      한 바로 그 문제였다) — `refresh-quotes.mjs`에서 뺐다. 앞으로는
      건수·소요시간만 남는다.
    - **로그 회전**: `refresh.log`는 원래도 최근 500줄로 트림하고 있었는데,
      `server.log`(재시작 루프가 쓰는 로그, 배치 파일이라 자체 회전이
      없었다)는 무한정 자랐다. `start-server.cmd`를 직접 고치는 건 위험도가
      높아서(한글 텍스트 문제로 이미 한 번 스크립트가 죽은 전적이 있다,
      WORK_ORDER B-0) 건드리지 않고, 대신 6시간마다 안정적으로 도는
      `refresh-quotes.mjs`가 자기 로그를 트림하는 김에 `server.log`도 같이
      500줄로 트림하게 했다(`scripts/lib/log-rotate.mjs`) — 새 예약 작업
      없이 기존 주기에 얹었다.
    - **검증**: `log-status.ts` 16건, `log-rotate.mjs` 3건(파일 없음/한도
      이내/한도 초과 각각). 브라우저로 **실제 운영 로그**(이 워크트리가 같은
      사용자 홈의 `~/cgportfolio-logs/`를 그대로 읽는다)를 띄워 확인했는데,
      뜻밖에도 오늘 하루 서버가 **62회** 재시작했고 "크래시 루프 의심"
      경고가 실제로 떴다 — 08:54~09:00 사이 P0-2 검증 때 겪은 EADDRINUSE
      크래시 루프(이미 WORK_ORDER B-0에 기록된 사고)를 이 화면이 정확히
      집어낸 것이었다. 화면의 시각이 실제 로그보다 9시간 빠르게 보이는
      현상은 당시 정상이라고 기록했으나, GPT 재검토 결과 **실제 버그**다.
      Windows 시스템 시각과 `server.log`는 KST인데 로그 파서가 이를 UTC로
      생성한 뒤 다시 KST로 표시해 9시간이 중복 가산된다. 다음 예정 시각도
      KST 예약시간을 UTC로 계산해 실제 Task Scheduler 값과 다르다. 아래 I절을
      기준으로 수정할 것. `npm test`
      199개 전부 통과, `npx tsc --noEmit`·`npx eslint .`·`npm run
      build`(`/status`가 `ƒ` Dynamic으로 등록됨) 통과.

11. **접근성·반응형·시각 회귀 점검** — ✅ 2026-09-16 완료 (Claude)
    - 375px/768px/데스크톱에서 차트 버튼 줄바꿈, 전광판, 표 가로 스크롤, 라이트/다크를
      다시 확인한다. SVG의 매매 타점은 `<title>`만 있으므로 키보드/스크린리더가 거래
      요약을 알 수 있는 별도 텍스트도 검토한다.
    - `prefers-reduced-motion`에서 전광판이 멈추고 중복 복사본이 숨겨지는지 자동 또는
      수동 체크리스트로 고정한다.
    - **찾은 것과 고친 것**: `<title>`은 SVG가 `role="img"`(전체를 하나의
      이미지로 취급)이라 스크린리더가 자식 요소의 개별 `<title>`까지
      읽어준다는 보장이 없다 — 실제로 빠진 부분이었다. `ValueChart`에
      시각적으로 숨겨진(Tailwind `sr-only`) `<ul>`을 추가해서 각 매매 타점의
      날짜·매수/매도 건수·종목을 텍스트로 나열한다(`groupTradeMarkers`의
      결과를 그대로 매핑 — P1-6에서 이미 촘촘히 테스트된 로직이라 새로
      검증할 계산은 없다).
    - **전광망(`prefers-reduced-motion`)·모바일 오버플로는 이미 돼 있었다**:
      `globals.css`에 애니메이션 정지·중복 복사본 `display:none`·
      `overflow-x:auto` 전환이 전부 구현돼 있었다(이전 라운드 작업). 새로
      할 일은 없었고, 실제로 문제없이 동작하는지 재확인만 했다.
    - **수동 체크리스트(전부 통과)**: 375px·768px에서 대시보드·계좌·종목·
      배당·백테스트·자산맵·**새로 만든 `/status`·`/accounts/import`**까지
      전 페이지 가로 오버플로 0(`scrollWidth`/`clientWidth` 비교로 확인).
      라이트 테마에서 `/status`·`/accounts/import`의 배지·카드 색상 대비
      확인. 매매 타점 `sr-only` 목록이 실제 DOM에 생성되고(브라우저에서
      `getComputedStyle`로 시각적으로 숨겨진 것까지 확인) 내용이
      `groupTradeMarkers` 결과와 일치하는 것 확인.
    - `npm test` 199개(변경 없음, 이 항목은 새 로직보다 확인·보강 위주라
      기존 테스트가 회귀를 이미 커버한다), `npx tsc --noEmit`·`npx eslint .`
      통과.

##### P3 — 데이터 확장과 배포(사용자 요청이 있을 때)

12. **실제 장기 종가로 백테스트 교체** — B-4가 최우선 장기 기능이다. 현재
    `prices.json`은 가상 난수라 결과를 투자 판단에 사용하면 안 된다. 수정주가, 분할,
    배당, 거래일 정렬과 환율 기준을 먼저 정의한 뒤 데이터를 넣는다.
13. **ETF 구성종목 자동 갱신** — B-5. 발행사 원천 자료를 우선하고, 공시일과 합계 비중
    검증을 저장한다. 실패 시 이전 정상 파일을 유지한다.
14. **인증·DB·배포** — B-2/B-3. 사용자가 외부 접근을 다시 요청할 때만 시작한다.
    인증을 먼저 붙이고, 파일 쓰기를 DB 트랜잭션으로 옮긴 뒤 배포한다. 임시 터널로 실제
    자산 화면을 인증 없이 공유하지 않는다.

##### Claude가 임의로 바꾸면 안 되는 기준

- 원금은 `예탁자산-손익` 역산이 아니라 명시적인 현금 입금-출금이다.
- 현재 평가손익은 증권사 현재잔고 원가와 예상 매도수수료가 기준이다.
- 과거 원장이 불완전하므로 현재 보유수량·평단을 거래원장만으로 다시 덮어쓰지 않는다.
- 희소한 차트 데이터에 보간한 가짜 평가금액을 만들지 않는다.
- 개인 데이터와 검증 리포트는 커밋하지 않는다.
- 외부 푸시·배포·서비스 설치는 사용자 요청 또는 승인 없이 진행하지 않는다.

#### I. 2026-09-16 GPT 재검토 — Claude 작업 신호등 판정

재검토 기준은 커밋 `c73729e`(`origin/main`과 동일)이었다. 코드·테스트·빌드뿐 아니라
현재 운영 로그, `/status` 출력, Windows Task Scheduler 값을 서로 대조했다.

**계좌 원본 데이터 후속 감사**

- Claude 작업 시작 직전의 운영 백업 `2026-09-15T22-24-29-001Z`(로컬 07:24)와 현재
  `data/`를 SHA-256으로 비교했다. `accounts.json`, `position-basis.json`,
  `transactions.json`, `cashflows.json`, `dividends.json`, `symbols.json`은 전부 완전히 같다.
- 달라진 것은 자동 시세 갱신 대상인 `snapshots.json`, `quotes.json`, `fx-quote.json`뿐이다.
  따라서 메인 운영 폴더의 계좌 원본은 Claude가 덮어쓰지 않았다.
- 현재 운영 `data/` 전체와 백업 `2026-09-16T01-08-34-400Z`는 파일별 SHA-256이 전부
  일치한다. 이 폴더가 현재 운영본의 안전한 복원 기준 후보이다.
- 반면 현재 가장 최신인 `2026-09-16T01-14-58-657Z`는 Claude의 별도 worktree
  `.claude/worktrees/gpt-handover-work-332bcb`에 있던 5계좌 seed 데이터 백업이다.
  **최신 백업 자동 선택으로 이 폴더를 운영본에 복원하면 안 된다.** 삭제도 하지 말고
  우선 격리/표시한 뒤 사용자가 승인할 때만 정리한다.
- 현재 `http://localhost:3000/accounts`의 서버 렌더링 결과도 메인 운영본의 1계좌
  식별자와 일치하고, Claude worktree의 5계좌 식별자와는 일치하지 않는다. 즉 현재
  실행 중인 홈페이지도 seed 계좌를 읽고 있지 않다.

| Claude 작업 | 판정 | 재검토 결과 |
| --- | --- | --- |
| P0-1 가져오기 전후 자동 검증기 | 🟢 | 실제 데이터 검증과 오류 fixture가 모두 통과했다. |
| P0-2 원자적 JSON 쓰기·잠금 | 🟡→🟢(import만) | 파일 하나의 교체와 동시 writer 차단은 정상이다. **2026-09-16 후속**: 세 가져오기의 백업을 `withDataLock` 안으로 옮겨 "백업이 잠금 전에 실행된다"는 지적은 해소했다. cron의 quotes/fx/snapshots을 하나의 세대로 묶는 것은 아직 안 됐다(아래 I절 4번). |
| P0-3 백업 복원 리허설 | 🟡 | 리허설 기능은 정상이나 별도 Claude worktree의 seed 백업이 공용 폴더에서 최신 백업이 됐다. 기본 `최신` 선택을 운영 복원에 쓰면 위험하다. |
| P0-4 데이터 신선도 배지 | 🟢 | 시세·원금·잔고 기준일을 구분해 표시한다. |
| P1-5 희소 차트 고지 | 🟢 | 관측치가 부족할 때 대체 표시 사실을 사용자에게 알린다. |
| P1-6 매매 타점 그룹화 | 🟢 | 순수 함수와 자동 테스트로 고정됐다. |
| P1-7 증권사 CSV 손익 교차검증 | 🟢 | CSV 내부 값과 재계산 값 불일치를 적용 전에 차단한다. |
| P1-8 동적 렌더링 회귀 방지 | 🟢 | 데이터 의존 페이지의 `force-dynamic` 누락을 테스트로 감지한다. |
| P2-9 CSV 가져오기 화면 | 🟡→🟢 | 기본 미리보기·검증·백업·적용은 작동한다. **2026-09-16 후속**: 미리보기 후 cron/다른 import가 관련 파일을 바꾸면 이제 적용이 거부되고 다시 미리보기를 요구한다(I절 3번). 적용 실패 시 토큰이 먼저 소모돼 재시도가 어렵던 문제도 retryToken으로 해소했고, 파일 크기·확장자·행 수 한도도 추가했다(I절 5번). |
| P2-10 운영 상태 화면·로그 | 🔴 | 화면은 만들어졌지만 두 시각 계산이 틀렸다. 실제 서버 마지막 기동 10:45를 19:45로, 실제 다음 예약 14:00을 11:00으로 표시했다. Task Scheduler의 마지막 결과도 `1`이고 최근 자동 갱신은 실패 상태다. |
| P2-11 접근성·반응형 | 🟢 | 매매 타점 스크린리더 텍스트와 모바일/감속 모션 점검이 반영됐다. |
| B-0 서버 안정성 | 🟡 | 현재 HTTP 응답은 정상이나 최근 24시간 재시작 63회, 예약 작업 마지막 결과 `1`이라 안정화 완료로 볼 수 없다. |
| P3 장기 실제 종가·ETF 자동화·인증/DB/배포 | 🔴 | 아직 미진행이다. 사용자 요청 전 배포는 시작하지 않는다. |

**이번 재검토에서 다시 통과한 검증**

- `npm test`: 23개 파일, 199개 테스트 통과
- `npm run lint`, `npx tsc --noEmit`, `npm run build`: 모두 통과
- `node scripts/validate-data.mjs`: 현재 실제 데이터 통과
- `node scripts/verify-backup.mjs`: 구조·무결성 검사는 통과하지만 운영본/seed 구분은 못함
- 운영 백업 `2026-09-16T01-08-34-400Z`: 현재 `data/` 전체와 해시 일치
- HTTP: `/`, `/accounts/import`, `/status` 모두 200 응답

**다음 담당자가 보완할 순서**

1. **P0 — 운영 백업과 테스트 백업 분리** — ✅ 2026-09-16 완료 (Claude, 커밋 `ae24fcf`)
   - `lib/import/paths.ts`의 `backupRoot`가 워크트리(`.git`이 파일)에서는 운영 폴더
     (`~/cgportfolio-backups/`)가 아니라 로컬 `.dev-backups/`를 쓰도록 고쳤다. 재발 방지
     차원이며, 이미 섞여 들어간 `2026-09-16T01-14-58-657Z`는 격리 폴더
     (`~/cgportfolio-backups-quarantine/`)로 옮겼고 사용자 확인 전까지 삭제하지 않는다.
2. **P0 — `/status` 시각 오류 수정** — ✅ 2026-09-16 완료 (Claude, 커밋 `218264a`)
   - `server.log`가 시스템 로컬(KST) 시각인데 `Date.UTC`로 잘못 라벨링한 뒤
     `shortDateTime`이 다시 로컬 변환을 적용해 9시간이 이중으로 더해지던 버그를
     `lib/domain/log-status.ts`에서 고쳤다.
3. **P0 — staged import의 낙관적 동시성 검사 추가** — ✅ 2026-09-16 완료 (Claude)
   - **한 것**: `lib/import/data-version.ts`에 `hashDataFiles`/`diffDataFiles`를
     순수 함수로 만들었다(파일 내용 sha256 해시 비교 — mtime 해상도에 기대지 않는다).
     세 가져오기(`position-basis-actions.ts`/`account-history-actions.ts`/
     `normalize-ledger-actions.ts`)의 미리보기가 검증·병합에 쓰는 입력 파일들의
     baseline 해시를 staged 토큰에 같이 저장한다. 적용 단계는 검증·백업·쓰기를 전부
     `withDataLock` 안으로 옮기고, 그 잠금 안에서 baseline을 다시 찍어 하나라도 다르면
     "다시 미리보기해 주세요" 오류로 거부하고 아무 파일도 쓰지 않는다 — TOCTOU 창을
     없애려고 "확인"과 "쓰기"를 같은 잠금 구간에 뒀다. account history가 가장 중요한
     경우다: `snapshots.json`은 6시간마다 cron이 새 스냅샷을 추가하는데, 미리보기 때
     만든 병합 결과를 그 사이 값이 바뀐 줄 모르고 통째로 덮어쓰면 cron이 쌓은 스냅샷을
     잃는다 — 이제는 거부되고 cron이 쓴 값이 그대로 보존된다. 부수적으로 item 4의
     "import의 `backupData`도 `withDataLock` 안으로 옮겨 혼합 세대 백업을 막는다"도
     세 가져오기 전부에서 같이 해결됐다(cron의 quotes/fx/snapshots 묶음 트랜잭션은
     아직 별도로 남아 있다 — 아래 4번 참고).
   - **검증**: `lib/import/__tests__/data-version.test.ts` 5건(해시 동일/변경/누락
     마커, diff 빈 배열/변경분 추출). 세 가져오기 각각에 "미리보기 이후 관련 파일이
     외부에서 바뀌면 적용이 거부되고 그 외부 변경이 보존된다" 회귀 테스트 1건씩 추가
     (account-history는 cron이 새 스냅샷을 추가하는 시나리오로 재현). 기존 회귀
     테스트(우회 방어·재사용 방지·취소 시 보존)는 리팩터링 후에도 그대로 통과.
     `npm test` 207개(신규 8건 포함) 전부 통과, `npx tsc --noEmit`·`npx eslint .`
     통과, `npm run build`(seed 데이터로 20개 라우트 전부 생성) 통과. 실제
     race(두 요청이 정말 동시에 도는 것)까지는 재현하지 않았다 — 테스트는 "미리보기와
     적용 사이에 파일이 바뀌었을 때"를 순차적으로 재현한 것이고, 그게 이 항목이
     막으려던 TOCTOU 시나리오와 같다.
4. **P0 — 여러 파일을 하나의 논리 트랜잭션으로 교체** — ✅ 2026-09-16 완료 (Claude)
   - **한 것**: import 세 개의 `backupData`+쓰기는 3번에서 이미 한 잠금 구간
     안으로 옮겼다. cron 쪽은 `lib/data/generation-write.ts`에
     `readOriginals`/`writeGenerationOrRollback`을 순수 함수로 만들었다 —
     쓰기 전에 quotes/fx-quote/snapshots 세 파일의 원본 내용을 먼저 읽어 두고,
     새 내용을 전부 메모리에서 준비한 뒤(그래서 `recomputeTotal`도 디스크
     재읽기 대신 방금 계산한 quotes/fx 값을 인자로 직접 받도록 고쳤다) 순서대로
     원자적 교체한다. 그중 하나라도 실패하면 이미 쓴 파일들을 실패 직전
     원본으로 되돌리고(원래 없던 파일이면 삭제) 에러를 다시 던진다 —
     `app/api/cron/refresh/route.ts`가 이 두 함수를 호출하도록 리팩터링했다.
     개별 파일 자체가 일부만 쓰이는 것은 `writeJsonAtomic`이 이미 막고, 이번
     것은 그 위에서 "파일 여러 개 사이"의 세대 일관성을 보장한다.
   - **검증**: `lib/data/__tests__/generation-write.test.ts` 4건 — 전부 성공,
     **두 번째 파일 쓰기에 실패를 주입해도 첫 번째 파일이 원본으로 롤백되고
     세 번째는 시도조차 안 되는 것**(`node:fs`의 `renameSync`만 spy로 바꿔
     회차별 실패 주입 — `scripts/lib/__tests__/atomic-write.test.mjs`와 같은
     기법), **원본이 없던(이번에 처음 생기는) 파일이 롤백 대상이면 지우는
     것**, 새 파일 단독 성공 경로까지 확인. `npm test` 211개(신규 4건 포함)
     전부 통과, `npx tsc --noEmit`·`npx eslint .`·`npm run build`(seed
     데이터로 20개 라우트 전부 생성) 통과. **실제 HTTP로 `/api/cron/refresh`를
     불러보는 종단간 검증은 못 했다** — 이 워크트리에는 `.env.local`(시세
     API 키)이 없어서 실제로 부르면 시세를 한 건도 못 받아 `report.quotes.length
     === 0`으로 새 코드(세대 쓰기)에 도달하기 전에 조기 반환된다. 실 API
     키가 있는 메인 체크아웃에서 반드시 한 번 실제로 호출해 확인할 것.
5. **P1 — 가져오기 실패 복구와 입력 한도** — ✅ 2026-09-16 완료 (Claude)
   - **한 것(실패 복구)**: 세 가져오기의 `applyX` 전부에서 `withDataLock` 호출을
     `try/catch`로 감쌌다. 동시성 충돌·검증 실패처럼 "다시 시도해도 똑같이
     실패할" 경우는 그대로 `{ ok: false, errors }`만 돌려주지만(재업로드가
     맞는 경우), 백업·잠금·쓰기 자체가 실패하는 것처럼 "이 시점까지는 아무
     파일도 안 바뀐 게 보장되는" 예기치 못한 오류는 잡아서 `staged` 데이터를
     `stageImport`로 다시 스테이징하고 새 `retryToken`을 `ApplyResult`에
     실어 돌려준다 — 재업로드·재파싱 없이 같은 내용으로 한 번 더 적용을
     시도할 수 있다. `components/import/*.tsx` 세 개도 `retryToken`이 있으면
     미리보기 상태를 유지한 채 토큰만 갈아끼우고, 없으면(=재시도해도 소용
     없음) 미리보기를 지워 다시 업로드하도록 안내한다. 이전엔 실패해도
     이미 소모된 토큰을 화면이 계속 붙들고 있어서 재시도가 그대로 "미리보기를
     찾을 수 없습니다"로 또 실패했었다 — 이제는 그 경로가 없다. 겸사겸사
     `normalize-ledger-actions.ts`의 `transactions.json`+`cashflows.json`
     두 파일 쓰기도 P0-4에서 cron용으로 만든 `writeGenerationOrRollback`으로
     묶었다(전에는 두 파일을 각각 `writeJsonAtomic`으로 따로 써서, 두 번째가
     실패하면 첫 번째만 새 세대로 남는 같은 문제가 있었다).
   - **한 것(입력 한도)**: `lib/import/limits.ts`에 `assertFileWithinLimits`
     (확장자 `.csv` + 파일당 2MB)와 `assertRowCountWithinLimits`(파싱 후
     5000행)를 순수 함수로 만들어 `previewPositionBasis`/`previewAccountHistory`
     양쪽 미리보기 맨 앞에서 부른다 — 파싱을 시도하기도 전에 친절한 한국어
     오류로 막는다. `next.config.ts`에도 `experimental.serverActions.bodySizeLimit:
     "8mb"`를 명시했다 — 계좌수익률 CSV는 여러 파일을 한 번에 올릴 수 있어
     기본 1MB 한도에 여러 파일의 multipart 오버헤드가 겹치면 우리 코드가
     실행되기도 전에 프레임워크가 먼저(불친절하게) 거부할 수 있어서, 우리
     쪽 2MB/파일 검사보다 넉넉한 여유를 뒀다.
   - **검증**: `lib/import/__tests__/limits.test.ts` 6건(확장자·크기 상한·
     경계값·행 수). 세 가져오기 테스트에 각각 크기 초과/확장자 거부 케이스
     추가. **백업 실패 → retryToken → 재업로드 없이 재시도 성공**을
     position-basis에서(`backupData`를 감싸 한 번만 throw하도록 mock),
     **두 번째 파일 쓰기 실패 → 첫 번째 파일 롤백 → retryToken으로 재시도
     성공**을 normalize-ledger에서(`node:fs`의 `renameSync`만 spy로 바꿔
     회차별 실패 주입, `generation-write.test.ts`와 같은 기법) 각각
     end-to-end로 확인했다. `npm test` 223개(신규 12건 포함) 전부 통과,
     `tsc`·`eslint`·`npm run build`(`experimental.serverActions` 옵션이
     빌드 로그에 실제로 반영된 것까지 확인) 통과. **브라우저 실측**: 이
     워크트리를 대체 포트(3201, 운영 3000과 충돌 방지)로 직접 띄워
     `/accounts/import`에서 (1) 2MB 초과 CSV 업로드 → 화면에 정확히
     "보유종목 CSV 크기가 너무 큽니다 (2.0MB, 최대 2.0MB)..." 표시,
     (2) 정상 CSV로 미리보기(파싱 1종목·검증/교차검증 통과)→적용까지
     실제로 반영되어 "현재 잔고 기준 1종목을 반영했습니다" 확인. 테스트에
     쓴 파일 입력은 `DataTransfer`로 `File` 객체를 주입했다(OS 파일
     선택창은 이 환경에서 자동화 불가 — P2-9 때도 같은 한계가 있었다).
     테스트 중 실제로 반영된 이 워크트리의 `position-basis.json`(1종목
     테스트 데이터)은 확인 후 삭제해 원래(파일 없음) 상태로 복구했다 —
     seed 데이터가 아니라 이번 테스트가 만든 것이었다.
6. **P1 — 다음 자동 갱신 관찰**
   - 실제 다음 예약 실행 후 Task Scheduler 결과가 `0`인지, `refresh.log`에 새 성공 기록이
     생겼는지 확인한다. API 비용을 쓰는 수동 갱신은 사용자 요청 없이 실행하지 않는다.
7. **P3 — 장기 데이터·외부 배포**
   - 실제 장기 종가, ETF 구성 자동 갱신, 인증/DB/배포는 위 정확성 문제를 끝낸 뒤 별도 진행한다.

---

#### J. 2026-09-16 GPT 2차 재검토 — Claude 마무리 후 남은 보강

**이 절은 2026-09-16 당시 I절보다 최신이었던 기록이다. 현재 최종 판정은 12절이다.**
I절은 Claude가 무엇을 구현했고 당시 어떤
검증을 통과했는지 보여주는 작업 기록으로 남긴다. 다만 아래에서 다시 🟡/🔴로 판정한
항목은 I절에 `완료`라고 적혀 있어도 실제 다음 작업이 남아 있다. 상세 실행 순서는
`WORK_ORDER.md`의 **B-0A**를 따른다.

**재검토 기준 상태**

- 브랜치: `main`, `HEAD bb4545f`, 재검토 시 `main == origin/main`, 작업 트리 깨끗함
- 정적 검증: `npx tsc --noEmit`, `npx eslint .`, `npm test`, `npm run build` 모두 통과
- 테스트: 26개 파일, 223개 테스트 통과
- HTTP: `/`, `/accounts/import`, `/status` 모두 200
- 실제 시세 API를 쓰는 수동 갱신은 호출하지 않았다. 무료 API 비용과 분당 제한 때문에
  사용자가 요청하지 않은 수동 호출로 운영 관찰값을 흐리지 않는다.

**운영 대조 결과**

- 최신 코드로 서버가 기동된 뒤 2026-09-16 14:00(KST) 예약 갱신이 시작됐지만
  14:05에 `fetch failed`로 끝났다. 당시 `server.log`에는 같은 시각의 명시적 서버
  종료가 없었고 재검토 시 서버와 세 주요 페이지는 다시 정상 응답했다. 따라서 현재
  로그의 `서버에 연결하지 못했습니다`만으로 실제 서버 사망, 긴 요청 중 연결 종료,
  외부 공급자 지연을 구분할 수 없다.
- `/status`는 최근 7회 갱신 중 3회 실패와 최근 24시간 66회의 기동 기록을 표시했다.
  수동 재시작도 `server starting`으로 집계될 수 있으므로 66회를 모두 크래시라고 단정하면
  안 되지만, B-0 완료 조건(하루 0~1회 이하)과는 거리가 멀다.
- `tryRecoverServerTask()`는 Task Scheduler가 `Running`이면 서버가 살아 있다고
  간주한다. 그러나 이 프로젝트는 이미 작업 상태와 실제 포트/프로세스 상태가 어긋나는
  사례를 여러 번 겪었다. `Running`은 헬스체크가 아니다.

| 우선순위 | 판정(2026-09-16 재검토 시) | 판정(Claude 처리 후) | 발견 사항 | 완료 조건 |
| --- | --- | --- | --- | --- |
| P0 | 🔴 | 🟢(코드·테스트), 예약 실행 관찰은 미검증 | 자동 갱신 실패가 계속되고, 호출자·Twelve Data·네이버 요청에 명시적 타임아웃과 단계별 진단이 없다. 복구도 Task Scheduler 상태만 신뢰한다. | 공급자별 타임아웃·단계별 소요시간·실제 HTTP 헬스체크를 추가하고 테스트한다. 다음 **예약** 실행이 성공하는지 관찰한다. 수동 API 호출은 사용자 승인 없이는 하지 않는다. |
| P0 | 🟡 | 🟢 | 세 import 미리보기는 관련 파일을 읽어 결과를 만든 뒤 나중에 baseline 해시를 찍는다. 다른 프로세스가 그 사이 쓰면 오래된 결과에 새 baseline이 붙는 TOCTOU가 남는다. | 입력 파일 읽기→결과 계산→baseline 생성을 같은 잠금 스냅샷에서 수행하거나 전후 해시가 다르면 미리보기를 폐기한다. 중간 쓰기 주입 테스트를 추가한다. |
| P0 | 🟡 | 🟢 | cron은 `loadPortfolio()`/`getFxQuote()`를 잠금 전에 호출하고, 잠금 안의 `getSnapshots()`는 같은 요청의 React cache에서 이미 읽은 값을 받을 수 있다. 읽기와 세대 쓰기가 하나의 일관된 스냅샷이 아니다. | 외부 시세 조회는 잠금 밖에서 하되, 실제 데이터 읽기·총액 계산·세 파일 쓰기는 같은 잠금 안의 fresh snapshot으로 묶는다. 읽기와 잠금 사이 변경을 재현하는 테스트를 추가한다. |
| P1 | 🟡 | 🟢 | position-basis import baseline에 실제 교체 대상 `position-basis.json`이 없다. 두 미리보기를 만든 뒤 순서대로 적용하면 두 번째가 첫 번째를 조용히 덮어쓸 수 있다. | target 파일도 baseline에 포함하고 `preview A → preview B → apply A → apply B 거부` 회귀 테스트를 추가한다. |
| P1 | 🟡 | 🟢 | `writeGenerationOrRollback()`은 원본 읽기 오류를 모두 `파일 없음`으로 처리하고 롤백 실패도 삼킨다. 강제 종료·재부팅에는 JS `catch`가 실행되지 않는다. 그런데 import 오류 문구는 항상 `데이터는 바뀌지 않았습니다`라고 단정한다. | `ENOENT`만 없음으로 처리하고 나머지는 즉시 실패시킨다. 롤백 성공/실패를 typed error로 구분하며, 롤백이 확인된 경우에만 retryToken을 준다. 강제 종료까지 견디는 보장이 필요하면 journal/manifest 기반 복구를 별도 설계한다. |
| P2 | 🟡 | 🟢 | retryToken으로 한 번 실패한 뒤 성공하면 세 import UI가 이전 빨간 오류를 지우지 않아 실패와 성공이 동시에 보일 수 있다. | 적용 시작 또는 성공 분기에서 오류를 지우고 컴포넌트 테스트로 고정한다. |
| P2 | 🟡 | 🟢 | 파일당 2MB 제한은 있지만 다중 계좌 이력의 파일 개수·합산 크기 제한이 없다. 2MB 파일 4개는 multipart 오버헤드 때문에 앱 검증 전에 8MB 프레임워크 한도에서 거부될 수 있다. | `MAX_FILES`와 `MAX_TOTAL_BYTES`를 정해 8MB보다 충분히 낮게 맞추고 경계/초과 테스트 및 사용자 메시지를 추가한다. |
| 보안 | 🟡 | 🟡(사용자가 현상 유지를 명시적으로 선택 — WORK_ORDER B-0A-6) | 현재 `next start`는 실제로 `0.0.0.0:3000`에서 수신하며 import Server Action에는 인증이 없다. Windows 방화벽만 믿으면 같은 네트워크의 다른 사용자가 데이터 변경 화면에 접근할 가능성이 있다. | 완전 로컬 전용이면 사용자 확인 후 `127.0.0.1`로 바인딩한다. LAN 접근이 필요하면 인증을 먼저 설계한다. 사용자 선택 전에는 임의로 바꾸지 않는다. |

**Claude 처리 결과 (2026-09-16, 커밋 `8a58710`~`674f7d3`, 로컬 브랜치 `claude/work-1998ce`에만 있고 아직 main·origin에 병합/푸시/배포 안 함)**

- **B-0A-1(자동 갱신 진단)**: `lib/providers/twelve-data.ts`/`naver-kr.ts`의 모든
  `fetch`에 `AbortSignal.timeout(15s)` 추가. `scripts/lib/refresh-diagnostics.mjs`
  (신규, 순수 함수)의 `classifyFetchError`가 timeout/ECONNREFUSED/기타를 구분하고,
  `describeRecoveryDecision`이 Task Scheduler `Running` 상태만으로 정상이라고
  단정하지 않는다(`Running`+헬스체크 실패 → `report-only`, 자동 종료·강제 재시작
  확대 안 함). `scripts/refresh-quotes.mjs`가 실패 시 서버 홈(`/`)으로 짧은
  헬스체크를 먼저 해서 "서버 사망" vs "요청 자체 문제"를 구분해 보고한다.
  `lib/data/cron-log.ts`(신규)가 `~/cgportfolio-logs/cron-refresh.log`에 단계별
  소요시간을 남긴다(`npm start`의 stdout이 어디에도 안 남는다는 걸 발견해서 추가).
  테스트: `lib/providers/__tests__/{twelve-data,naver-kr}.test.ts`(fetch 모킹,
  timeout 응답이 매달리지 않고 다음 항목으로 넘어가는지), `scripts/lib/__tests__/
  refresh-diagnostics.test.mjs`(7건, 실제 API 없음). **예약 실행 관찰은
  미검증** — 다음 02/08/14/20시(KST) 예약 갱신이 실제로 성공하는지는 프로덕션에
  이 코드가 배포된 뒤에야 확인 가능하다.
- **B-0A-2(import TOCTOU)**: 세 `preview*` 함수의 "입력 읽기 → 계산 →
  baseline 해시"를 전부 `withDataLock` 안으로 묶었다(업로드 파일 decode/parse는
  느린 작업이라 밖에 둠). `position-basis-actions.ts`의 `BASELINE_FILES`에
  실제 교체 대상 `position-basis.json`을 추가. 회귀 테스트: "preview A → preview
  B(같은 baseline) → apply A 성공 → apply B 거부"를 그대로 재현해 통과.
- **B-0A-3(cron 스냅샷 일관성)**: `lib/data/store.ts`에 `readJsonUncached`
  (React cache 우회), `lib/data/views.ts`에 `loadRawUncached`/`makeFxLookup`
  export 추가. cron route가 외부 조회 뒤 잠금 안에서 `loadRawUncached()`로
  "지금" 값을 다시 읽고, 직접 구현했던 `recomputeTotal`(부분적 계산 중복)을
  `buildPortfolio()` 재사용으로 교체. `vitest.config.mts`에 `@/*` alias 해석
  추가(새 의존성 없음 — tsconfig.json에 이미 있던 매핑을 vitest에도 적용한
  것뿐. 전에는 `@/...`로 값을 import하는 파일이 전혀 테스트 불가능했다).
  회귀 테스트(`app/api/cron/refresh/__tests__/route.test.ts`): 모킹된
  `fetchAllQuotes`의 부수효과로 "외부 조회 중" transactions.json을 바꿔, 쓰인
  스냅샷이 캐시된 옛 값이 아니라 잠금 시점 최신 값을 반영하는지 확인 — 통과.
- **B-0A-4(rollback 정확성)**: `readOriginals()`가 `ENOENT`만 "없음"으로
  처리하고 나머지 읽기 오류는 그대로 던짐. `writeGenerationOrRollback()`이
  `GenerationWriteError`(원래 오류+롤백 오류 전부 보존)를 던지도록 변경.
  `normalize-ledger-actions.ts`/cron route가 `rollbackOk`를 보고 분기 —
  롤백이 확인된 경우에만 "데이터는 안 바뀜"+retryToken 제공. 테스트: 두 번째
  쓰기 실패 + 롤백 쓰기까지 실패를 함께 주입해 `rollbackOk: false`와 두 오류
  모두 보존되는지 확인.
- **B-0A-5(UI 상태·업로드 한도)**: 세 import 컴포넌트가 성공 분기에서
  `error`를 안 지우던 버그 수정(`lib/import/apply-ui-state.ts` 순수 함수로
  분리 — 이 저장소에 React 컴포넌트 테스트 도구가 없어서 로직만 분리해
  테스트). `lib/import/limits.ts`에 `assertFileCountAndTotalWithinLimits`
  (`MAX_FILES=3`, `MAX_TOTAL_BYTES=6MB`, `next.config.ts`의 `bodySizeLimit`
  8MB보다 낮게) 추가, 화면에도 한도 문구 표시.
- **B-0A-6(보안)**: 사용자에게 물어봄 — 휴대폰 등 다른 기기에서도 쓰고 싶다고
  했고, Claude가 인증 없이 열어두는 위험을 설명한 뒤에도 **"그냥 다 오픈된
  페이지여도 상관없다"**며 현재 상태(`0.0.0.0:3000`, 인증 없음) 유지를 명시적으로
  선택했다. **코드 변경 없음.**

**검증**: `npx tsc --noEmit`·`npx eslint .`·`npm test`(33개 파일, 259개
테스트)·`npm run build`(20개 라우트) 전부 순차 실행해 통과 확인. `git status
--short`·`git diff --check`·`git diff -- data` 전부 깨끗함(빈 출력). 실제
`data/*.json`은 이번 커밋 어디에도 없음(`git log <range> --stat --name-only
| grep ^data/` 결과 없음, B-0A-1 커밋 시점에 이미 확인). `/`·`/accounts/import`
`/status` 전부 200(단, 프로덕션 서버는 아직 이전 커밋 `bb4545f` 기준으로
떠 있어서 이번 B-0A 변경사항을 서빙하는 상태는 아니다 — 병합·배포는 별도
승인 필요).

**재발 방지 테스트로 반드시 추가할 것**

1. preview가 입력을 읽은 직후 외부 writer가 대상 파일을 바꾸면 preview 자체가 실패하거나
   apply가 거부되고 외부 변경이 보존된다.
2. position-basis 미리보기 두 개를 같은 baseline에서 만든 뒤 첫 적용만 성공한다.
3. generation의 두 번째 쓰기 실패뿐 아니라 **롤백 쓰기 자체 실패**도 주입해, 데이터가
   보존됐다고 거짓 안내하거나 retryToken을 발급하지 않는지 확인한다.
4. retryToken 재시도 성공 뒤 오류 문구는 사라지고 성공 문구만 남는다.
5. 다중 파일의 개수·합산 크기 경계와 Server Action 8MB보다 작은 애플리케이션 상한을
   검증한다.
6. 공급자 응답 지연/중단, cron 호출 연결 종료, `Running`이지만 HTTP 헬스체크가 실패하는
   경우를 실제 API 없이 mock으로 재현한다.

**Claude 보고 형식**

- 수정한 파일과 각 수정이 막는 구체적 사고 시나리오
- 신규 테스트 이름과 재현한 실패 조건
- `tsc`/ESLint/전체 테스트/프로덕션 빌드 결과
- 예약 갱신은 실제로 관찰했는지, 못 했다면 `미검증`이라고 명시
- 서버 재시작·Task Scheduler·포트 바인딩·인증을 바꿨다면 사용자 승인 범위와 실제 적용 상태
- 실제 계좌 금액·수량·평단·API 키는 문서, 커밋 메시지, 로그에 쓰지 않는다

---

## 2. 이 프로젝트가 뭔지

qld.kr(기승전QLD)과 같은 종류의 개인 자산 대시보드를 사용자 소유로 새로 만든 것이다.
화면 구성과 기능 범위를 참고했고, **코드·CSS·이미지·글은 전혀 가져오지 않았다.**
원본은 Laravel + Blade 서버렌더라 애초에 소스가 공개돼 있지도 않다. 저장소 전체에
그 사이트에 대한 참조가 한 건도 없다(`git grep` 으로 확인).

### 스택을 Next.js로 고른 이유

사용자가 처음엔 원본과 같은 Laravel을 택했다가 되돌렸다. PC에 PHP·Composer가 없고,
PHP 호스팅에 월 5천~8천원이 드는 반면 Next.js는 Vercel 무료로 가능하다는 점 때문이다.
**이 논의는 이미 끝났다. 다시 꺼내지 말 것.**

---

## 3. 아키텍처 — 3가지 원칙

### 원칙 1. 과거 이력과 현재 잔고의 책임을 분리한다

매매·입출금·배당 이력은 원장에서 파생한다. 다만 제공된 과거 이력이 계좌 개설 시점부터
전부가 아니므로, 현재 보유수량·평균단가·매입원가는 증권사 잔고 CSV를 변환한
`position-basis.json`을 최종 기준으로 쓴다. 평가손익은 이 현재 원가와 최신 시세,
증권사 예상 매도수수료율로 계산한다.

`position-basis.json`은 개인 금융 데이터라 gitignore 대상이다. 화면용 계산 결과를 임의로
캐시하지 말고, 새 잔고 CSV를 받으면 가져오기 스크립트로 기준값을 명시적으로 갱신한다.

### 원칙 2. 시세는 cron이 적재하고, 화면은 저장된 값만 읽는다

```
[cron] → /api/cron/refresh → 시세 공급자 → data/quotes.json
                                               ↓
                           페이지 요청 → 저장된 값만 읽어 서버 렌더
```

페이지를 열 때 외부 API를 부르지 않는다. 방문 수가 늘어도 API 호출 수는 안 늘어서
무료 플랜 한도 안에서 돌아간다. 스냅샷이 한 점씩 쌓이므로 추이 차트와 MDD가 공짜로 나온다.

**절대 하지 말 것**: 페이지나 컴포넌트에서 직접 시세 API를 호출하는 것. 같은 실수로
기존 `web-stock` 프로젝트가 Twelve Data 하루 한도(800회)를 넘겼던 전례가 있다.

### 원칙 3. 시세 공급자는 교체 가능하다

`lib/providers/types.ts`의 `QuoteProvider` 인터페이스만 맞추면 어떤 공급자든 꽂힌다.
공급자를 추가·교체할 때 건드리는 파일은 `lib/providers/index.ts` 하나뿐이다.

---

## 4. 파일 지도

### 계산 (`lib/domain/`) — 이 프로젝트의 핵심

| 파일 | 줄 | 역할 |
|---|---|---|
| `portfolio.ts` | 371 | 이동평균법 포지션, 실현손익, 예수금, 원화환산, 비중, 계좌 집계 |
| `backtest.ts` | 303 | 일별 시뮬레이션. 적립·인출·리밸런싱·수수료·차익과세 |
| `metrics.ts` | 164 | 기간 필터, 최고/최저, MDD, 구간 수익률, 다운샘플링 |
| `dividends.ts` | 147 | 월별·연도별·종목별 집계, 연간 예상 |
| `types.ts` | 125 | 도메인 타입 정의 |
| `lookthrough.ts` | 103 | ETF를 구성종목으로 펼쳐 합산 |
| `treemap.ts` | 78 | squarified 타일 배치 |

### 데이터 (`lib/data/`)

- `store.ts` — 읽기 전용 저장소. **DB로 옮길 때 고칠 곳이 여기다.** 함수 본문만 쿼리로 바꾸면 된다
- `views.ts` — 페이지용 뷰모델 조립. `react cache`로 요청당 1회만 계산

### 시세 (`lib/providers/`)

- `types.ts` — `QuoteProvider` / `FxProvider` 인터페이스, `ProviderShapeError`
- `twelve-data.ts` — 미국 종목 + USD/KRW
- `naver-kr.ts` — 국내 ETF (비공식 경로)
- `index.ts` — 시장별 공급자 라우팅

### 화면 (`app/`)

`/` 대시보드 · `/asset-map` · `/accounts` · `/symbols` · `/symbols/[id]` · `/dividends`
· `/backtests` · `/backtests/[id]` · `/calendar` · `/philosophy` · `/reports` · `/reports/[slug]`
· `/api/cron/refresh`

### 컴포넌트 (`components/`)

차트는 **전부 SVG 직접 구현**이다. 차트 라이브러리를 쓰지 않는다.
상호작용이 필요한 `value-chart.tsx`만 클라이언트 컴포넌트고 나머지는 서버 렌더.

### 테스트 (`lib/domain/__tests__/`)

Vitest. 5개 파일 57개 테스트. `npm test`로 실행.
`portfolio` / `backtest` / `metrics` / `dividends` / `lookthrough` 각각에 대응하며,
8절의 과거 버그들이 경계 사례 테스트로 고정돼 있다. **계산 로직을 고치면 여기도 같이 고칠 것.**

### 운영 스크립트 (`scripts/`)

| 파일 | 역할 | 실행 |
|---|---|---|
| `seed.mjs` | 가상 데이터 한 벌 생성 (처음 클론했을 때) | `node scripts/seed.mjs` |
| `import-holdings-csv.mjs` | 증권사 "보유종목" CP949 CSV → 부트스트랩 원장 변환 | `node scripts/import-holdings-csv.mjs <csv> --dry-run` |
| `refresh-quotes.mjs` | `/api/cron/refresh` 호출 + 로그 기록 (스케줄러가 6시간마다 실행) | `node scripts/refresh-quotes.mjs` |
| `backup-data.mjs` | `data/` → `~/cgportfolio-backups/` 복사, 최근 30개 보관 | `npm run backup` |
| `start-server.cmd` | `npm start` 배치 래퍼 (스케줄러가 호출) | 스케줄러 전용 |

**⚠ `seed.mjs`를 실수로 돌리지 말 것.** 지금 `data/`에는 실제 데이터가 들어 있다.
이 스크립트는 `accounts.json`/`symbols.json`이 이미 있으면 덮어쓰지 않지만,
`prices.json`·`transactions.json`·`snapshots.json` 등은 **가상 값으로 덮어쓴다.**

---

## 5. 검증된 것 / 검증되지 않은 것

**이 절이 이 문서에서 제일 중요하다.**

### 검증됨

| 항목 | 방법 |
|---|---|
| 타입체크 | `npx tsc --noEmit` 통과 |
| 린트 | `npx eslint .` 통과 (경고 0) |
| 프로덕션 빌드 | 20개 라우트 전부 생성 |
| 화면 렌더링 | 브라우저에서 전 페이지 육안 확인 |
| 모바일 반응형 | 375px에서 가로 오버플로 0 (DOM 측정) |
| 라이트/다크 테마 | 전환 동작 확인 |
| 새 클론 실행 | 실제로 clone → npm install → seed → build 완주 |
| 계산 정확성 | 비중 합계 100%, 예수금 양수, 백테스트 결과 상호 정합성 **육안 검토 수준** |
| **계산 엔진 단위 테스트** | Vitest 도입. `lib/domain/` 5개 파일(portfolio/backtest/metrics/dividends/lookthrough) 57개 테스트, `npm test` 전부 통과. 이체·액면분할·현재 잔고 기준값·수수료 포함 평가손익·희소한 차트 기간 대체도 회귀 테스트로 고정 |
| **Twelve Data 실호출** | 2026-09-15 `tsx`로 `lib/providers/twelve-data.ts` 실제 실행. QLD/TQQQ/SCHD 단일·복수 조회, USD/KRW 환율 전부 성공. 응답 형태가 코드 가정과 정확히 일치했다 (단일 심볼은 객체, 복수는 심볼 키 객체 — 가정대로) |
| **네이버 국내 시세 크롤링** | 2026-09-15 `lib/providers/naver-kr.ts` 실제 실행. 418660/0015B0/490590/491620 4종목 전부 성공. `closePrice`/`compareToPreviousClosePrice` 필드명 가정이 맞았고, 하락 종목(490590)에서 `compareToPreviousClosePrice`가 음수로 오는 것도 확인(부호 포함). 버그 1건 발견·수정: `marketState`를 항상 `"closed"`로 하드코딩하고 있었는데, 실제 응답의 `marketStatus` 필드(`"OPEN"`/그 외)를 반영하도록 고침 |
| **공급자 실패 처리** | 존재하지 않는 심볼을 섞어 호출 → 해당 심볼만 `ProviderShapeError`로 건너뛰고 나머지는 정상 반환되는 것을 Twelve Data·네이버 양쪽에서 확인 (`console.warn`에 원인 메시지 출력) |
| **`/api/cron/refresh` 실행** | 2026-09-15 `npm run dev` + `curl`로 실제 호출. 7종목 전부 갱신, `data/quotes.json`·`fx-quote.json`·`snapshots.json`이 실제로 갱신되고 대시보드 HTML에 새 총액이 그대로 반영되는 것까지 확인. `recomputeTotal()`이 계산한 총액이 `loadPortfolio()`(정식 계산 경로)가 페이지에 렌더한 총액과 정확히 일치함을 확인 — 두 계산 경로가 어긋나지 않는다 |
| **부분 실패 시 기존 값 유지** | Twelve Data 키를 일부러 무효화해 US 3종목만 실패시켜 봄. 실패한 종목은 `asOf`가 그대로였고(값이 안 바뀜), 성공한 KR 4종목만 갱신됨. `fx-quote.json`도 환율 조회 실패 시 안 덮어써짐. 빈 값으로 덮어쓰는 사고는 없었다 |
| **`CRON_SECRET` 인증** | 설정 후 무인증/오답 헤더는 401, 올바른 `Authorization: Bearer <secret>`만 200. 실제로 헤더를 바꿔가며 확인 |
| **실제 거래원장·현재 잔고 연결** | 증권사 거래원장을 매매·입출금·배당 이력으로 복원하고, 불완전한 기간 때문에 생기던 현재 수량·원가 오차는 증권사 잔고 CSV를 별도 기준값으로 적용해 제거. 원본 평가손익과 수수료 포함 재계산이 1원 단위까지 일치함을 확인. 실제 값은 Public 저장소 문서에 적지 않는다 |
| **Twelve Data 대량 종목 처리** | 실제 28종목으로 cron을 돌려보다 발견: 무료 플랜은 분당 8크레딧인데 심볼 하나당 1크레딧이라, 8개 넘는 심볼을 한 URL에 묶어도 429로 전부 실패한다. `twelve-data.ts`를 8개씩 분당 한 묶음으로 보내도록 고쳐 30개 심볼 전부 성공(약 3.1분 소요)하는 것까지 실측 확인 |
| **백업 자동화** | `scripts/backup-data.mjs` 작성 후 Windows 작업 스케줄러에 "CGPORTFOLIO 데이터 백업" 이름으로 등록 완료 (매일 새벽 3시, `C:\Users\ACC-002\cgportfolio-backups\`, 최근 30개 보관). `Start-ScheduledTask`로 수동 트리거해 `LastTaskResult: 0`(성공)과 실제 백업 폴더 생성까지 확인 |
| **작업 폴더를 메인 체크아웃으로 합침** | 이 작업은 원래 `.claude/worktrees/continue-previous-work-f7f06a`라는 워크트리에서 했다. 실제 데이터가 워크트리 삭제 시 같이 사라지는 걸 막기 위해 브랜치를 `C:\Users\ACC-002\Desktop\cgportfolio`(메인 체크아웃, `main` 브랜치)에 fast-forward 병합하고, gitignore된 실제 데이터 파일 8종(`accounts.json` 등)과 `.env.local`도 그대로 옮겼다. 메인에서 `npm install` 후 `tsc`/`eslint`/`test`/`build` 전부 통과 |
| **워크트리 중첩 버그** | 위 병합 과정에서 발견: 워크트리가 저장소 안에 있다 보니 메인에서 `eslint .`/`vitest run`을 돌리면 워크트리의 `.next` 빌드 산출물·테스트 파일까지 같이 스캔돼 eslint 683개 오류, vitest 테스트 2배 중복 실행이 났다. `eslint.config.mjs`/`vitest.config.mts`에 `**/.claude/worktrees/**` 제외 규칙을 추가해 고침 |
| **시세 자동 갱신 스케줄링 (P2-1)** | `scripts/refresh-quotes.mjs` + `scripts/start-server.cmd` 작성, Windows 작업 스케줄러에 "CGPORTFOLIO 서버"(매일 새벽 4시 기동, 실패 시 3회 재시도)·"CGPORTFOLIO 시세 갱신"(6시간마다 `/api/cron/refresh` 호출) 등록. 수동 트리거로 30종목 갱신 성공(30/0/0) 2회 확인, 로그는 `~/cgportfolio-logs/refresh.log` |
| **무인 자동 실행 실증** | 위 스케줄이 **사람이 개입하지 않은 상태에서 실제로 동작하는 것까지 확인했다.** 2026-09-15 20:00 정각 자동 실행 → `성공: 갱신 30건, 누락 0건, 오류 0건`(평가금액은 로그 파일에만 남고 이 문서에는 적지 않는다 — 저장소가 Public이다). 같은 시각 서버도 5시간 이상 무중단 유지됨. `data/snapshots.json`에 시계열 점이 실제로 누적되는 것도 확인 (P2-1의 완료 조건 "사람이 개입하지 않아도 스냅샷이 쌓인다" 충족) |

### 검증되지 않음 — 반드시 확인할 것

| 항목 | 상태 |
|---|---|
| **Twelve Data/네이버 값의 육안 대조** | 코드 실행 결과는 확인했으나, 이 환경의 브라우저 도구가 `finance.naver.com` 접근을 차단해 시세 앱과의 육안 대조는 못 했다. 네이버 시세는 그 사이트가 쓰는 실시간 폴링 API를 직접 부른 값이라 원천은 같다 |
| **배포** | 한 번도 안 해봤다 |
| **서버 상시구동 안정성** | 약 36시간 안에 서버가 **최소 2번** 원인 불명으로 죽었다 (2026-09-15 저녁 1회, 2026-09-16 새벽 02:03~03:46 사이 1회). Task Scheduler 이벤트 로그가 이 환경에서 비활성이라(`wevtutil` 조회 결과 없음) 원인은 끝내 못 찾았다. **로그온 트리거(`ONLOGON`)는 이 환경에서 `Access is denied`로 등록 자체가 안 돼서**, 대안으로 "매일 새벽 4시 기동" DAILY 트리거를 쓴다. 2026-09-16에 `scripts/start-server.cmd`에 무한 재시작 루프를 추가했다(증상 완화, 근본 원인은 여전히 미상). 적용 중 이 cmd 파일에 한글 로그 메시지(특히 em dash)를 넣었다가 `cmd.exe`가 잘못 해석해 스크립트 자체가 죽는 걸 겪었다 — `ECHO`로 쓰는 텍스트는 영문 ASCII만 쓸 것. **2026-09-16 오전 후속 조사**: Task Scheduler 로그 대신 **System 이벤트 로그**(`Get-WinEvent -LogName System`, Id 6005/6006/109 등)를 보니 02:45:44~02:47:00에 `TrustedInstaller.exe`의 "OS: Upgrade(계획됨)" 자동 재부팅이 있었고 `LastBootUpTime`도 일치 — 2차 사망의 원인은 **Windows Update 자동 재부팅**으로 특정됐다. 같은 패턴이 08-24/08-25(2회)/08-28/09-09(2회)에도 있어 주기적으로 재발할 것으로 예상. 다만 04:12~05:46 사이 8회의 초단기 반복 재시작(각 1초 미만 생존)은 이 재부팅과 무관하고 여전히 원인 불명(System/Application 로그 모두 공백). Windows Update 재부팅 설정 변경은 시스템 설정이라 사용자 요청 없이는 건드리지 않음. 대신 `scripts/refresh-quotes.mjs`에 연결 실패 시 서버 작업을 감지·재기동하는 로직을 추가해, OS 재부팅으로 재시작 루프까지 죽어도 다음날 4시가 아니라 최대 6시간 안에 복구되게 함(`tryRecoverServerTask`, 문법 확인·정상 상태 감지 경로만 검증, 실제 다운 상태에서의 기동 경로는 미검증). 상세는 `WORK_ORDER.md` B-0 참고 |
| **포트 3200의 정체 불명 dev 서버** | 작업 중 발견. 같은 프로젝트를 `next dev --port 3200`으로 서빙하는 프로세스가 돌고 있는데 이전 담당자가 띄운 것이 아니다. 사용자나 다른 세션의 작업일 수 있어 건드리지 않았다. 두 서버가 같은 `data/`를 공유하므로 3000과 3200 화면이 다르면 이것 때문이다 |

### 실거래 데이터 상태 (2026-09-16 갱신)

증권사 거래원장은 매매·입출금·배당 이력으로 사용한다. 현재 잔고는 원장의 제한된
조회 기간 때문에 어긋날 수 있으므로 `position-basis.json`의 증권사 잔고 스냅샷을
최종 기준으로 덮어쓴다. 이 방식으로 현재 전 종목의 보유수량·평균단가·매입금액을
원본과 맞췄다.

**검증한 것**:
- 잔고 CSV의 평가손익 합계와 `평가금액 - 매입금액 - 예상 수수료` 재계산 결과가
  1원 단위까지 일치한다
- 주식 이체와 액면분할은 일반 매매가 아니므로 현금 및 실현손익 계산에서 제외한다
- 정규화 전후 예수금 차이는 통화 최소 표시 단위보다 작다
- 계좌수익률 CSV의 일별 실제 평가금액과 `입금-출금` 누적 현금원금을 차트
  스냅샷으로 가져왔다. `예탁자산-누적손익`은 비현금 자산이동까지 원금으로 잡혀
  사용하지 않는다
- 기본 차트 기간은 1년이고, `매매` 토글로 초록 매수·빨강 매도 점선을 표시한다

**주의할 것**:
- 과거 원장이 계좌 전체 기간을 담지 않으므로 누적 실현손익은 참고값이다. 화면의 주
  손익 지표는 현재 평가손익이다
- `position-basis.json`과 `snapshots.json`은 민감 데이터이며 커밋하면 안 된다
- 새 잔고나 계좌수익률 CSV를 받으면 아래 가져오기 스크립트로 교체한다

**가져오기 도구**:
- `scripts/import-position-basis-csv.mjs` — 현재 보유수량·평균단가·매입원가·예상
  매도수수료율
- `scripts/import-account-history-csv.mjs` — 실제 일별 평가금액·현금 입출금 누적 원금
- `scripts/normalize-ledger.mjs` — 이체·액면분할·현금흐름 성격 정규화

**`prices.json`은 여전히 가상 데이터다.** `scripts/seed.mjs`가 만든 난수 시계열이라
백테스트 숫자는 **의미가 없다**. 실데이터로 바꾸는 건 `WORK_ORDER.md` B-4 참고.

**환경 관련 발견**: 이 작업 환경에서 Browser 도구(`preview_start`)가 띄운 `next dev`
프로세스는 외부 API(Twelve Data·네이버)로 나가는 `fetch`가 전부 `fetch failed`로
실패했다. 반면 터미널(Bash 도구)에서 직접 `npm run dev`로 띄운 프로세스는 같은
코드로 정상 호출됐다. Browser 도구가 띄우는 프로세스에 별도의 아웃바운드 네트워크
제약이 있는 것으로 보인다 — 실제 VPS나 로컬 상시구동 환경에서는 해당하지 않을
가능성이 높지만, 다음에 이 환경에서 cron을 다시 검증할 때는 dev 서버를 Browser
도구가 아니라 터미널에서 직접 띄울 것.

시세 공급자 코드는 응답 구조가 다르면 `ProviderShapeError`로 **즉시 터지게** 만들어
뒀다. 조용히 틀린 값을 쓰는 것보다 낫기 때문이다. 실제로 돌려본 결과 이 설계가
의도대로 동작함을 확인했다 (잘못된 심볼만 걸러지고 나머지는 살아남는다).

---

## 6. 알려진 제약

1. **서버리스 배포 불가 (현재 형태로는)** — `/api/cron/refresh`가 `data/*.json`에 파일을
   쓴다. Vercel 같은 읽기전용 파일시스템에서는 동작하지 않는다. 해결하려면 저장 대상을
   DB로 바꿔야 하고, 고칠 곳은 `lib/data/store.ts`(읽기)와 그 라우트(쓰기) 둘뿐이다.
   VPS나 로컬 상시 구동이면 지금 그대로 된다.
2. **국내 시세는 비공식 경로** — 상대 사이트 구조가 바뀌면 깨진다. 상용으로 쓰려면
   증권사 Open API로 갈아타야 한다. 그때 `naver-kr.ts`만 새로 쓰면 된다.
3. **ETF 구성종목은 수동 갱신** — `data/lookthrough.json`. 자동 수집 소스를 안 붙였다.
4. **로그인 코드는 구현됐으나 아직 운영 활성화 전** — `npm run auth:setup`으로 계정을
   만든 뒤 새 빌드·서버 재시작이 필요하다. 외부 공개는 HTTPS 구성 뒤에만 한다.
5. **백테스트 구간이 2.7년** — `prices.json`에 2024-01-02 이후만 있다. 더 긴 백테스트를
   하려면 과거 종가를 더 넣어야 한다.
6. **개인 데이터는 git이 지켜주지 않는다** — `.gitignore`로 뺐으므로 **별도 백업 필수.**

---

## 7. 데이터 정책 — 지켜야 할 선

저장소는 **Public**이다. 개인 금융 데이터는 `.gitignore`로 빼 두었다.

| 저장소에 있음 | 저장소에 없음 (로컬 전용) |
|---|---|
| `lookthrough.json` (공개 ETF 구성) | `accounts.json`, `symbols.json` |
| `backtests.json` (시나리오 정의) | `transactions.json`, `cashflows.json`, `dividends.json` |
| `philosophy.md`, `reports/` (직접 쓴 글) | `snapshots.json`, `quotes.json`, `prices.json`, `fx*.json` |
| `sample/` (실행용 가상 데이터) | `calendar.json` |

**절대 하지 말 것**: 위 오른쪽 파일들을 `git add` 하는 것. 한 번 커밋하면 이력에 영구히
남아 지워도 이력·포크·캐시에 남는다. `.gitignore`의 해당 블록을 지우거나 `-f`로 강제
추가하지 말 것.

지금까지 커밋된 데이터는 전부 가상 값이라 실제 금액이 이력에 남은 적은 없다.
2026-09-15부터 `data/`에 실제 금융 정보(미국주식 위탁계좌)가 들어 있다 — 커밋·
스크린샷·로그에 노출되지 않도록 각별히 주의할 것 (`WORK_ORDER.md` P1-2 참고).

---

## 8. 이미 잡은 버그 — 다시 만들지 말 것

작업 중 실제로 발생해서 고친 것들이다. 같은 실수를 반복하기 쉬운 지점이라 남긴다.

| 버그 | 원인 | 교훈 |
|---|---|---|
| cron이 파일을 갱신해도 화면이 낡은 값 표시 | `store.ts`가 모듈 수준 `Map`에 캐시 | 요청 경계에서 비워지는 `react cache`를 쓸 것. 모듈 캐시는 프로세스가 사는 동안 영원히 남는다 |
| 예수금 -1.2억, 비중 합계 170% | 시드가 초기 매수에 대응하는 입금을 안 만듦 | 매수 전에 입금이 있어야 한다 |
| 모바일 가로 스크롤 | `grid`에 기본 컬럼 미지정 → 암시적 컬럼이 `max-content` | 반응형 그리드엔 항상 `grid-cols-1`을 기본으로 둘 것 |
| 모바일 가로 스크롤 (2차) | `opacity-0` 라벨이 레이아웃 폭을 차지 | 숨길 땐 `absolute`로 흐름에서 빼거나 `hidden` |
| 백테스트가 첫 달부터 "자금 소진" | 수수료 때문에 실수령이 목표보다 몇백 원 모자란 것을 소진으로 오판 | "못 만든 것"과 "조금 모자란 것"을 구분할 것 |
| 인출 시나리오 결과가 과도하게 나쁨 | 매도대금 **전체**에 과세 | 원금 회수분은 비과세. 차익에만 매길 것 |
| 리밸런싱 후 세금 과소 계산 | 전량매도·재매수로 매입원가가 현재가로 리셋 | 리밸런싱은 원가를 이어받아야 한다 |
| 적립식에서 "연평균 0.00%" | 초기 일시금 0이면 CAGR 자체가 성립 안 함 | `cagrApplicable`로 판별해 순증률로 대체 |
| 네이버 `marketState`가 항상 "closed" | 실제 응답의 `marketStatus` 필드를 안 쓰고 하드코딩 | 실거래·실호출 전에는 이런 자리도 의심할 것 |
| 실제 28종목으로 cron 돌리자 전부 429 | Twelve Data 무료 플랜은 분당 8크레딧인데 심볼 1개=1크레딧. 여러 심볼을 한 URL에 묶어도 크레딧은 심볼 수만큼 그대로 듦 | 테스트할 땐 항상 소수 종목만 썼던 게 이 버그를 가렸다. 실제 규모(보유 종목 전체)로 반드시 한 번은 돌려볼 것. 8개씩 분당 나눠 보내도록 고침 |
| 실거래 이력 복원 후 예수금이 크게 틀어짐 | `buildCashBalances()`가 `symbols.json`에 없는 종목의 거래는 현금 영향을 조용히 건너뜀. 과거에 샀다 판(지금은 청산한) 종목 33개가 등록이 안 돼 있었다 | 보유수량은 `buildPositions()`가 심볼 등록 여부와 무관하게 계산해서 맞아 보였지만, 예수금은 틀렸다. 거래 이력을 다루기 전에 **거래된 전 종목**(청산분 포함)이 symbols.json에 있는지 먼저 확인할 것 |
| 같은 초에 매도 후 1초 뒤 매수하는 거래에서 보유수량이 어긋남 | `buildPositions()`가 `at` 문자열로 재정렬하는데, 로봇 리밸런싱이 매도를 매수보다 먼저 기록해 일시적으로 마이너스 보유량이 되고 `Math.min` 클램프에 걸려 수량이 사라짐 | 배열 순서를 바꿔도 소용없다 — `buildPositions()`가 내부에서 `at` 값 자체로 다시 정렬한다. 같은 날 매수/매도가 섞인 거래는 매수의 `at`을 그날 00:00:00으로 당겨야 한다 |
| 주식 이체를 매수로 처리해 평가손익·현금이 왜곡됨 | 계좌로 입고된 주식에 증권사 원가가 있어도 실제 현금 매수는 아니다 | `action: "transfer"`로 매매와 분리하고, 현재 원가는 증권사 잔고 기준값을 사용한다. 이체는 예수금과 실현손익을 바꾸지 않는다 |

---

## 9. 검증 명령

```bash
npx tsc --noEmit
```

```bash
npx eslint .
```

```bash
npm run build
```

```bash
npm test
```

```bash
node scripts/seed.mjs
```

개발 서버는 `npm run dev`. 데이터가 없으면 화면이 오류를 내는데, 그때는
`node scripts/seed.mjs`를 먼저 돌리라는 안내가 나온다.

---

## 10. 작업 환경 메모

### 기본

- Windows 11, PowerShell / Git Bash 병행
- Node v24, npm. `node`는 `C:\Program Files\nodejs\node.exe`
- GitHub CLI(`gh`) 2.100.0 **설치돼 있으나 로그인 안 됨.** `gh auth login` 필요
- git 신원은 저장소 단위 설정 (`changgyun2631`). 전역 설정은 비어 있음
- 기존 관련 프로젝트: `C:\Users\ACC-002\Desktop\web-stock` (별개 프로젝트, 건드리지 말 것.
  단 `.claude/launch.json`에 이 프로젝트 프리뷰 구성 한 줄이 추가돼 있음)

### 실제로 밟은 지뢰들 (`WORK_ORDER.md` 0-4절과 같은 내용)

1. **Avast 안티바이러스가 TLS를 가로챈다.** 외부 HTTPS를 `curl`로 부르면 인증서 폐기
   검사에서 실패한다 (`CRYPT_E_NO_REVOCATION_CHECK`). `curl --ssl-no-revoke`를 쓸 것.
   Node의 `fetch`(undici)는 영향 없다.
2. **Git Bash에서 `node`에 `/tmp/x` 같은 경로를 넘기면 `C:\tmp\x`로 해석된다.**
   Node 스크립트 인자에는 Windows 절대경로를 넘길 것.
3. **Git Bash에서 `taskkill /F /PID`는 `taskkill //F //PID`로 써야 한다.**
4. **Windows 작업 스케줄러에서 `ONLOGON` 트리거는 `Access is denied`로 거부된다.**
   시간 기반(`DAILY`, `ONCE`+`RepetitionInterval`)은 정상 등록된다.
5. **`Register-ScheduledTask`에 `[TimeSpan]::MaxValue`를 주면 XML 범위 오류**가 난다.
   유한값(`New-TimeSpan -Days 3650`)을 쓸 것.
6. **Task Scheduler 이벤트 로그가 비활성이다.** 작업이 왜 죽었는지 사후 추적이 안 된다.
   `LastTaskResult` 코드로만 추정해야 한다 (`267009`=실행 중, `0`=성공,
   `0xC000013A`=Ctrl+C류 종료, `2147946720`=이미 실행 중이라 새 인스턴스 거부).
7. **에이전트 Browser 도구로 띄운 dev 서버는 외부 API fetch가 전부 막힌다.** 터미널에서
   직접 `npm run dev`로 띄우면 정상. cron을 검증할 때는 반드시 터미널에서 띄울 것.
8. **에이전트 Browser 도구는 `finance.naver.com` 접근이 정책상 차단된다.**
9. **`.cmd` 배치 파일에 한글 `ECHO` 텍스트(특히 em dash 같은 특수문자)를 넣으면
   `cmd.exe`가 잘못 해석해 스크립트가 즉시 죽을 수 있다.** `REM` 주석의 짧은 한글은
   괜찮지만 `ECHO`로 파일에 쓰는 텍스트는 영문 ASCII만 쓸 것. 고치면 Task Scheduler를
   거치지 않고 `cmd.exe /c "경로\파일.cmd"`로 먼저 직접 실행해 검증할 것.
10. **`taskkill`로 자식 프로세스만 죽이면 Task Scheduler가 그 작업을 계속 "Running"으로
    착각해 새 실행을 거부한다.** `Stop-ScheduledTask`로 먼저 정리할 것.

---

다음에 할 일은 **`WORK_ORDER.md`** 참고. 처음 인계받았다면 그 문서 0절부터 읽을 것.

---

## 11. 2026-09-17 검수 후 보강 작업명령

### 목표와 우선순위

Claude가 기존 인계 작업을 수행한 결과, 테스트·빌드·데이터 검증은 통과했으나 아래 운영상
문제가 확인됐다. 다음 순서로 수정하고 실제 운영 검증까지 끝낼 것.

1. **P0 — 시세 갱신의 거짓 실패 판정**
2. **P1 — 주간 리포트의 런타임 반영**
3. **P1 — 원자적 저장 실패 시 임시파일 정리**
4. **P1 — MDD에서 입출금 효과 분리**
5. **P2 — 인계·운영 문서 최신화**

작업 시작 전 `AGENTS.md`, 이 문서, `WORK_ORDER.md` 및 관련 테스트를 읽는다. 기존 사용자
변경사항을 보존하고 관련 없는 파일은 되돌리지 않는다. 실제 계좌 값이나 인증정보를 로그,
테스트 픽스처, 커밋에 넣지 않는다. 각 수정에는 회귀 테스트를 추가하며, 검증 전에는 완료로
표시하지 않는다.

### 11-1. P0 — 시세 갱신 결과와 예약 작업 종료 상태 일치

#### 확인된 현상

- `CGPORTFOLIO 시세 갱신` 예약 작업이 `LastTaskResult=1`로 종료된다.
- `scripts/refresh-quotes.mjs` 요청은 약 5분 뒤 `UND_ERR_HEADERS_TIMEOUT`으로 실패한다.
- 서버 갱신은 약 7분 뒤 실제 완료되며 `quotes.json`, `fx-quote.json`, `snapshots.json`도
  갱신된다.
- 즉 실제 성공이 호출 스크립트와 작업 스케줄러에는 실패로 기록된다.

#### 필수 수정

1. 장시간 HTTP 연결 하나에 의존하지 않는 실행 구조로 변경한다.
2. 권장 구조는 API가 작업 접수 후 `jobId`를 빠르게 반환하고, 호출 스크립트가 상태 API나
   신뢰 가능한 상태 파일을 조회하는 방식이다.
3. 실제 완료만 exit code 0으로 처리하고, provider 실패·서버 미기동·작업 시간 초과는 서로
   구분해 non-zero로 종료한다.
4. 이미 갱신이 진행 중이면 두 번째 호출이 중복 쓰기를 시작하지 않게 잠금 또는 단일 실행을
   보장한다.
5. 실패 로그에는 provider, 단계, jobId, 경과시간, 원인 코드를 남기되 비밀값은 남기지 않는다.
6. 다른 방식을 택한다면 Undici `headersTimeout`까지 실제로 제어하고 선택 이유를 문서화한다.
   `AbortSignal`만 10분으로 늘리는 수정은 인정하지 않는다.

#### 완료 조건

- 5분이 넘는 정상 갱신도 호출 스크립트와 작업 스케줄러가 성공으로 끝난다.
- 데이터 세 파일의 실제 갱신까지 확인한 뒤 성공 처리한다.
- 성공·provider 실패·서버 미기동·시간 초과·중복 호출 테스트가 있다.
- Windows 작업 스케줄러에서 직접 1회 실행해 `LastTaskResult=0`을 확인한다.

### 11-2. P1 — 새 주간 리포트를 재빌드 없이 노출

#### 확인된 현상

- `app/reports/page.tsx`는 빌드 시 정적으로 생성된다.
- `app/reports/[slug]/page.tsx`도 정적 파라미터 기반이다.
- 스케줄러가 빌드 후 `weekly-reports.json`을 갱신해도 새 보고서가 목록에 나타나지 않을 수
  있다.

#### 필수 수정과 완료 조건

1. 목록과 상세 페이지가 런타임에 최신 파일을 읽게 한다. 우선안은 두 경로를
   `force-dynamic`으로 지정하는 것이다.
2. 재검증 방식을 선택한다면 외부 스케줄러가 파일 저장 후 실제 Next.js 캐시까지 확실히
   무효화하는 전체 흐름과 실패 처리를 구현한다.
3. 서버 빌드·기동 후 테스트용 새 리포트를 안전하게 추가해, 재빌드 없이 목록과 상세에
   나타나는지 검증한다.
4. 존재하지 않는 slug의 404와 기존 정렬을 유지한다.
5. 검증용 데이터는 검증 후 제거한다.

### 11-3. P1 — 원자적 저장의 실패 임시파일 제거

#### 확인된 현상

- `lib/data/atomic-write.ts`와 `scripts/lib/atomic-write.mjs`는 rename 실패 시 임시파일을
  제거하지 않는다.
- 실제로 `data/.weekly-reports.json.tmp-24068-1789586461058`이 Git 미추적 상태로 남았다.
- 이 파일에는 계좌 데이터가 포함될 수 있고 현재 `.gitignore` 대상도 아니다.

#### 필수 수정과 완료 조건

1. 두 구현 모두 성공 여부와 무관하게 임시파일을 best-effort로 정리한다.
2. 정리 실패가 원래 예외를 덮어쓰지 않도록 하되 진단 정보는 남긴다.
3. `data/.*.tmp-*`를 `.gitignore`에 추가해 우발적 커밋을 이중 방지한다.
4. rename 실패 뒤 대상 JSON이 보존되고 임시파일이 제거되며 재시도가 성공하는 테스트를
   추가한다.
5. 수정·테스트 후 위 실제 잔여 파일을 내용 노출 없이 삭제하고 `git status`와
   `git check-ignore`로 결과를 확인한다.

### 11-4. P1 — 주간 MDD를 현금흐름 조정 기준으로 계산

#### 확인된 현상

- `lib/domain/metrics.ts`는 `totalKrw` 고점 대비 변화만으로 MDD를 계산한다.
- `principalKrw`의 입출금이 투자 수익·손실처럼 섞인다.
- 현재 데이터에서 두 방식의 결과가 눈에 띄게 다르다(하락 구간에도 납입을 이어온 계좌라
  평가액 기준이 실제 성과보다 덜 빠진 것처럼 나온다). 실제 수치는 이 문서가 커밋되므로
  적지 않는다 — 로컬에서 `cashflowAdjustedDrawdown`으로 직접 확인할 것.

#### 필수 수정과 완료 조건

1. 주간 리포트의 성과 MDD를 현금흐름 조정 수익지수 기준으로 계산한다.
2. 스냅샷 사이 `principalKrw` 변화를 외부 현금흐름으로 간주한다는 데이터 모델상의 가정을
   코드 주석과 문서에 명시한다.
3. 최초 스냅샷, 중복·오정렬 시각, 0 이하 평가액, 입출금만 있는 기간, 평가액과 원금이 함께
   변한 기간을 안전하게 처리한다.
4. 기존 평가액 기준 낙폭을 유지해야 하는 화면은 별도 함수와
   `계좌 평가액 낙폭(입출금 포함)` 같은 명확한 이름을 사용한다.
5. 입출금만 있고 가격 변화가 없는 경우 MDD가 악화되지 않는 테스트를 포함해 순수 하락,
   회복, 추가 입금, 일부 출금 시나리오를 검증한다.
6. 최근 1년 범위 제한과 스냅샷 정렬을 유지하고, 다른 소비자의 의미를 조용히 바꾸지 않는다.

### 11-5. P2 — 문서를 실제 운영 상태와 일치시킬 것

1. 예약 작업이 현재 다음 4개임을 반영한다.
   - `CGPORTFOLIO 서버`
   - `CGPORTFOLIO 데이터 백업`
   - `CGPORTFOLIO 시세 갱신`
   - `CGPORTFOLIO 주간 리포트`
2. QQQ 익스포저, 주간 리포트 생성 흐름, 데이터 파일과 스케줄을 기록한다.
3. 이번에 확정한 갱신 프로토콜, timeout, 재시도, 중복 실행 방지를 기록한다.
4. 리포트 라우트의 동적 데이터 정책, MDD 정의, 임시파일 정리·ignore 정책을 기록한다.
5. LAN 공개·무인증 정책은 사용자 승인하에 유지되는 수용 위험으로 남긴다.
6. 실행으로 확인하지 않은 항목은 `완료`가 아니라 `미확인` 또는 `수용 위험`으로 구분한다.

### 11-6. 필수 검증

```powershell
npm test
npm run lint
npx tsc --noEmit
npm run build
node scripts/validate-data.mjs
npm run backup:verify
git diff --check
git status --short
```

추가로 서버 `/status` 200, 수동 시세 갱신 전체 성공, 예약 작업 `LastTaskResult=0`, 빌드 후
생성한 리포트의 무재빌드 노출, 실제 데이터 참조 무결성 및 최신 스냅샷 보존을 확인한다.
민감한 임시파일·테스트 데이터·예상하지 않은 산출물이 남아서는 안 된다.

### 11-7. 완료 보고 형식

1. 문제별 원인과 수정 내용
2. 변경 파일과 추가·수정 테스트 목록
3. 필수 검증별 성공·실패 결과
4. 실제 예약 작업 실행 시각, 종료 코드, 데이터 최종 갱신 시각
5. 남은 위험·미확인 사항·사용자 결정 필요 사항
6. 커밋 해시와 원격 반영 여부

다음 중 하나라도 남으면 전체 작업을 완료로 표시하지 않는다: 실제 갱신과 종료 상태의 불일치,
새 리포트 노출에 재빌드 필요, 실패한 저장 뒤 임시파일 잔존, 주간 MDD에 입출금 효과 포함,
자동·데이터·백업 검증 실패, 문서와 실제 운영 흐름의 불일치.

### 11-8. 처리 결과 (2026-09-17, Claude)

| 항목 | 상태 | 근거 |
|---|---|---|
| 11-1 P0 갱신 거짓 실패 | ✅ 완료 | 예약 작업 수동 실행 `LastTaskResult=0`, 작업 7분 17초(옛 5분 한계 통과), 데이터 3파일 mtime 갱신 확인 |
| 11-2 리포트 런타임 노출 | ✅ 완료 | 두 라우트 `force-dynamic`(빌드 출력 `ƒ`), 없는 slug 404 확인 |
| 11-3 임시파일 정리 | ✅ 완료 | TS·mjs 양쪽 정리 + 실패 주입 테스트, `.gitignore` 보강, 실제 잔여 파일 삭제 |
| 11-4 MDD 현금흐름 조정 | ✅ 완료 | `cashflowAdjustedDrawdown` 신설, 인계서가 제시한 두 수치와 일치 확인(수치는 미기재) |
| 11-5 문서 최신화 | ✅ 완료 | `WORK_ORDER.md` 0-3/0-5a/0-5b |

**원인 요약(11-1)**: Undici `headersTimeout`이 5분 고정인데 갱신이 약 7분 걸려, 성공한 갱신이
스크립트에서 끊기고 작업 스케줄러에 실패로 남았다. `AbortSignal`로는 해결되지 않는다.
긴 연결을 없애고 접수(`202 {jobId}`) + 상태 조회로 바꿨다. 자세한 규약은
`WORK_ORDER.md` 0-5a절.

**남은 위험 / 미확인**

- 서버 작업은 "로그인한 사용자로 실행"이라 **부팅 후 로그인 전에는 서버가 안 뜬다** — 수용 위험.
- LAN 공개·무인증 유지 — 사용자가 명시적으로 선택한 수용 위험.
- **미확인**: 갱신 도중 서버가 죽는 경로는 코드상 `failed`로 답하게 했으나 실제 재현 검증은
  하지 않았다.
- 성과 수치(낙폭 등)는 이 저장소가 Public이라 문서·커밋메시지·테스트 픽스처에 적지 않는다.
  검증은 로컬에서 하고 결과만 "일치 확인"으로 남긴다.

---

## 12. 2026-09-17 GPT 3차 검수 — 보강 작업 수행 후 판정

**이 절이 I·J절 및 11절의 완료 표보다 최신이다.** 과거 절은 당시의 문제와 해결 과정을
보존하기 위한 기록이고, 다음 담당자는 아래 판정과 `WORK_ORDER.md` B-0B를 우선한다.

### 12-1. 독립 검증 결과

- 기준: `main`/`origin/main` 모두 `3727e7f`, 검수 시작 시 작업 트리 깨끗함.
- `npm test`: 39개 파일, **344개 테스트 전부 통과**.
- `npm run lint`, `npx tsc --noEmit`, `node scripts/validate-data.mjs`,
  `npm run backup:verify`, `git diff --check`: 모두 통과.
- 최신 프로덕션 빌드 산출물은 2026-09-17 08:57 생성됐고 서버 작업은 08:58에 기동됐다.
  검수 중 `/status`는 200을 반환했다. 실행 중인 서버를 흔들지 않기 위해 빌드는 다시
  덮어쓰지 않았다.
- 리포트 목록 200, 최신 주간 리포트 상세 200, 없는 slug 404를 확인했다. 두 페이지의
  `force-dynamic`도 소스와 현재 빌드에 반영돼 있다.
- `CGPORTFOLIO 시세 갱신`의 2026-09-17 08:46 실행은 `LastTaskResult=0`으로 끝났다.
  63건을 437,408ms에 갱신했고 세 데이터 파일 수정 시각은 08:53:44로 일치한다.
- 백업·주간 리포트 예약 작업의 마지막 결과도 0, 서버 작업은 정상 Running이다.
- `data/`에 `.tmp-*` 잔여 파일이 없고 Git 작업 트리에도 민감한 미추적 파일이 없다.

따라서 11-1(거짓 실패), 11-2(동적 리포트), 11-3(정상적인 임시파일 정리),
11-4(현재 데이터의 현금흐름 조정 MDD), 11-5(운영 문서 보강)의 핵심 목적은 달성됐다.
다만 아래 세 가지는 완료 판정 뒤에도 남은 회귀·진단 공백이라 추가 보강한다.

### 12-2. P1 — `principalKrw`가 중간부터 생기는 구간의 MDD 오류

#### 확인된 문제

`lib/domain/metrics.ts`의 `cashflowAdjustedDrawdown()`은 이전 스냅샷에
`principalKrw`가 없고 현재 스냅샷에만 있을 때 현재 원금 전체를 그 구간의 순입금으로
계산한다. 함수 주석은 “원금이 없는 스냅샷은 그 구간 현금흐름을 0으로 본다”고 설명하지만
실제 구현과 다르다. 기존 테스트는 두 스냅샷 모두 원금이 없는 경우만 검사해 이 혼합
경계를 잡지 못했다.

현재 운영 스냅샷 631개에는 모두 `principalKrw`가 있어 **현재 리포트 수치에는 영향이
없다.** 그러나 원금 필드 도입 전 데이터나 부분 가져오기 자료가 섞이면 성과 낙폭이 조용히
왜곡될 수 있다.

#### 필수 보강

1. 이전·현재 원금이 둘 다 유한한 경우에만 차이를 현금흐름으로 사용한다.
2. 한쪽이라도 없거나 유효하지 않으면 해당 구간 현금흐름을 0으로 볼지, 성과 지수를
   끊고 다음 완전한 구간에서 다시 시작할지 명시적으로 결정하고 주석·문서와 맞춘다.
3. `둘 다 있음`, `이전만 없음`, `현재만 없음`, `둘 다 없음` 네 조합과 잘못된 숫자 값을
   단위 테스트로 고정한다.
4. 현재 실데이터 결과가 의도치 않게 바뀌지 않는지 로컬에서만 비교하고 실제 수치는
   커밋 문서나 로그에 쓰지 않는다.

### 12-3. P1 — 임시파일 삭제 실패가 진단 없이 묻힘

#### 확인된 문제

`lib/data/atomic-write.ts`와 `scripts/lib/atomic-write.mjs`는 rename 실패 후
`unlinkSync(tmpPath)`도 실패하면 그 오류를 아무 기록 없이 삼킨다. 원래 rename 오류를
보존하는 것은 맞지만, 삭제 실패를 전혀 알리지 않으면 실제 계좌 내용이 든 임시파일이
남아도 운영자가 모른다. 11-3의 요구사항 중 “정리 실패를 진단 가능하게 남긴다”는 부분은
아직 충족되지 않았다.

#### 필수 보강

1. 원래 저장 오류를 그대로 다시 던지면서 삭제 실패의 오류 코드와 임시파일 basename을
   진단 가능하게 남긴다. 파일 내용과 전체 계좌 경로는 출력하지 않는다.
2. 서버용과 스크립트용 두 구현의 동작을 맞춘다.
3. rename 실패에 이어 unlink도 실패하도록 주입한 테스트에서 원래 rename 오류가 유지되고,
   정리 실패 진단이 남는지 확인한다.
4. `.gitignore`의 `/data/.*.tmp-*` 방어선은 그대로 유지한다.

### 12-4. P2 — 서버 중단 시 갱신 상태 복구 경로 테스트

접수·상태 조회 방식은 실제 7분 17초 실행으로 정상 동작이 확인됐다. 그러나 11-8에 적힌
“갱신 중 서버가 죽는 경로”는 아직 실제 또는 자동 테스트로 검증되지 않았다.

1. 임시 상태 폴더에 `running` 상태를 저장한 뒤 모듈 메모리를 초기화해 서버 재시작을
   흉내 내고, 같은 jobId 조회가 `failed`로 끝나는지 테스트한다.
2. 상태 파일이 쓰는 도중 중단돼 JSON이 손상된 경우를 `unknown-job`과 구분해 진단할 수
   있게 한다. 가능하면 상태 파일도 임시파일+rename으로 원자적으로 쓴다.
3. 상태 조회 라우트의 인증 성공·실패와 재시작 후 실패 응답을 라우트 수준에서 검증한다.
4. 실제 운영 서버를 강제로 죽이는 검증은 사용자 승인 없이 하지 않는다. 코드·테스트만
   끝냈다면 운영 중단 재현은 `미검증`으로 남긴다.

### 12-5. 완료 조건

- 위 세 항목에 회귀 테스트가 추가되고 전체 344개 기존 테스트를 포함해 모두 통과한다.
- lint, TypeScript, 프로덕션 빌드, 데이터 검증, 백업 복원 검증이 다시 통과한다.
- 운영 데이터를 변경하지 않고, `git status --short`에 임시파일이나 테스트 데이터가 없다.
- `HANDOFF.md`와 `WORK_ORDER.md`의 완료 표기·테스트 수·최종 기준 절을 갱신한다.
- 코드 수정 후 새 빌드를 운영 서버에 반영했다면 `/status`, 리포트 200/404, 다음 예약
  갱신 결과를 다시 확인한다. 실제로 확인하지 않은 운영 항목은 완료라고 쓰지 않는다.

### 12-6. B-0B 처리 결과 및 GPT 4차 검수 (2026-09-17)

Claude가 커밋 `a1966ee`로 B-0B 세 항목을 구현해 `main`과 `origin/main`에 반영했다.
GPT가 변경 코드·테스트·현재 프로덕션 서버를 다시 대조했다.

| 항목 | 판정 | 검수 결과 |
|---|---|---|
| 12-2 원금 필드 혼합 구간 MDD | ✅ 완료 | 양쪽 `principalKrw`가 모두 유한할 때만 차이를 현금흐름으로 사용한다. 이전만 없음·현재만 없음·둘 다 없음·둘 다 있음·NaN 경계 테스트를 확인했다. 현재 운영 스냅샷 결과는 바뀌지 않는다. |
| 12-3 임시파일 삭제 실패 진단 | ✅ 완료 | TS·mjs 구현 모두 원래 rename 오류를 유지하면서 unlink 실패 코드와 basename만 `console.error`에 남긴다. 전체 경로·내용이 노출되지 않는 연속 실패 주입 테스트를 확인했다. |
| 12-4 서버 재시작 상태 복구 | ✅ 코드·자동 테스트 완료 | 상태 파일을 원자적으로 저장하고, persisted `running`+모듈 초기화를 재시작으로 인식해 `failed`로 응답한다. 손상 JSON은 `500 corrupted-state`, 미존재 job은 `404 unknown-job`으로 구분하며 상태 라우트 인증 테스트도 추가됐다. |

**독립 검증 결과**

- `npm test`: 42개 파일, **367개 테스트 전부 통과**.
- `npm run lint`, `npx tsc --noEmit`, `node scripts/validate-data.mjs`,
  `npm run backup:verify`, `git diff --check`: 모두 통과.
- 새 프로덕션 빌드 산출물은 09:39 생성, 서버 작업은 09:40 기동. `/status` 200,
  갱신 상태 API 200, 리포트 목록·상세 200, 없는 slug 404를 확인했다.
- `data/`와 상태 로그 폴더에 `.tmp-*` 잔여 파일이 없다.
- 검수 시작 시 `main == origin/main == a1966ee`, 작업 트리는 깨끗했다.

**남은 운영 관찰**

- 실제 운영 서버를 갱신 도중 강제로 종료하는 재현은 데이터 갱신과 서비스 중단 위험 때문에
  수행하지 않았다. 동일 경로는 모듈 메모리 초기화 회귀 테스트로 검증했다.
- 새 빌드가 09:40에 배포됐고 다음 자동 시세 갱신은 14:00이다. 검수 시점에는 아직 실행 전이라
  **새 빌드 이후 예약 갱신 결과는 미확인**이다. 직전 08:46 실행은 `LastTaskResult=0`이지만
  이전 빌드의 실행 기록이다. 14:00 이후 결과 0과 새 상태 파일 완료 기록을 확인하기 전에는
  이 운영 관찰만 완료로 바꾸지 않는다.

위 운영 관찰을 제외하면 B-0B에서 요구한 코드 보강은 완료됐고, 이번 검수에서 새 차단 문제는
발견되지 않았다.

---

## 13. 로그인 인증 구현 (2026-09-17)

### 사용자 결정

실제 서버 등록을 준비하면서 사용자가 이전의 LAN 무인증 허용 결정을 철회하고 로그인
인증을 선택했다. 로그인 상태는 약 1주일 유지해 달라는 요청에 따라 발급 시점부터 정확히
7일인 고정 세션으로 구현했다.

### 구현 내용

- `proxy.ts`: 로그인·인증 API·크론 API·정적 자산을 제외한 전체 화면/API를 보호한다.
  production에서 인증 환경변수가 빠져도 공개되지 않고 설정 안내 로그인 화면으로 보낸다.
- `lib/auth/`: HMAC-SHA256 서명 세션, scrypt 비밀번호 검증, 내부 주소만 허용하는 로그인
  후 복귀 경로, 15분/5회 로그인 실패 제한, 크론 Bearer 인증을 분리했다.
- 로그인 쿠키: `HttpOnly`, `SameSite=Lax`, 외부 호스트 `Secure`, `Path=/`, 7일 만료.
- `app/login`, `app/api/auth/login`, `app/api/auth/logout`: 로그인 화면과 발급/삭제 경로.
- 가져오기 Server Action 6개는 Proxy와 별개로 세션을 직접 검사한다.
- 시세 갱신·상태·주간 리포트 API는 production에서 `CRON_SECRET`이 없으면 거부한다.
  Windows 예약 스크립트는 실행 때 `.env.local`을 읽어 해당 비밀키를 자동 사용한다.
- `npm run auth:setup`: 비밀번호를 터미널에서 숨김 입력받아 원문 대신 scrypt 해시와
  무작위 `SESSION_SECRET`/`CRON_SECRET`을 `.env.local`에 저장한다. 실제 값은 문서·git에
  남기지 않는다.

### 이번 검증

- `npm test`: **49개 파일, 391개 테스트 전부 통과**. 세션 만료/변조, 비밀번호,
  로그인 쿠키, 외부 redirect 차단, 실패 제한, Proxy, production 크론 fail-closed 포함.
- `npm run lint`, `npx tsc --noEmit --incremental false`, `git diff --check`: 통과.
- 프로덕션 빌드와 브라우저 로그인은 아직 실행하지 않았다. 현재 3000 포트 운영 서버의
  `.next`를 실행 중 덮어쓰지 않고, 사용자가 `npm run auth:setup`으로 계정을 만든 뒤
  빌드·재시작과 함께 검증하기 위함이다.

### 다음 작업자가 할 일

1. 사용자가 직접 저장소 터미널에서 `npm run auth:setup`을 실행하게 한다. 비밀번호를
   채팅으로 받거나 출력하지 않는다.
2. 계정 설정 후 프로덕션 빌드, 서버 작업 재시작, HTTPS 주소에서 로그인/로그아웃과
   `Max-Age=604800` 쿠키를 확인한다.
3. 예약 시세 갱신과 주간 리포트 작업을 각각 한 번 실행해 `CRON_SECRET` 자동 로드가
   정상인지 확인한다.
4. 위가 끝나기 전에는 인터넷 공유기 포트포워딩이나 방화벽 공개를 하지 않는다.

### 13-1. 배포 및 차단 버그 수정 (2026-09-17, Claude)

GPT가 구현만 하고 중단한 뒤 이어받아 커밋·빌드·배포하고, 그 과정에서 **로그인이
원천적으로 불가능한 버그**를 발견해 고쳤다.

#### 차단 버그 — `$` 구분자가 dotenv 변수확장에 먹힘

`setup-auth`가 만든 해시는 `scrypt$v1$16384$8$1$<salt>$<hash>` 형식이었다. Next가
`.env.local`을 읽을 때 쓰는 dotenv는 `$v1`·`$16384` 등을 **변수 참조로 보고 확장**해
버려서, 129자 해시가 앱에는 24자로 도착했다. 그 결과 `AUTH_PASSWORD_HASH` 형식 검사가
항상 실패해 `isAuthConfigured()`가 false가 되고, 모든 요청이 `/login?setup=1`로 튕겼다.
**비밀번호를 맞게 넣어도 로그인은 성공할 수 없는 상태였다.**

- 작은따옴표·큰따옴표 모두 확장을 막지 못한다(직접 확인).
- `\$` 이스케이프는 동작하지만 유지보수 중 쉽게 깨진다.
- → 구분자를 **`.`**로 바꿨다. base64url 알파벳에 `.`이 없어 값과 충돌하지 않는다.
- 기존 `.env.local`의 해시는 `$`→`.` 치환으로 **자리에서 마이그레이션**했다(길이 129 유지
  확인). 비밀번호를 다시 입력할 필요는 없었다. 백업은 `.env.local.bak-before-delim`.
- 회귀 테스트: 생성 해시에 `$`가 없고 `.`으로 7조각이 되는지 고정.

#### 배포 후 실제 확인

| 확인 | 결과 |
|---|---|
| 비로그인 페이지 접근 | `/`·`/status`·`/reports`·`/symbols` 모두 307 → `/login?next=…` |
| 수정 전/후 구분 | 수정 전에는 `&setup=1`이 붙었고, 수정 후 사라짐 = 설정 인식됨 |
| 틀린 비밀번호 | 303 → `/login?error=invalid` (설정 오류가 아니라 자격 검증까지 도달) |
| 비인증 크론 API | 401 |
| `CRON_SECRET` 자동 로드 | 스크립트와 같은 경로로 상태 API 호출 시 200, 무인증 401 |
| 주간 리포트 스크립트 | exit 0 |
| 테스트 | 49개 파일, **392개 통과** (회귀 1건 추가) |
| lint / tsc / build | 통과 |

#### 남은 확인 (미완료)

1. **브라우저 실제 로그인과 `Max-Age=604800` 쿠키 확인은 못 했다.** 성공 경로는 비밀번호가
   있어야 하는데, 비밀번호는 받지도 출력하지도 않는 것이 이 작업의 전제다. 사용자가
   직접 로그인해 확인해야 한다.
2. 새 빌드 이후 **예약 시세 갱신의 종료 코드 0 확인**은 진행 중이었다(수동 트리거).
3. 인터넷 공개(포트포워딩·방화벽)는 위 1번이 끝나기 전까지 하지 않는다.

## 14. 인터넷 공개 (2026-09-17, Tailscale Funnel)

사용자가 "휴대폰에서 외부 접속"을 요청해 공개했다. 무료로 **고정 주소 + HTTPS**를 얻는
방법이 사실상 Tailscale Funnel뿐이라 이걸 골랐다(Cloudflare Tunnel은 도메인이 있어야
주소가 고정된다. 도메인 없이는 재시작마다 주소가 바뀐다).

### 구성

| 항목 | 값 |
|---|---|
| 공개 주소 | `https://cgportfolio.tailab9ee1.ts.net` |
| 경로 | Funnel → `http://127.0.0.1:3000` (프로덕션 서버) |
| 머신 이름 | `cgportfolio` |
| HTTPS | Tailscale이 인증서 자동 발급·갱신 |
| 공유기 설정 | **불필요** (포트포워딩 없음, 공인 IP 노출 없음) |

`tailscale funnel --bg 3000`으로 켰다. 끄려면 `tailscale funnel --https=443 off`.

### 재부팅 후 유지

- Tailscale 서비스는 `StartType=Automatic`이고, serve/funnel 설정은 노드 상태에 저장돼
  데몬이 뜨면 자동 복구된다 → **Funnel은 별도 작업 등록이 필요 없다.**
- 앱 서버(`CGPORTFOLIO 서버`)도 **부팅 시 실행으로 바꿨다**(2026-09-17). 부팅 트리거
  (30초 지연)를 추가하고 실행 주체를 `LogonType=S4U`로 바꿔, **로그인하지 않아도**
  뜬다. S4U는 비밀번호를 저장하지 않는 방식이라 자격증명을 어디에도 남기지 않는다.
  - 주체 변경은 관리자 권한이 필요해 사용자가 직접 실행했다. 원래 정의 백업은
    `~/cgportfolio-logs/task-server-backup.xml`.
  - 같이 고친 것: `scripts/start-server.cmd`의 재시작 대기가 `timeout` 명령이었는데,
    이건 콘솔이 없는 부팅 환경에서 "Input redirection is not supported"로 **즉시 실패**한다.
    그러면 10초 대기 없이 재시작 루프가 폭주한다. `ping -n 11 127.0.0.1`로 바꿨다.
  - 검증: 포트 3000을 완전히 내린 뒤 작업만으로 기동 → 로컬 307, 공개 주소 307,
    로그인 화면 200, `server.log`가 기존과 같은 사용자 프로필 경로에 기록되는 것까지 확인.

### 공개에 맞춰 바꾼 것

- `next.config.ts`의 `serverActions.allowedOrigins`에 공개 도메인을 등록했다. Server Action은
  Origin과 Host를 대조하는 CSRF 검사를 하므로, 없으면 가져오기 6개가 전부 거부된다.
- 로그인 리다이렉트를 상대 경로로, Secure 쿠키를 프로토콜 기준으로 바꿔 둔 덕분에
  (13-1 참고) 공개 도메인에서 코드 수정 없이 그대로 동작했다.

### 공개 후 실제 확인

| 확인 | 결과 |
|---|---|
| 외부 HTTPS 접속 | 307 → `https://cgportfolio.tailab9ee1.ts.net/login?next=%2F` (localhost로 안 튕김) |
| TLS 인증서 검증 | 정상(`ssl_verify_result=0`) |
| 로그인 화면 | 200 |
| HTTPS에서 Secure 쿠키 | `Secure; HttpOnly; SameSite=lax` 자동 적용 확인 |
| 외부에서 크론 API | `/api/cron/refresh`·`/status`·`/weekly-report` 전부 **401** |

### 남은 위험

- **로그인 비밀번호가 유일한 방어선이다.** 주소를 알면 누구나 로그인 화면까지 도달한다.
  비밀번호가 짧거나 재사용된 것이면 `npm run auth:setup`으로 교체할 것.
- **로그인 실패 제한의 클라이언트 식별이 미확인이다.** `x-real-ip`/`x-forwarded-for`를
  키로 쓰는데, Funnel이 이 헤더를 어떤 값으로 넣는지 확인하지 않았다. 모든 외부 요청이
  같은 키로 묶이면 제3자의 실패 시도로 본인이 15분간 잠길 수 있다. 브루트포스가 실제
  문제가 되면 이것부터 확인할 것.
- 대역폭은 Tailscale Funnel 무료 한도를 따른다. 개인 용도 수준에서는 문제되지 않는다.

## 15. 다계좌 전환과 가져오기 결함 정리 (2026-09-21, Claude)

계좌를 1개에서 5개로 늘렸다. 그 과정에서 **"계좌가 하나뿐"을 전제로 짜여 있던
코드들이 연달아 드러났고**, 그중 하나는 실제로 데이터를 날렸다(백업에서 복구).
다음 담당자는 이 절을 먼저 읽을 것.

### 15-1. 등록한 계좌

| id | 이름 | 성격 | 양도세 |
|---|---|---|---|
| `acc-us-main` | 키움 일반(메인) | 위탁 | 대상 |
| `kiwoom-2` | 키움 일반2 | 위탁 | 대상 |
| `mirae-main` | 미래에셋 일반 | 위탁 | 대상 |
| `mirae-pension` | 미래에셋 연금저축 | 연금저축 | **비대상** |
| `mirae-irp` | 미래에셋 퇴직연금 | 퇴직연금 | **비대상** |

기존 계좌는 **id를 그대로 두고 이름만** 바꿨다(`acc-us-main`) — id를 바꾸면
거래 1,068건·입출금 792건이 전부 끊긴다. 앞으로도 id는 건드리지 말 것.

연금계좌 구분은 `Account.taxDeferred` 플래그로 한다. `kind`가 자유 문자열이라
문자열 매칭으로 추측하면 오타 하나에 세금 계산이 틀어진다 — 플래그만 믿는다.

### 15-2. 이번에 고친 결함 (전부 "계좌 1개" 전제에서 나온 것)

1. **잔고 가져오기가 파일 전체를 덮어썼다** — `position-basis.json`에 이번 계좌
   값만 써서 다른 계좌 보유가 사라졌다. **실제로 kiwoom-2를 넣다가 메인 계좌
   28종목이 날아가 백업에서 복구했다.** CLI·웹 액션 양쪽 모두 "해당 계좌 줄만
   교체"로 수정하고 회귀 테스트를 넣었다.
2. **inbox가 잔고를 항상 `acc-us-main`에 넣었다**(코드에 계좌 고정). 이제
   `~/cgportfolio-inbox/<계좌ID 또는 계좌이름>/` 하위 폴더로 계좌를 정하고,
   폴더가 없거나 모르는 계좌면 **반영하지 않고 `실패/`로 뺀다**.
3. **CSV 파서가 따옴표 안의 줄바꿈을 못 읽었다** — 키움 2167 화면은 헤더에
   줄바꿈을 넣어 내보낸다(`"유가증권\n 평가금"`). 한 행이 두 줄로 쪼개져
   **1년 반치 계좌 이력이 계속 "판별 못 함"으로 빠지고 있었다.** `parseCsvRows`를
   따옴표 상태를 추적하도록 고치고, 판별기도 같은 파서를 쓰게 했다.
4. **잠긴 파일 하나가 inbox 실행 전체를 멈췄다** — 엑셀로 열어 둔 `.xlsx`를
   옮기다 EBUSY가 나면 예외가 위로 올라가 뒤의 멀쩡한 파일이 처리되지 않았다.
   파일 옮기기를 best-effort로 바꿨다.
5. **키움 잔고 CSV는 소수점 보유를 별도 행으로 내보낸다**(`소수점구분` = 온주/소수점).
   같은 종목이 두 줄이라 계좌×종목 중복으로 검증에서 막혔다. 수량·매입금액·수수료를
   합치고 평단을 가중평균으로 다시 구하도록 파서를 고쳤다.

### 15-3. 아직 남은 함정 — 계좌수익률 CSV는 합산이 안 된다

`buildAccountHistorySnapshots`는 CSV 값을 넣은 뒤 **기존 스냅샷으로 덮어쓴다**
(같은 `at` 키면 기존 값이 이김). 그래서 계좌수익률 CSV를 **하나씩 따로 넣으면
계좌별 합산이 안 되고 먼저 들어간 계좌 값만 남는다.** 여러 계좌를 합치려면
`parseAccountHistoryTotals`에 **CSV를 한 번에 같이** 넘겨야 한다.

inbox는 파일을 하나씩 처리하므로 이 경로로는 합산이 안 된다. 이번엔 손으로
처리했다 — CSV로 만든 스냅샷(`at`이 `T15:30:00+09:00`)만 걷어내고 cron이 찍은
것은 남긴 뒤, 두 CSV를 한 번에 넣어 645개로 재생성했다. 검산도 맞췄다
(`2026-09-15`: 메인 + 키움2 = 스냅샷 값과 일치).

**미래에셋 3개 계좌 이력을 넣을 때 같은 작업이 또 필요하다.** 근본적으로는
(a) inbox가 계좌수익률 CSV를 모아 한 번에 넘기게 하거나, (b) 병합에서 CSV가
기존 CSV 유래 스냅샷을 이기게 하되 cron 스냅샷은 보존하도록 고쳐야 한다.

### 15-4. 지금 데이터 상태

| 계좌 | 거래 | 잔고 | 입출금 | 배당 |
|---|---|---|---|---|
| acc-us-main | 1,068 | 28 | 792 | 68 |
| kiwoom-2 | 881 | 4 | 1,016 | 15 |
| mirae-* 3개 | — | — | — | — |

- 스냅샷 645개(2025-01-01 ~ 2026-09-20) = 메인 + 키움2 합산
- 키움2는 잔고만 있어 평가금액엔 잡히지만 실현손익·양도세엔 안 잡힌다

### 15-5. 다음 담당자가 할 일

1. **서버 재시작** — 15-2의 1번(웹 잔고 가져오기 수정)이 아직 운영에 안 올라갔다.
   올라가기 전까지 `/accounts/import`의 잔고 업로드를 쓰면 **여전히 다른 계좌가
   날아간다.** 세션 0 문제로 관리자 권한이 필요하다(WORK_ORDER 0-4 함정 11번).
2. **미래에셋 3개 계좌 CSV 받기** — 잔고는 계좌 폴더에, 계좌수익률은 15-3대로
   한 번에 합쳐서.
3. **거래내역 가져오기** — ~~inbox는 이 종류를 처리하지 않는다~~ → **16절에서 만들었다.**
   (여기 적어 둔 "웹 화면의 원장 정규화로 넣으면 된다"는 사실과 달랐다 — 그 화면은
   기존 원장을 다시 정규화할 뿐 CSV를 읽지 않는다. importer 자체가 없었다.)
   키움2 실현손익·양도세가 나오려면 이게 필요하다.
4. **importer가 없는 종류** — 2138(권리·배당), 3134(예수금 입출금). 필요하면 새로 만들 것.

---

## 16. 거래내역(2110) 가져오기를 만듦 (2026-09-21, Claude)

15-5의 3번을 하려고 보니 **거래내역 CSV를 읽는 importer가 아예 없었다.** 15절에 적힌
"웹 화면의 원장 정규화로 넣으면 된다"는 사실과 다르다 — 그 화면(`/accounts/import`의
원장 정규화)은 **이미 저장된 원장을 다시 정규화할 뿐 CSV를 읽지 않는다.** 메인 계좌
1,068건은 예전에 사람이 손으로 만들어 넣은 것이었다.

### 16-1. 만든 것

| 파일 | 하는 일 |
|---|---|
| `scripts/lib/import-transactions.mjs` | 2110 CSV → 거래·현금흐름·배당 (순수 함수) |
| `scripts/import-transactions-csv.mjs` | CLI. 기본은 미리보기, `--replace`로 반영 |
| `scripts/lib/cashflow-kind.mjs` | 적요명 → `CashFlow.kind`. 가져오기와 정규화가 **같은 규칙**을 쓴다 |
| `scripts/lib/inbox-classify.mjs` | `거래일자`+`거래종류`+`거래수량` 헤더면 거래내역으로 판별 |
| `scripts/import-inbox.mjs` | 거래내역도 **계좌 폴더**에 넣어야 반영한다(잔고와 같은 규칙) |

```powershell
node scripts/import-transactions-csv.mjs "<거래내역.csv>" --account-id=kiwoom-2
```

**이 CSV는 한 거래가 세 줄이다.** 헤더도 세 줄이다(원화 금액 / 종목·잔고 / 외화 금액).
흔한 "헤더 한 줄 + 데이터 한 줄" 파서로 읽으면 통째로 어긋난다. 세 줄로 끊고 남는 줄이
생기면 조용히 버리지 않고 멈춘다.

반영은 **그 계좌의, 조회 기간 안에 있는 줄만** 갈아끼운다. 파일 전체를 덮어쓰면 다른
계좌 원장이 사라지고(15-2의 1번에서 실제로 겪은 사고다), 같은 계좌라도 조회 기간 밖의
옛 거래까지 지워진다.

### 16-2. 검증 — 메인 계좌 원장과 1:1로 맞췄다

메인 계좌의 2110 CSV가 `실패/`에 남아 있었고, **기존 원장이 바로 그 파일로 만든
것**이어서 그대로 정답지가 됐다. 파서 출력과 기존 원장을 맞춰 본 결과:

| 항목 | 결과 |
|---|---|
| 매매 1,041건 | 날짜·종목·매수매도·수량·단가·수수료까지 **전부 일치** |
| 배당 68건 | 전부 일치(세후 정산금액) |
| 이체(대체입출고) 26건 | 건수 일치 |
| 액면분할 | QLD 2배 1건, 한 줄짜리 `action: "split"`로 |

CSV 내부 검산도 넣었다: 매매는 `거래금액 ≈ 수량 × 단가`, 환전은
`정산금액(원) ≈ 정산금액(외) × 환율`. 두 파일 2,300줄에서 불일치 0건이다.
**`정산금액` 열로 수수료를 역산하려던 시도는 접었다** — 181건이 0이고(통합증거금·미결제로
보인다) 149건이 거래금액±수수료와 맞지 않았다. 외화 수수료는 `수수료(외)` 열을 쓴다.

`npm test` 470개 통과(27개 추가), `npm run lint`·`npx tsc --noEmit` 통과.

### 16-3. 이번에 드러난 것 — 예전 원장이 31건을 빠뜨렸다

CSV에는 있는데 기존 원장에는 **없는** 현금흐름이 31건 있었다. 전부 계좌가 스스로
만든 돈이다: 예탁금이용료(이자), 대여수수료 입금, 외국납부세 환급·출금 등.
B-1의 **"원인 미상 원장 재구성 보정"의 유력한 원인**이다(금액까지 맞춰보지는 않았으니
단정은 못 한다). 새 importer는 이 줄들을 `kind: "income"`으로 넣는다.

그래서 분류 규칙을 `cashflow-kind.mjs` 한 곳으로 모으고 이자·이용료·대여수수료·납부세를
수익으로 넣었다. **기존 데이터 분류는 한 줄도 바뀌지 않는다**(본 저장소 792건, 워크트리
816건 모두 확인).

### 16-4. 알아 둘 것 — 저장된 원장에는 `action`도 `kind`도 없다

`data/transactions.json` 1,069건 전부 `action` 없음, `cashflows.json` 816건 전부
`kind` 없음이다. 즉 **원장 정규화 결과가 파일에 반영된 적이 한 번도 없다.** 대신
이체·분할의 잘못된 현금 효과를 "이체입고 상쇄", "원장 재구성 보정" 같은 손으로 만든
현금흐름으로 덮어 왔다.

지금 화면이 틀리게 나오지는 않는다 — 원금은 스냅샷의 `principalKrw`(645개 전부 있음)를
먼저 쓰고 원장 합계는 그게 없을 때의 대비책이기 때문이다. 다만 **대비책 경로는 지금
틀린 값을 낸다**(`kind`가 없으면 전부 `external`이라 환전 336건이 원금에 섞인다).
새 importer로 넣은 계좌는 `action`·`kind`가 채워지므로 이 문제가 없다.

정규화기도 이에 맞춰 고쳤다: 이미 `action`이 붙은 줄은 적요명으로 다시 추측하지 않고
(대체입고/이체입고처럼 증권사 표현이 조금만 달라도 이체가 매매로 뒤집혔다), 한 줄로
합쳐진 분할을 다시 짝지으려 하지 않는다(그대로 두면 터졌다).

### 16-5. 아직 안 한 것

1. ~~실제 데이터 반영~~ → **16-6에서 키움2를 반영했다.**
2. ~~종목 등록~~ → `CONY`, `URNM`, `CEPT` 등록 완료(symbols 67 → 70).
3. **웹 화면에는 거래내역 섹션을 안 만들었다.** inbox(계좌 폴더에 넣기)와 CLI만 된다.
4. **2138(권리·배당)·3134(예수금 입출금) importer는 여전히 없다.** 2110에 배당금
   입금이 들어 있어 배당은 이걸로도 들어오지만, 2138은 권리 배정까지 담는다.
5. **원화 매매 수수료는 미검증.** 이 CSV에는 원화 수수료 열이 없어 `세금합`만 넣고
   경고를 띄운다 — 두 파일 모두 원화 매매가 한 건도 없어 확인할 수 없었다.

### 16-6. 키움2 반영 완료 (2026-09-21)

본 저장소에서 백업 후 반영했다(`~/cgportfolio-backups/2026-09-21T10-27-18-866Z`).
`CONY`·`URNM`·`CEPT`를 먼저 등록했다(symbols 67 → 70).

| 계좌 | 거래 | 현금흐름 | 배당 |
|---|---|---|---|
| acc-us-main | 1,068 (건드리지 않음) | 792 (건드리지 않음) | 68 (건드리지 않음) |
| kiwoom-2 | 881 (신규) | 1,016 (신규) | 15 (신규) |

`scripts/validate-data.mjs` 통과. **키움2 실현손익·양도세가 처음으로 나온다**
(2025·2026 두 과세연도). 금액은 실제 금융 데이터라 여기 적지 않는다 — 화면에서 볼 것.

**접다가 발견해 고친 것 — `처리시간`은 체결 시각이 아니라 그날 정산 배치 시각이다.**
하루 거래가 전부 같은 값을 갖는다(어느 날의 모든 거래가 `08:09:36`). 그래서 파일
순서대로 접으면 **보유수량보다 많이 판 것으로 계산되는 날**이 생기고,
`stepPosition`이 보유분까지만 처리하는 바람에 원가가 잘려 실현손익이 틀어졌다
(키움2에서 5건). 같은 시각이면 매수를 먼저 놓도록 고쳤다(커밋 `0336f0d`) — 하루 안의
진짜 체결 순서는 이 파일로 알 수 없으니 **수량이 음수로 가지 않는 쪽**을 택한 것이다.

고친 뒤 원장의 잔여수량이 증권사 잔고와 맞는다 — 네 종목 중 둘은 정확히 같고, 둘은
소수점 이하만 다르다. 그 차이는 2138(권리·배당) 배정분으로 보인다(importer 없음).

### 16-7. 남은 문제 — 키움2 예수금이 음수로 나온다

원장으로 계산한 키움2 원화 예수금이 **음수**다(외화는 소액 플러스). 있을 수 없는
값이다. 증권사 자기 계좌수익률 CSV는 2026-09-20 기준 예수금(예탁자산 −
유가증권평가금)을 **사실상 0**으로 적고 있다.

원인은 **조회 기간이 계좌 개설 시점까지 닿지 않는 것**이다. 2110 파일의 첫 줄이
2025-02-20 **이체출금**이다 — 그 전에 이미 돈이 들어 있던 계좌라는 뜻이고, 기초잔고가
빠진 채 출금부터 세니 그만큼 음수로 내려간다. 매매·환전 자체는 맞는다(잔여수량이
증권사 잔고와 맞는 게 근거다).

둘 중 하나로 해결할 것:

1. **2110을 계좌 개설일부터 다시 내려받아 다시 넣는다** — 보정값 없이 정확해진다.
   `~/cgportfolio-inbox/kiwoom-2/`에 넣어 두면 10분마다 도는 inbox가 알아서 반영한다
   (조회 기간이 더 넓으므로 이번에 넣은 줄을 통째로 갈아끼운다).
2. **기초잔고 보정 한 줄을 넣는다** — 조회 시작 하루 전 날짜로 `kind: "adjustment"`인
   입금. 금액은 `증권사 예수금 − 원장 계산 예수금`이다. 원금에는 안 잡히고
   (`external`만 원금) 예수금만 맞춘다. 메인 계좌의 "원장 재구성 보정"과 같은 방식이다.

1번이 낫다. 2번은 증권사 조회 기간 제한으로 1번이 불가능할 때 쓸 것.
