# 업데이트 안내 — 계좌 내용이 바뀌었을 때 뭘 넣으면 되나

매매를 했거나 입출금이 있었을 때, **CSV 파일 2개를 받은 폴더에 넣어두면 끝난다.**
나머지(시세, 환율, 백업, 주간 리포트)는 예약 실행이 알아서 한다.

---

## 1. 한 줄 요약

| | |
|---|---|
| **넣는 곳** | `C:\Users\ACC-002\cgportfolio-inbox` |
| **넣을 것** | 계좌수익률 CSV, 보유종목(잔고) CSV |
| **언제 반영되나** | 최대 10분 (예약 실행이 10분마다 훑는다) |
| **끝났는지 확인** | 파일이 `처리완료\` 폴더로 옮겨졌으면 반영된 것 |
| **화면 확인** | 브라우저 새로고침. 상단 `UPDATE :` 시각이 바뀐다 |

---

## 2. 필요한 파일 2개

증권사 프로그램에서 **CSV로 내보내기**만 하면 된다.
파일 이름은 아무래도 상관없다 — 이름이 아니라 **안에 있는 열 이름**으로 종류를 판별한다.

### ① 계좌수익률 CSV — 자산 추이·수익률·입출금의 근거

날짜별 총 예탁자산과 입출금이 들어 있는 파일. 차트의 모든 기간(1일~전체)과
MDD·수익률 계산이 전부 이 파일에서 나온다.

**반드시 있어야 하는 열 4개**

- `일자`
- `예탁자산`
- `입금`
- `출금`

넷 중 하나라도 없으면 처리하지 않는다. 특히 `입금`·`출금`은 실제 외부
입출금을 구분하는 유일한 근거라서, 이 두 열이 빠진 요약본은 쓸 수 없다.

> **기간을 짧게 뽑지 말 것.** 내보내기 기간을 1개월로 잡으면 1개월치만
> 반영되고, 그 이전 구간은 기존에 반영된 값이 그대로 남는다. 처음부터
> 오늘까지 **전체 기간**으로 뽑는 것이 안전하다.

### ② 보유종목(잔고) CSV — 지금 뭘 얼마나 들고 있나

종목별 수량·매입단가. 자산 구성, 자산 맵, 종목별 비중이 여기서 나온다.

**반드시 있어야 하는 열 2개**

- `종목명`
- `평가손익`

`평가손익`은 화면에 그대로 쓰지 않는다. 우리가 원장에서 계산한 손익과
증권사 값이 맞는지 **교차검증**하는 데 쓴다. 그래서 이 열이 빠지면 받지 않는다.

---

## 3. 저장할 때 지켜야 할 것

- **CSV로 저장한다.** `.xlsx`, `.xls`는 처리하지 않고 실패 폴더로 간다.
  엑셀에서 열었다면 "다른 이름으로 저장 → CSV"로 내보낼 것.
- **인코딩은 신경 쓰지 않아도 된다.** 증권사가 바로 내려준 파일(EUC-KR)도,
  엑셀에서 "CSV UTF-8"로 다시 저장한 파일도 둘 다 읽는다.
- **다운로드가 끝난 뒤에 옮긴다.** 쓰는 중인 파일은 건너뛰었다가 다음 주기에
  다시 본다. 굳이 기다릴 필요는 없고, 알아서 처리된다.

---

## 4. 넣고 나면 어떻게 되나

```
cgportfolio-inbox\
├─ (여기에 CSV를 넣는다)
├─ 처리완료\   ← 반영에 성공한 파일이 날짜가 붙어 여기로 옮겨진다
└─ 실패\       ← 판별 못 했거나 반영에 실패한 파일
```

1. 10분마다 예약 실행이 폴더를 훑는다
2. 열 이름으로 종류를 판별한다 (판별 못 하면 **건드리지 않고** 실패 폴더로)
3. **반영 직전에 `data` 폴더를 통째로 백업한다** (`C:\Users\ACC-002\cgportfolio-backups`)
4. 반영하고, 파일을 `처리완료\`로 옮긴다

기록은 여기서 볼 수 있다:

```bash
type "%USERPROFILE%\cgportfolio-logs\inbox-import.log"
```

10분을 기다리기 싫으면 바로 한 번 돌릴 수 있다:

```bash
schtasks /run /tn "CGPORTFOLIO 받은CSV 가져오기"
```

---

## 5. 지원하지 **않는** 파일

아래 파일들은 넣어도 실패 폴더로 간다. 가져오기가 만들어져 있지 않다.

| 내용 | 왜 안 되나 |
|---|---|
| 거래내역 (체결 내역) | 한 건이 3줄로 쪼개져 있고, 환전이 한쪽 다리만 기록되어 있어 그대로 넣으면 현금 잔고가 어긋난다 |
| 입출금 내역 | 위와 같은 파일에서 파생되는 것이라 같은 문제가 있다 |
| 권리내역 (배당·청약) | 형식을 아직 다루지 않는다 |

배당은 계좌수익률 CSV의 `배당금액` 열로 이미 들어오고 있어서, 당장 아쉬운 것은 없다.

---

## 6. 사람이 **안 해도 되는** 것

이미 예약 실행으로 자동이다. 손댈 필요 없다.

| 예약 작업 | 언제 | 하는 일 |
|---|---|---|
| `CGPORTFOLIO 서버` | 부팅 시 | 사이트를 띄운다 (로그인 없이도 켜진다) |
| `CGPORTFOLIO 시세 갱신` | 매시 정각 | 주가·환율을 받아 스냅샷을 남긴다 |
| `CGPORTFOLIO 받은CSV 가져오기` | 10분마다 | 위의 CSV 처리 |
| `CGPORTFOLIO 데이터 백업` | 매일 새벽 3시 | `data` 폴더 백업 |
| `CGPORTFOLIO 주간 리포트` | 매주 월요일 오전 9시 | 테마·레버리지 노출 리포트 생성 |

---

## 7. 잘 안 될 때

**파일이 `실패\`로 갔다**
→ 로그의 사유를 먼저 본다. 대부분은 필요한 열이 빠진 경우다.
2번 항목의 열 이름이 **정확히** 그대로 있는지 확인할 것 (`예탁자산`을 `총평가금액`으로
바꿔 내보냈다면 못 알아본다).

**반영은 됐는데 숫자가 이상하다**
→ 되돌릴 수 있다. 반영 직전 백업이 `C:\Users\ACC-002\cgportfolio-backups`에
날짜별로 쌓여 있다.

**화면에 옛날 값이 보인다**
→ 브라우저 새로고침. 그래도 그대로면 상단 `UPDATE :` 시각을 보고, 시각이
안 바뀌었으면 아직 반영 전이다.

**사이트가 아예 안 열린다**
→ 재부팅 직후라면 2~3분 기다린다. 서버보다 네트워크 쪽이 늦게 올라온다.

---

## 8. 넣으면 안 되는 것

이 저장소는 **공개(Public)**다. 계좌 데이터는 저장소 밖에만 둔다.

- 증권사 CSV를 저장소 폴더(`Desktop\cgportfolio`) 안에 두지 말 것 — 받은 폴더는
  홈 폴더 아래(`C:\Users\ACC-002\cgportfolio-inbox`)에 일부러 떼어 놓았다
- 실제 금액·수익률을 커밋 메시지나 문서에 적지 말 것
- 로그인 비밀번호는 `npm run auth:setup`으로 본인이 직접 설정한다 — 채팅이나
  파일로 주고받지 않는다
